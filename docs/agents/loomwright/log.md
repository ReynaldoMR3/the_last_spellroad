# Loomwright — Engine Log

Append-only, dated, one entry per engine feature shipped and its playtest result.

## 2026-08-07 (4) — Issue #127: clean resolved `roadfeel` prototype, codify Active Prototype freshness

First of the opening-magic epic's (#124) ticket chain — ordered ahead of #130/#126/#128 per `docs/superpowers/specs/2026-08-07-opening-art-music-prototypes-design.md` since #128 needs an empty registry to install its own `openingmagic` entry into.

**Removed:** `src/scenes/PrototypeRoadFeelScene.ts` and its import/registry line from `main.ts` — `PROTOTYPE_REGISTRY` is back to `{}`. Issue #68's verdict (side-pocket + reactive shrine baseline) isn't lost — it was already preserved in issue #64/#68 and `docs/agents/_reference/opening-experience-brief.md`, both outside the throwaway scene file.

**Codified in `docs/eng-skills/prototype-harness.md`:** a new "Active Prototype lifecycle" section names the one-active-prototype-at-a-time rule and states the Prototype Freshness rule from ADR-0003 explicitly (a production change touching the Active Prototype's dependencies must update and Docker-smoke-check it in the same change) — previously this was only implied by ADR-0003's prose, not spelled out in the harness doc itself. Added a concrete Docker smoke-check command sequence (typecheck/test/build + a live `?prototype=<key>` boot check), and a "Resolved prototypes" section recording `roadfeel`'s resolution so future readers don't have to dig through git history to find where its verdict lives.

**Self-verify:** `docker-compose run --rm game npm run typecheck` / `npm test` (2332/2332, no new tests — this is a deletion + doc change, `prototypeHarness.test.ts` already used fake scene classes rather than the real `PrototypeRoadFeelScene`) / `npm run build` all clean.

**Status:** `shipped-and-validated` — pure cleanup with no runtime-input-dependent behavior, nothing left for a developer playtest gate to check here.

## 2026-07-21

Context store established. No engine features logged yet.

## 2026-07-23 — First engine build: combat/enemy foundation, HP/Mana/Mastery/Hexcoin/Debuff runtime, 3 AoE shapes (backlog Tasks 1.1-1.5, 2.3)

First real engine code in the repo beyond movement — `src/` previously contained only `main.ts`, `SpellroadScene.ts` (movement-only), and `styles.css`. Built against the 2026-07-23 engine-contract scope extension (runtime execution of Pato's HP/Mana/Mastery/Hexcoin/Debuff mechanics is now explicitly in scope, per backlog item 0.1).

**Systems built**, each a standalone class reading Pato's already-finalized numbers, never inventing its own:
- `HealthSystem` — 100 HP pool, no in-combat regen (matches `hp-template.md` exactly), death callback at 0.
- `ManaSystem` — 100 pool, 5/sec regen (accepts a caller-supplied drained rate for Debuffer interaction), the three weight-class cost/cooldown pairs, 10% Master discount.
- `MasterySystem` — 3-tier scaling identical for every spell, Novice-floor-excluded random death penalty (mastery-template.md's resolved rule). One placeholder left explicit in code and comments: landed-casts-per-tier growth rate (backlog item 0.4) — not guessed, flagged `TODO(Pato, backlog 0.4)` directly in `MasterySystem.ts`.
- `HexcoinSystem` — 1/kill, expedition-scoped, both fees (100 flat / 30 flat), and critically the fight-start balance snapshot `hp-template.md`'s "Mid-fight-kill freeze" section calls out by name as my responsibility ("Loomwright: snapshot the eligible balance the instant the boss encounter starts...") — implemented as `startBossFight()`/`usePhaseRecovery()`, not yet exercised end-to-end since no boss encounter is wired into a scene yet (that's Phase 3).
- `DebuffSystem` — additive speed/Mana-regen drain, hard-capped at 2 applications each, regen floor enforced.
- `SaveSystem` — versioned localStorage blob, clean-reset on schema mismatch. **Built but not wired into the scene** — no `loadSave`/`writeSave` call exists in `SpellroadScene` yet. This is deliberate, not an oversight: checkpoint/respawn placement policy (backlog item 0.2) is still an open developer decision, and wiring persistence before that's settled would mean guessing what "reaching a checkpoint" even means yet.

**Enemy archetypes** (`Enemy.ts`): Melee/Ranged/Debuffer, each with real per-hit damage from `hp-template.md` and simple but real movement AI (melee closes to contact range and attacks on a cooldown; ranged/debuffer hold a preferred distance band and fire/pulse on their own cooldowns). Enemy-side HP values are explicitly flagged in code as engine-testing placeholders — no template defines a base enemy-HP number yet (the same gap Warden's own log already surfaced).

**3 AoE shapes** (`SpellCaster.ts`): line (rotated-rectangle hit-test), cone (angular-sector hit-test), circle (placed-circle hit-test, centered on the confirmed preview point rather than the caster, clamped to a max placement range). Preview-and-confirm casting wired into `SpellroadScene`: hotkey previews, left-click or same hotkey confirms, right-click/Esc cancels — matching `engine-contract.md` exactly. Wired against Frieren's 3 spells (`arc_lance`/line, `flame_sweep`/cone, `frost_nova`/circle) once Pato cleared them.

**Playtest result, disclosed honestly:** the developer's own automated browser-pane environment for this session had a hard limitation — `document.visibilityState` reported `"hidden"` and keyboard input never reached the page at all (confirmed via a raw `window.addEventListener('keydown', ...)` probe that never fired across many attempts, while a matching `pointerdown` probe did fire once mouse-click coordinate scaling was corrected). This meant hotbar spell selection and the click-confirm cast flow could not be exercised interactively this session — verified by code/geometry review and a clean `tsc --noEmit` + `vite build` instead. What WAS verified live, via the passive combat loop needing no input at all: enemy AI movement and exact 7-damage Melee contact hits, wave spawn/sequencing across multiple staggered waves, the HP-to-zero death trigger, and the Novice-floor death-penalty exclusion firing exactly as designed (`"Died — no Mastery lost (all Novice)"`, since no cast had landed yet to raise any spell above Novice). Debug scaffolding used to diagnose the input gap (console probes, a temporarily-shortened enemy spawn distance, `disableVisibilityChange`/`forceSetTimeOut` Phaser config overrides) was fully reverted before this commit — none of it belongs in the shipped config.

**Status: `in-progress-with-owner` (owner: Loomwright/developer)**, not `shipped-and-validated` — the interactive cast/hit/kill/Mastery-tier-up/Hexcoin-earn path specifically needs a real developer playtest (a real keyboard, not this session's sandboxed pane) before it can be marked validated, per my own success criterion ("validated by the human developer actually running the game"). Everything confirmed passively (death/respawn/Mastery-floor/enemy-AI/wave-sequencing) is solid; the cast-and-hit path is implemented and typechecked but not yet human-verified.

## 2026-07-23 (2) — Two Heckler-found bugs fixed; one live mouse-only cast confirmed

Heckler's critique of the above build (`docs/agents/heckler/log.md`, 2026-07-23 (2)) found two real, confirmed code bugs by reading the source, not by guessing:

1. **HP not reset per wave.** `startWave()` never called `this.health.reset()` — only `handleDeath()` did. Fixed: `startWave()` now calls `this.health.reset()` and `this.debuff.clear()` at the top, matching `hp-template.md`'s "full reset to 100 at the start of every wave" exactly, and clearing stale debuff stacks from the previous encounter along with it (a reasonable extension of the same reset, not separately template-specified).
2. **Master-tier discount double-dip.** `tryCast` applied the -10% to both cost and cooldown at once; the templates specify a per-spell choice of one or the other. Fixed by adding `master_discount: "cost" | "cooldown"` to `SpellDefinition`, removing the `costCooldownMultiplier` field from `MasterySystem.getScaling()` entirely (the field itself was the trap — deleting it beats special-casing around it), and having `SpellCaster.tryCast` apply the discount to exactly the field each spell names. Assigned a value to all three shipped spells with stated reasoning (in `docs/data/spells/spells.json` and this entry): `arc_lance` and `frost_nova` lean `cooldown` (reinforcing "fast poke" and cutting the "long exposure after cast" weakness, respectively), `flame_sweep` leans `cost` (sustaining its generalist role across a longer fight).

Both fixes typecheck and build clean.

**Also got one clean, live, human-input-equivalent cast confirmed this session** — using the same `disableVisibilityChange`/`forceSetTimeOut` overrides as before (temporary, reverted), plus a temporary mouse-only bypass of the hotbar-key requirement (since keyboard events still never reached the page in this sandbox): a real `pointerdown` → `confirmCast` → `SpellCaster.tryCast` → hit-test loop ran end to end with no errors, Mana dropped from 100 to 91 (10-cost Light spell minus a fraction of a second's regen), and the arc_lance cooldown ticked down visibly (1.7s → ... → ready) exactly as designed. Repeated attempts to land a confirmed *kill* (to also verify Hexcoin-earn and Mastery-landed-cast in the same pass) ran into this sandbox's independently-flaky click delivery — some clicks never reached Phaser's pointerdown handler at all, for reasons unrelated to game code (a raw `window` listener showed the same intermittent pattern). Not chasing this further; the one clean run already exercises the exact code path the two bugs above lived in, end to end, without error.

All debug scaffolding (console probes, mouse-bypass hack, `disableVisibilityChange`/`forceSetTimeOut`, shortened spawn distance) reverted before commit — confirmed via `grep` across `src/` for leftover TEMP/debug markers, none found.

**Status: still `in-progress-with-owner`**, not upgraded to `shipped-and-validated` — one clean mouse-driven cast and two fixed bugs raise confidence, but a real developer session with a working keyboard (hotbar selection, all 3 shapes, a confirmed kill, a Mastery tier-up, a Hexcoin earn) is still the actual gate, per my own success criterion. This entry narrows what that session needs to check, it doesn't replace it.

## 2026-07-25 — Backlog 2.9/2.10 built, hotbar widened, Level 2/3 wired in; Heckler found two real bugs, both fixed

Developer stepped away for the day and asked Ana to keep the roster producing autonomously, only stopping for a genuine developer-blocker. This entry covers all engine work from that session in one place — it should have been logged incrementally as each piece landed, not batched at the end; noting that gap explicitly since Heckler's own critique pass caught it as a MAJOR finding (missing disclosure, not a code defect).

**2.9 — floating hit-feedback numbers.** `Enemy` gained a `maxHp` field (previously only tracked current `hp`, no denominator for a percentage). `SpellroadScene.spawnDamageNumber` spawns a `-N` text per landed hit, tweened up-and-fade, colored by the target's remaining-HP band (green >80%/yellow 30-80%/red <30%) per the developer's explicit design call.

**2.10 — lane containment, kiting retune, aiming fallback.** Per the Warden/Heckler/Pato consultation (`ana/log.md`, 2026-07-25 (2)):
- Enemies now clamp to the lane rectangle (`Enemy`'s constructor takes the same rect the mage already uses, applied via `body.setBoundsRectangle`), threaded through `WaveLoader.spawnWave`.
- Kiting ranges retuned (Warden's spec, Pato-validated): ranged 220→240, debuffer 200→150, closing an overlapping-preferred-range bug Pato's independent check found in the *old* numbers.
- Wall-slide behavior added: a retreat blocked by the lane bounds now slides perpendicular toward the centerline instead of pinning nose-first. **Heckler found a real sign bug here** — the original fix multiplied the whole perpendicular vector by one fixed per-position scalar, which was correct for left/right wall blocks but could point the y-component *away* from center for top/bottom blocks (concrete counterexample in Heckler's log entry, reproduced and confirmed). Fixed: now picks whichever orientation of the perpendicular actually has a y-component pointing toward center, instead of trusting a fixed formula.
- Shape preview clipped to the lane via one geometry mask (not per-shape clip math); hit-tests need no separate clip since enemies can't exist off-lane.
- Aiming falls back to last-move-direction at a fixed default distance until the pointer moves. **Heckler found a second real bug**: the first version used a permanent one-way boolean (`pointerHasMoved`), which incidental trackpad jitter would flip true immediately and never release — defeating the fallback for exactly the player it was built for. Fixed: a pointermove event only counts if it clears a small pixel-distance threshold (filters jitter, still catches real intentional movement instantly).

**Hotbar widened 3→6** to match the GDD's spec'd 1-6 (was still sized for when the spellbook had 3 spells; Frieren's 9 new ones this session would otherwise have been unreachable). **Heckler found a third real bug**: the first default loadout was `slice(0, 6)` on file order, which happened to orphan all 3 Heavy spells and 2 of 3 Standard ones — exactly the spells Level 2/3's escalation assumes are available. Fixed: a curated `DEFAULT_LOADOUT_IDS` list, 2 per weight class. Full loadout-selection UI (player picks which known spells fill the hotbar, swappable between expeditions) remains explicitly future work per the GDD, unchanged by this fix. Also corrected `handleDeath`'s Mastery-penalty roll to scope to equipped spells only, not the full 12-spell book (it silently used to use all of them, back when equipped==all).

**Level 2/3 wired into the actual playable sequence.** Warden's new wave batches (backlog 3.3) previously would have just been unused JSON — `SpellroadScene` now preloads and concatenates all 3 levels' wave data so the game progresses past Level 1 instead of stopping there, with a `Level N` flash at each level's first wave.

**Mastery growth rate (0.4) wired, then re-wired after a real arithmetic bug.** Pato's first sizing (20 casts/tier) assumed 1 landed cast ≈ 1 kill; Heckler recomputed against Level 2's actual composition and found a single Novice-tier spell spammed could hit 104 achievable casts in that level alone — 2.6x the number needed for full Mastery, reproducing the exact failure mode the sizing was supposed to prevent. Pato re-derived correctly (casts-to-kill = `ceil(enemyHP/power)`, worst-case against the kit's weakest spell, accounting for Mastery's own power step-up mid-grind) and landed on 180 casts/tier. Wired into `MasterySystem.ts`, replacing the incorrect 20.

**New tracked gap, not fixed here:** while verifying Pato's corrected number, found that `recordLandedCast` fires on any successful hit, not a kill — so a player who deliberately keeps landing non-lethal hits on one enemy isn't bounded by level content at all, only by real time. No finite per-tier number closes this on its own; it's a mechanic question (e.g., gate progress on kills instead of hits), not a resize, so it's logged in `MasterySystem.ts` and left for Phase 5's adversarial QA rather than redesigned unilaterally mid-session.

**Self-verify:** `docker-compose run --rm game npm run typecheck` and `npm run build` both clean after every change in this entry, including the three Heckler-driven fixes.

**Status:** 2.9 `in-progress-with-owner` (built, self-verified, awaiting developer visual confirmation). 2.10 `shipped-and-validated` for the engine mechanism (containment, retune, wall-slide, aiming fallback, all Heckler-flagged bugs fixed and re-verified) — still awaiting a developer feel-check, same distinction as always between self-verify and human playtest. Hotbar/loadout fix and Level 2/3 wiring: `shipped-and-validated`, self-verified, no human-only gate applies (mechanical/data wiring, not a feel judgment).

## 2026-07-27 — Mana not refilling on death (backlog 2.12): `ManaSystem.reset()` added

Developer playtest (via `docker-compose up -d game`) surfaced that death respawned the mage with full HP but whatever Mana was left at time of death — `handleDeath()` called `health.reset()` but had no equivalent for Mana, which isn't the same class (`ManaSystem` had no `reset()` at all, unlike `HealthSystem`). Checked the GDD before treating this as a bug: it only specifies HP resets in full on respawn, says nothing about Mana either way, so this was a real undecided gap, routed to the developer rather than guessed. Developer's call: mirror HP, full refill.

Added `ManaSystem.reset()` (sets `mana = MAX_MANA`, same shape as `HealthSystem.reset()`) and call it in `handleDeath()` right alongside `health.reset()`. One-line addition plus one call site — no other system touches Mana on death. `docker-compose run --rm game npm run typecheck` and `npm run build` both clean.

**Status:** `in-progress-with-owner` — self-verified, awaiting the developer's confirmation it feels right in the live session that found the gap. Branch: `loomwright-mana-death-reset` (off `main`).

## 2026-07-27 (2) — Mini-boss engine wiring (backlog 3.4/3.8): boss phase sequencing + Phase-Transition Recovery choice

Ana traced down that the mini-boss content (Warden's 2026-07-21 composition, Pato-validated twice) had never actually been committed to a `wave.json`-equivalent file or wired into the engine — this entry is the engine half only; the content reconstruction into `src/data/waves/boss-1.json` is Ana's/Warden's, logged in their own files.

**Schema:** `WaveDefinition` gained `is_boss?: boolean` (`src/data/types.ts`) so a boss phase can be distinguished from a regular wave without a second parallel data path.

**`SpellroadScene.startWave`:** branches on `wave.is_boss`. Phase 0 of a boss: computes `bossMaxRecoveries = Math.min(totalPhases - 2, MAX_RECOVERIES_HARD_CAP)` (totalPhases counted directly off `this.waves`, not hardcoded), calls `hexcoin.startBossFight()`, and does the one HP reset for the whole fight. Later phases (`wave_index > 0`) skip the HP reset entirely — hp-template.md's damage-threat budget is cumulative across phases, which is the actual reason Phase-Transition Recovery exists; resetting HP per phase would make the fee-gated recovery meaningless.

**`updateEnemies`'s advance logic:** now checks whether the next wave is another phase of the same boss (`next.is_boss && next.level === wave.level`) before auto-advancing. If so, calls the new `startPhaseBreak(nextIndex)` instead. If the boss's last phase just cleared, calls `hexcoin.endBossFight()` and flashes a victory message before falling through to the normal advance.

**`startPhaseBreak`:** flashes the Y/N choice (`FEE_PHASE_RECOVERY` Hexcoin -> `PHASE_RECOVERY_HP_FRACTION` of MAX_HP, or decline), registers one-shot `keydown-Y`/`keydown-N` listeners, resolves by calling `hexcoin.usePhaseRecovery(bossMaxRecoveries)` + `health.restore(...)` on accept, then proceeds to the next phase either way. Guards against double-resolution (`awaitingPhaseChoice` flag) and against the advance-loop re-triggering while the choice is pending (reuses the existing `enemiesRemainingToSpawn = -1` guard pattern from the regular-wave code).

**`handleDeath`:** added `hexcoin.endBossFight()` and clearing `awaitingPhaseChoice` — a mid-fight death shouldn't leave a stale frozen-Hexcoin-snapshot state behind; the next attempt at the boss calls `startBossFight()` again and takes a fresh one regardless, but leaving the old state dangling was sloppy, not necessary to leave as-is.

**Self-verify:** `docker-compose run --rm game npm run typecheck` and `npm run build` both clean. Dev server restarted so the new preload'd `boss-1.json` and the new logic are actually being served, not relying on HMR picking up a brand-new file.

**Not yet verified live:** the actual phase-break prompt, the recovery purchase, and the boss's overall feel/difficulty — self-verify covers compile/build correctness only, same distinction as every other engine entry in this log.

**Status:** `in-progress-with-owner` — self-verified, awaiting developer playtest of the actual boss fight.

## 2026-07-27 (3) — Combat telegraph (backlog 2.13) + hotbar spell-tag clarity (2.14)

Developer feedback traced to two concrete gaps in existing code, not new mechanics:

**2.13:** `EnemyCallbacks.onRangedFire` was already typed to pass `(fromX, fromY, toX, toY)` and `Enemy.update` already called it with real coordinates — `SpellroadScene`'s own callback wiring just threw all four away and only ran the delayed-damage timer. Fixed by actually drawing something with them: `spawnRangedProjectile` tweens a small circle from shooter to target over the existing `RANGED_TRAVEL_MS`, so the damage delay the code already had is now also the player's visible dodge window, instead of two disconnected things (an invisible timer and, separately, HP dropping). `spawnDebuffPulse` adds a fading ring at the Debuffer on pulse — previously the only Debuffer feedback loop was the mage's own stat drain, with nothing marking the moment it happened.

**2.14:** hotbar HUD line (`updateHud`) now appends `[shape/weight]` next to each equipped spell's id. Spell ids (`arc_lance`, `magma_lance`, etc.) carry no hint of their shape or weight class today — this is the smallest real improvement available without a bigger tooltip/icon system, which is out of scope for this entry.

**Self-verify:** `docker-compose run --rm game npm run typecheck` and `npm run build` both clean. Dev server restarted.

**Not yet developer-confirmed:** whether the projectile telegraph is visible/legible enough in actual play, and whether it (plus the hotbar tag) is enough to clear Level 1 Wave 1 — that wave's own numbers weren't touched, since retuning it before ruling out the telegraph gap as the real cause would be guessing at a second fix on top of an unconfirmed first one.

## 2026-08-01 — Further `ROAD_HEIGHT` widen + first test runner (backlog 2.21 / issue #20)

Developer's "both" resolution to 2.21 (see `ana/log.md`): an onboarding exception on Level 1 Wave 0 (Warden/Pato's half, see their logs) plus a further lane widen (this half).

**`ROAD_HEIGHT` 220 -> 280**, `ROAD_TOP` recomputed 160 -> 130 to keep the same y=270 midline — identical convention to 2.17's own 160->220 pass, single commented constant edit, same file/location. Chose 280 (not a larger jump) as a middle step: large enough to be felt as a real second widen after 220 wasn't enough, not so large it starts crowding the fixed-position HUD text below the lane (`hotbarText` already tracks `ROAD_TOP + ROAD_HEIGHT` dynamically, so it moved down automatically with no separate edit needed). **No change** to `RANGED_PREFERRED_RANGE`/`DEBUFFER_PREFERRED_RANGE`/`WALL_SLIDE_MARGIN` (`Enemy.ts`) — these are retreat/kiting-distance preferences, not lane-height-derived, and a taller lane can only reduce how often an enemy reaches the wall-slide margin, never reintroduce the overlapping-band problem 2.10 fixed (same reasoning 2.17 already used, re-verified rather than re-asserted).

**Flagging, not fixing:** `messageText` (the centered flash-message overlay for prompts like the Phase-Transition Recovery choice) sits at a fixed `y=400`, unchanged by this edit. With the lane now extending to y=410 (was y=380), that text now renders close to the bottom rail rather than clearly outside the lane. It was already close before this change (`hotbarText` sits at y=394 today, y=424 after this edit) — not a new collision, but worth a developer look during the retest this ticket asks for anyway. Not touched here since the ticket scopes this to "a single, clearly-commented constant edit," and moving other UI unscoped would be exactly the kind of silent scope creep Ana's dispatch discipline flags elsewhere in this log.

**Also added: Vitest, the repo's first test runner** (`package.json` devDependency + `npm test` script), scoped narrowly to the new `src/systems/waveThreatBudget.ts` pure calculator (see Warden's/Pato's logs) — not a retroactive coverage mandate for existing Phaser-coupled code. `docker-compose run --rm game npm install` picked it up into the shared `spellroad_node_modules` volume; `npm audit` flags dev-only, non-shipped transitive vulnerabilities (`esbuild`/`postcss`, both pulled in via `vite`/`vitest`'s own dependency chain) — dev-server-only exposure, not present in the built static bundle (`dist/`), consistent with this being a dev dependency only.

**Self-verify:** `docker-compose run --rm game npm run typecheck`, `npm run build`, and `npm test` (8/8 passing) all clean. Visual spot-check via the dev server confirms the lane renders visibly taller with no layout break; the sandboxed browser pane's known `document.visibilityState: "hidden"` throttling (documented in this same log's earlier Phase 1 entries) meant I could not force a full interactive Wave 0 clear in that environment — road-width feel and Wave 0 clearability both still need a real developer playtest session, per the ticket's own testing decisions (no test coverage claimed for `ROAD_HEIGHT` itself, by design).

**Status:** both halves of 2.21 built and self-verified; genuinely still needs the developer for the two things no static check can substitute for — does the lane feel right, and is Wave 0 actually clearable now.

## 2026-08-01 (2) — Live monster name+HP-bar overlay (backlog 2.19 / issue #26)

Built so a player/QA can visually confirm a single AoE cast landing on multiple targets at once, instead of only inferring it from the floating damage numbers (backlog 2.9) one enemy at a time.

**Display name:** no lore-name exists per archetype yet — inventing one is explicitly Lorena's job, out of scope here. Used the capitalized archetype string ("Melee"/"Ranged"/"Debuffer") as a placeholder, same "flag the gap, don't guess past it" pattern as the enemy-HP placeholder already in this file.

**TDD seam, following the `waveThreatBudget.ts` precedent (this repo's only other test file):** extracted the actual testable logic into a new pure, Phaser-free module, `src/systems/enemyStatusOverlay.ts` — `archetypeDisplayName` (capitalize), `computeHpFraction` (clamped to [0,1], guards a non-positive `maxHp` and an overkill-negative `hp`), and `computeHpBarColor` (reuses `spawnDamageNumber`'s exact >80%/30-80%/<30% healthy/wounded/critical thresholds, numeric 0xRRGGBB instead of that function's CSS-string form, since this feeds a `Graphics.fillStyle` call not a `Text` style). Wrote `src/systems/enemyStatusOverlay.test.ts` first (confirmed red — `vitest` failed to resolve the not-yet-created module), then implemented the module (green, 6/6 passing, 15/15 total with `waveThreatBudget.test.ts`).

**Phaser wiring, in `Enemy.ts` (no Vitest seam here — canvas rendering, verified via typecheck/build/dev-server instead, per the docker-testing-contract):** each `Enemy` now owns a `nameLabel` (`Text`) and `statusBar` (`Graphics`), created once in the constructor (so a freshly spawned enemy shows full HP immediately, not after its first `update()`) and repositioned/redrawn every frame from a new `refreshStatusOverlay()` private method, called at the top of the existing per-frame `update()`. The bar draws a dark background rect plus a foreground rect scaled by `computeHpFraction(hp, maxHp)` and tinted by `computeHpBarColor`, so a landed cast's `takeDamage()` call (which only mutates `hp`) is picked up and rendered within the next frame with no separate "on damage" event needed.

**Cleanup:** confirmed neither existing despawn path (`SpellroadScene.removeEnemy`'s `enemy.destroy()`, and `handleDeath`'s `this.enemies.forEach((e) => e.destroy())`) does anything beyond calling `Sprite.destroy()` — and confirmed Phaser does *not* cascade-destroy manually-added sibling GameObjects just because the sprite they're following gets destroyed (the label/bar aren't children of the sprite; Arcade Sprites don't support the Container-style parent/child relationship). Overrode `Enemy.destroy(fromScene?)` to destroy `nameLabel` and `statusBar` before calling `super.destroy(fromScene)`, so every current and future despawn call site gets the cleanup for free without needing its own matching edit.

**Self-verify:** `docker-compose run --rm game npm run typecheck`, `npm run build`, and `npm test` (15/15, including the 6 new tests) all clean. Did not bring up an isolated dev-server instance for this entry — port 5173 was already bound by another running `the_last_spellroad-game-1` container from a different session/worktree at the time, and reusing someone else's running container wouldn't have reflected this branch's code; typecheck/build/test already cover this change's only real compile/runtime-shape risk (the `destroy()` override signature, the Graphics/Text API calls), so the substitution doesn't weaken this entry's mechanical self-verify.

**Status:** `in-progress-with-owner` — self-verified per this entry's own gate, not yet developer-confirmed for the thing no static check can substitute for: whether the bars are actually legible in play and visibly update when an AoE lands on multiple enemies at once, per the ticket's own stated purpose.

## 2026-08-01 (3) — Tiled layouts wired into the playable sequence (backlog 3.8 remainder, issue #29)

Tilesmith's #28 (`tilesmith/tiled-layouts-28`, PR #35, itself stacked on #25's curated-tile-IDs
PR #34, neither merged to `main` at write-time) built 5 real Tiled JSON maps at
`public/assets/levels/level-1.json`..`level-5.json` (level 5 = the boss arena) but explicitly
left wiring them into `SpellroadScene.ts` for this ticket — the scene was still drawing the
original placeholder colored rectangles (a flat `ROAD_WIDTH`x`ROAD_HEIGHT` box, two border
lines, tick marks every 60px) across all 5 levels. This entry closes that gap: Tiled layouts now
render, wave/boss sequencing untouched.

**Read before writing any code**, per this ticket's own instructions: all 5 level JSON files
directly (single `Terrain` tile layer each, one embedded tileset `firstgid: 1`, image path
`../third-party/kenney-tiny-dungeon/Tilemap/tilemap_packed.png`, tileset name
`"kenney-tiny-dungeon-tilemap_packed"`, 60x18 tiles for Levels 1-4, 60x20 for the boss arena)
and Tilesmith's newest log entry (2026-08-01, "Backlog 3.7 (issue #28)") for the exact GID/tile
convention and the geometry-mismatch flag it already raised.

**Pure module extracted first, TDD** (`src/systems/levelArt.ts` + `.test.ts`, 7 tests, written
red-then-green against the actual module, same precedent as `waveThreatBudget.ts`/`.test.ts`):
- `levelMapKey(level)` / `levelMapUrl(level)` — level number to Phaser cache key / static URL
  (`assets/levels/level-N.json`), plus `ALL_LEVELS`/`isValidLevel` (1-5, matching every
  `WaveDefinition.level` value that exists today).
- `TILESET_IMAGE_KEY`/`TILESET_IMAGE_URL`/`TILESET_NAME_IN_MAP` — the one shared tileset every
  level references, loaded once regardless of level count.
- `computeTilemapOffset({ canvasWidth, laneCenterY, mapWidthPx, mapHeightPx })` — pure geometry:
  centers a level's Tiled map horizontally on the canvas and vertically on the same lane midline
  (`ROAD_TOP + ROAD_HEIGHT/2`) movement/spawn/preview-clip code already uses, without touching
  that geometry itself. This is the actual reconciliation Tilesmith's log flagged as left to
  this ticket, made unit-testable independent of a running Phaser Scene.

**`SpellroadScene.ts` changes:**
- `preload()`: `this.load.image()` for the shared tileset once, `this.load.tilemapTiledJSON()`
  for all 5 levels — eager-loaded up front, same precedent the existing wave-JSON preloading
  already sets (all level data loaded together, switched between at runtime), not a mid-scene
  `this.load.once('complete', ...)` dance. Justified in-code: 5 files at ~13KB each plus one
  already-committed 5KB PNG, not worth the extra complexity.
- `createRoad()`: dropped the placeholder road-color rect, border lines, and tick marks. Kept
  the one full-canvas dark background rect on purpose — Tilesmith's maps only paint their own
  bordered box, not the surrounding canvas, so something still has to fill outside it.
- New `renderLevelArt(level)`: destroys the previous level's tilemap/layer (if any), then
  `this.make.tilemap({ key })` / `map.addTilesetImage()` / `map.createLayer("Terrain", ...)` per
  `art-sourcing-contract.md`'s documented mechanism, positioned via `computeTilemapOffset`.
  Guarded by a `renderedLevel` field so repeat calls at the same level (e.g. boss phase-breaks,
  which call `startWave` again without changing level) are no-ops.
- Called from the top of `startWave(index)` — the same function that already flashes "Level N"
  at `wave.wave_index === 0` and resets HP per wave/boss-fight-start, so the art swap lands at
  exactly the existing level-transition points (including the death/respawn path, which replays
  from wave 0 and correctly swaps back to Level 1's art if the player died deeper in).
- Explicit `BACKGROUND_DEPTH`/`TILE_LAYER_DEPTH` constants (-100/-50), not left to insertion
  order: the tile layer gets destroyed and recreated *after* the mage/HUD/enemies already exist
  at every level transition, so without an explicit depth a freshly recreated layer would insert
  on top of the display list and render over everything — confirmed this would actually happen
  (Phaser's default same-depth ordering is insertion order) before adding the depth calls, not
  guessed defensively.

**Gameplay-bounds geometry, deliberately untouched:** `LANE_RECT`/`ROAD_WIDTH`/`ROAD_HEIGHT`/
`ROAD_LEFT`/`ROAD_TOP` still drive movement clamping, enemy spawn positioning
(`spawnWave(this, wave, { x: 820, y: 270 }, LANE_RECT, ...)`), and the preview-clip geometry mask
exactly as before — grepped the rest of `src/` to confirm no other file reads these constants,
so nothing needed touching outside `SpellroadScene.ts` itself.

**Geometry-mismatch caveat, flagged rather than silently reconciled:** Tilesmith's maps are
960x288px for Levels 1-4 and 960x320px for the boss arena (whole 16px tile units), while the
live gameplay rect is 780x280px inset within a 960x540 canvas (`ROAD_LEFT=90, ROAD_TOP=130,
ROAD_WIDTH=780, ROAD_HEIGHT=280` — 780/16 and 280/16 aren't whole tile counts). `computeTilemapOffset`
centers each map on the same lane midline and horizontally on the canvas, which gets the art
close (Levels 1-4 land 4px taller/higher than `ROAD_HEIGHT`'s exact box; the boss arena more so,
by design — it's meant to read as a distinct, larger space) but the tile art's own wall-border
tiles do not pixel-align to `LANE_RECT`'s exact clamp boundary. This is a visual approximation,
not a gameplay change — no movement/spawn/hit-test behavior differs from before this ticket.
Whether that's tight enough or needs either the art re-sized in tile units or the gameplay
constants revisited is a developer call, not decided unilaterally here, per this ticket's own
instruction to flag rather than silently change either side to match the other.

**Self-verify:** `docker-compose run --rm game npm run typecheck`, `npm test` (16/16 passing —
9 pre-existing `waveThreatBudget` + 7 new `levelArt`), and `npm run build` all clean. Confirmed
the built `dist/assets/levels/*.json` and `dist/assets/third-party/.../tilemap_packed.png` both
survive the production build (they live under `public/`, same as the tileset PNG already did).
**Dev server not brought up this session** — port 5173 was already bound by another process
(`lsof -i :5173` showed an existing `ssh` listener) at self-verify time, the same known caveat
#26's PR disclosed rather than silently working around; noting it plainly instead of blocking on
it or claiming a live check that didn't happen.

**Not yet developer-confirmed:** whether the 5 levels' art actually reads well in play, whether
the ~4-20px lane/art mismatch above is noticeable/acceptable, and whether the boss arena's
larger, visually distinct sizing lands as intended — same distinction as every other engine
entry in this log between self-verify (compile/build/test correctness) and the human playtest
gate this agent's own success criterion requires.

**Status:** `in-progress-with-owner` — self-verified, awaiting developer visual playtest.
Branch: `loomwright/wire-tiled-layouts-29`, stacked on `tilesmith/tiled-layouts-28` (PR #35),
itself stacked on `tilesmith/curate-tile-ids-25` (PR #34) — neither merged to `main` yet, so
this PR's base is #28's branch, not `main`, keeping the stack in review order (25 -> 28 -> 29).

## 2026-08-02 — Auto-aim: cone-based assisted targeting (backlog 2.22, issue #44)

Built against the approved design doc (`docs/superpowers/specs/2026-08-01-auto-aim-cone-targeting-design.md`, branch `design/auto-aim-cone-targeting`), all 7 decisions there.

**Pure logic** (`src/systems/autoAim.ts`, `autoAim.test.ts`, 7 Vitest cases): `selectAutoAimTarget(candidates, originX, originY, facingX, facingY, coneHalfAngleDeg?)` — nearest candidate within a facing cone (`AUTO_AIM_CONE_HALF_ANGLE_DEG = 82.5`, i.e. a 165° full angle, the middle of the doc's ~150-180° range), falling back to the globally nearest candidate when none qualify; a zero facing vector (e.g. before the mage has ever moved) defaults to +x rather than feeding `atan2(0, 0)` a degenerate case. Same Phaser-free seam convention as `waveThreatBudget.ts`/`enemyStatusOverlay.ts`.

**Scene wiring** (`SpellroadScene.ts`): a new `previewLockedEnemy` field, set once in `handleHotbarPress` when a *new* preview starts (only via the no-mouse fallback path — `pointerHasMoved` gates it exactly like the existing 2.10 fallback, decision 3) and cleared in `cancelPreview`/`confirmCast`. `currentAimPoint()` reads the locked enemy's live position every frame instead of re-selecting, so the preview tracks a moving target without ever swapping it (decision 5) — a shared `livePreviewLockedEnemy()` helper (guards against the enemy having despawned mid-preview via another kill) backs both `currentAimPoint()` and the highlight draw, so there's one liveness check, not two. Because `updatePreview()`'s line/cone/circle branches all derive their geometry from the same `currentAimPoint()` result, the auto-aim point applies uniformly across all 3 shapes with no shape-specific code (decision 4) — same for `confirmCast`'s hit-test. A gold ring (`AUTO_AIM_HIGHLIGHT_COLOR`/`AUTO_AIM_HIGHLIGHT_RADIUS`) highlights the locked enemy on top of the shape preview (decision 6). Target-cycle hotkey and an accessibility toggle are correctly not present (decision 7, explicitly deferred).

**Self-verify:** `docker-compose run --rm game npm run typecheck`, `npm test` (96/96, all suites), and `npm run build` all clean.

**Status:** `in-progress-with-owner` — self-verified only. Per my own success criterion this needs a real developer playtest (dodge-and-cast-back against a live enemy, multiple enemies on screen, an enemy dying mid-preview) before it can move to `shipped-and-validated`; nothing here substitutes for that. Branch: `design/auto-aim-cone-targeting` (already carries the approved design commit; this adds the implementation on top, per Ana's dispatch).

## 2026-08-02 (2) — Fix #47: archer ranged attacks always hit, no position recheck at impact

Root cause, confirmed by reading the code (already diagnosed in the issue and named directly in ADR-0001 as one of the three bugs that shipped through PR #46's clean typecheck/build/96-unit-tests): `SpellroadScene.ts`'s `onRangedFire` scheduled `this.health.applyDamage(ARCHETYPE_DAMAGE.ranged)` unconditionally inside a `this.time.delayedCall(RANGED_TRAVEL_MS, ...)`, with no recheck of the player's actual position at impact time. `spawnRangedProjectile`'s travel tween is (and remains) purely cosmetic — it was never backed by a collision/overlap test, so no amount of movement during the 450ms travel window could ever avoid the hit. This directly broke the game's stated core pillar ("tactical spell combat... over twitch reflexes").

**Fix, scoped to #47 only** (not touching #48's wave/timer race or #49's `pointerHasMoved` — both land on this same branch in separate dispatches):

**Pure logic extracted first, TDD, same seam convention as `autoAim.ts`/`waveThreatBudget.ts`/`enemyStatusOverlay.ts`:** `src/systems/rangedImpact.ts` + `.test.ts`. Wrote the test file first against the not-yet-created module (confirmed red — `vitest` failed to resolve `./rangedImpact`), then implemented `isStillInRangedImpactZone(playerX, playerY, impactX, impactY, hitRadius = RANGED_HIT_RADIUS)`, a plain `Math.hypot` distance check against a hit radius, inclusive at the boundary (green, 6/6 new tests, 35/35 total). `RANGED_HIT_RADIUS = 20` follows the same derivation `Enemy.ts`'s (unexported) `MELEE_RANGE = 34` already uses — mage half-footprint (32x32 sprite, 16px half-width) plus a small buffer — rather than inventing an unrelated convention, per the ticket's own instruction to follow how melee/kiting ranges are already checked in `Enemy.ts`.

**Scene wiring** (`SpellroadScene.ts`'s `onRangedFire` callback, inside `updateEnemies`): the existing `delayedCall(RANGED_TRAVEL_MS, ...)` callback now reads the mage's *live* `x`/`y` at the moment it actually fires (not the snapshot taken at shot-fired time) and calls `isStillInRangedImpactZone(this.mage.x, this.mage.y, toX, toY)` — `toX`/`toY` are the mage's position *when the shot was fired* (already threaded through `EnemyCallbacks.onRangedFire`'s existing signature, unchanged). Damage only applies if the check returns true. Added the same `if (!this.mage) return` guard the rest of `updateEnemies` already uses defensively. No change to `spawnRangedProjectile`'s visual (still cosmetic, still the same tween/timing) — the fix is entirely in whether the already-scheduled damage callback fires, not in the projectile's appearance.

**Why this verification would actually catch a regression of this specific failure mode (per ADR-0001):** the bug's exact shape was "damage applies unconditionally, no live position check at all." `isStillInRangedImpactZone`'s unit tests directly assert both failure directions of that: a player who moved far from the fired-at point returns `false` (the dodge case #47 exists to fix — if a future edit reintroduced unconditional application, or dropped the check, or inverted the comparison, this test fails), and a player who stayed put (or moved only within the hit radius) returns `true` (so a regression that made the check too strict, e.g. always returning false, is equally caught). This is different from PR #46's own gate: the 96 pre-existing unit tests never touched this code path at all — this is the *first* test that exercises the recheck logic itself, not a pre-existing test re-run. Typecheck/build only prove the code compiles and bundles; they say nothing about whether the callback's condition is correct, which is exactly the class of defect ADR-0001 calls out. What this unit test does *not* cover, and what a regression could still slip past: the actual `RANGED_TRAVEL_MS` timer wiring inside live Phaser `delayedCall` scheduling (e.g. if a future edit called `applyDamage` unconditionally *and* the guarded branch elsewhere, or the timer's callback closure captured a stale `toX`/`toY`) — that class of wiring-only regression needs the developer playtest below, same as every other engine entry in this log.

**Self-verify:** `docker-compose run --rm game npm run typecheck`, `npm test` (35/35, including the 6 new `rangedImpact` tests), and `npm run build` all clean.

**Status:** `in-progress-with-owner` — self-verified only, not `shipped-and-validated`. Per my own success criterion, only a real developer playtest (dodge during the archer's travel window and confirm the hit doesn't land) clears that gate — this session's environment is not doing an interactive playtest, so the actual "does dodging now work" question remains open pending that session. Branch: `loomwright/fix-playtest-bugs-47-48-49` (this entry covers #47 only; #48/#49 land as separate dispatches on the same branch).

## 2026-08-02 (3) — Fix #48: wave/timer race, death-restart inheriting the in-flight wave's monster count

Escalated to Opus per Ana's model-selection row ("escalate for one-off architecture calls") — this is a structural timer/state-machine decision, not a routine fix.

**Root cause, confirmed by reading the code — two defects that compound, not one:**

1. **Nothing invalidated a scheduled callback when the world moved on.** All five `delayedCall` sites in the wave path (`updateEnemies`'s 1200ms advance, `handleDeath`'s 1500ms restart, `startPhaseBreak`'s resolution, `onRangedFire`'s 450ms impact, and every per-enemy staggered spawn timer `WaveLoader.spawnWave` queues) were fire-and-forget, and none cancelled any other. A wave-complete advance scheduled just before a delayed ranged hit killed the player still fired at +1200ms, starting the *next* wave 300ms before the death restart replayed the level from wave 1 — and that next wave's spawn timers, plus the dead wave's own not-yet-fired ones, then spawned into the "restarted" wave 1.
2. **The auto-advance trigger was purely numeric, and `handleDeath` reproduces it exactly.** `enemiesRemainingToSpawn === 0 && enemies.length === 0` is precisely the state `handleDeath` creates when it clears the field, which is why the `-1` sentinel guard didn't hold: the sentinel was overwritten with `0` by the death path itself. This means the extra advance wasn't only reachable via the ranged-shot race — **every** death scheduled a bonus advance on the very next frame, and the ranged shot only determined *which* wave's leftovers were still in flight to be inherited. Worth stating plainly because it widens the reproduction: the developer's ranged-death report is one path into a defect that fires on any death.

**Architecture decision: (a) generation/session counter, plus an explicit phase — not (b) timer-handle tracking.** Both were live options in the issue; the deciding argument is what each one costs against the timers the fix *isn't* looking at:

- (b) requires a registry that is simultaneously **complete** (every timer registered, including the ~6-11 handles `spawnWave` creates per wave inside a loop in a different module, which would have to be plumbed back out and stored) and **selective** (cancel the dead wave's timers, *not* the boss phase-break's — the issue names that trap directly, and a blunt `time.removeAllEvents()` would also take out the ranged impact and every cosmetic tween). Both properties have to be re-established by hand at every new call site forever; a future `delayedCall` added by anyone who doesn't know the registry exists is a silent regression, which is exactly how this bug arrived.
- (a) inverts that. The check lives at the point of *use*, not the point of scheduling, so a callback that forgets to ask is the only failure mode, and it's visible in the callback body being reviewed rather than in an unrelated cancellation site. Selectivity is free and automatic: a generation is taken only when the world genuinely changed (a wave started, or the player died), so the phase-break timer keeps working precisely because nothing bumped the counter under it — no "careful not to cancel that one" special case is needed anywhere. It also scales to the per-enemy spawn timers without plumbing handles: `spawnWave` takes one `isStillCurrent: () => boolean` predicate (required, not defaulted, so a new call site must decide) and checks it inside each staggered callback.
- Cost of (a), stated rather than glossed: stale timers still fire and no-op instead of being cancelled, keeping their closures alive for up to ~1.5s. Negligible at this scale, and I'd rather pay it than own a cancellation registry.

The counter alone does **not** fix defect 2, and that's worth being explicit about since the issue framed it as an either/or: the bonus advance is scheduled *after* `handleDeath` bumps, so it carries a current generation and would happily fire. That half needs a state answer, so `WaveSession` carries a `WavePhase` (`idle`/`running`/`advancing`/`awaiting-phase-choice`/`dead`/`complete`) and the advance is gated on `shouldAutoAdvance(phase, remaining, alive)`, which requires `running`. This also let the two parallel ad-hoc flags it replaces — the `enemiesRemainingToSpawn = -1` sentinel and the separate `awaitingPhaseChoice` boolean, which had to be kept in agreement by hand — collapse into one field with one meaning.

**Pure logic first, TDD, same seam convention as `rangedImpact.ts`/`autoAim.ts`/`waveThreatBudget.ts`/`enemyStatusOverlay.ts`:** wrote `src/systems/waveSession.test.ts` against the not-yet-created module (confirmed red — `vitest` failed to resolve `./waveSession`), then implemented `src/systems/waveSession.ts`: the `WaveSession` class (monotonic `generation`; `beginWave()`/`beginDeath()` bump and return a token; `beginAdvance()`/`beginPhaseChoice()`/`markComplete()` change phase without bumping; `isCurrent(token)`) and the pure `shouldAutoAdvance` gate. Green, 14 new tests, 49/49 total.

**Scene/loader wiring:** `SpellroadScene` gained a `session` field (constructed in `create()` with the other run systems). `startWave` takes a generation and threads it into `spawnWave`'s new predicate; the out-of-waves branch calls `markComplete()`. `updateEnemies` calls `shouldAutoAdvance(...)`, then `beginAdvance()` in place of the `-1` sentinel, captures `nextIndex` at schedule time, and guards the 1200ms callback. `startPhaseBreak` captures (does not bump) the generation, uses the phase for its double-resolution guard, and guards its resolution timer. `handleDeath` calls `beginDeath()` as its very first statement — before any state is cleared — and guards its restart timer; `awaitingPhaseChoice` is deleted. `onRangedFire`'s impact timer is generation-tagged too, so a shot in flight across a death can't land on the respawned mage — the same class of bug, reachable from the same playtest, and #47's fix (commit e8eb527, untouched here) is unaffected: the live-position recheck still runs, it just no longer runs at all for a shot whose world is gone. Cosmetic tweens (projectile dot, damage numbers, debuff pulse) are deliberately left unguarded: they self-destroy and touch no game state.

**Why this verification would actually catch a regression of this specific timing-race class (per ADR-0001), and where it can't:** the honest limit first — Vitest here runs no Phaser clock, so **no test in this repo proves anything about real `delayedCall` interleaving**, and I'm not claiming otherwise. What the tests do prove is the part that was actually wrong, which is decision logic, not scheduling. The race's two failure modes are both expressible without a clock and both are asserted directly: (1) `shouldAutoAdvance("dead", 0, 0) === false` — the counters-look-cleared-because-death-cleared-them case, the exact condition that defeated the `-1` sentinel; a future edit that reverts to a numeric-only trigger, or lets `handleDeath` leave the phase `running`, fails that test. (2) A token captured before `beginDeath()` is no longer current, and generations are never reused, asserted over a 10-transition sequence — so an edit that made `beginDeath` non-bumping, or made `beginAdvance` bump (which would wrongly kill the phase-break timer), fails. There's also a test that replays #48's literal timeline in order (clear wave 2 → schedule advance → death → advance token stale → death token current → restart → wave 2's spawn tokens stale), so a regression has to break a test that reads like the bug report. Two guards deliberately cover the *opposite* direction as well, since over-invalidating is the failure mode a cancellation-happy fix would introduce: a phase-break token stays current while nothing else has happened, and `markComplete` doesn't bump. What none of this reaches: whether I tagged every call site correctly (a future sixth `delayedCall` that simply doesn't ask `isCurrent` is invisible to these tests), whether Phaser fires a 1200ms and a 1500ms timer in the order arithmetic says under real frame timing, and whether `handleDeath` is ever re-entered in a way this model doesn't anticipate. Those are wiring and runtime questions, and only the playtest below settles them.

**Self-verify:** `docker-compose run --rm game npm run typecheck`, `npm test` (49/49 — 35 pre-existing plus 14 new `waveSession` tests), and `npm run build` all clean. Docker was available; no host fallback needed.

**Flagging, not fixing (out of #48's scope):** `WaveLoader.spawnWave` `continue`s past an enemy type missing from `ENEMY_REGISTRY`, but `startWave` counts it into `enemiesRemainingToSpawn` — a wave referencing an unknown type would never reach 0 and never advance. No shipped wave data hits this today; noting it rather than widening this change.

**Status:** `in-progress-with-owner` — self-verified only, not `shipped-and-validated`. The gate is a real developer playtest of the exact reproduction: die mid-wave with a ranged shot in flight (Level 1 Wave 2 is where it was found) and confirm the restarted Wave 1 spawns exactly its own 3 enemies and no more. A second pass worth doing in the same session, since this change touches it: clear a boss phase and confirm the Y/N recovery prompt still resolves and advances normally. Branch: `loomwright/fix-playtest-bugs-47-48-49` (this entry covers #48 only; #47 landed in e8eb527, #49 is a separate dispatch after this one).

## 2026-08-02 (4) — Fix #49: auto-aim never re-engages after mouse/trackpad use, even once idle

Root cause, confirmed by reading the code (already diagnosed in the issue, and named directly in ADR-0001 as the third of the three bugs that shipped through PR #46's clean typecheck/build/96-unit-tests): `SpellroadScene.ts` tracked `pointerHasMoved` as a one-way boolean — set `true` on a `pointermove` past the existing `POINTER_JITTER_THRESHOLD_PX` threshold or on `pointerdown`, never reset back to `false` anywhere in the file. It was meant to represent "recent mouse activity" but actually meant "mouse moved at least once this session." Both `currentAimPoint()` and the auto-aim soft-lock decision in `handleHotbarPress` read the flag and permanently deferred to `this.input.activePointer` once tripped, regardless of idle time — exactly the developer's playtest report (aim correctly disengaged on first trackpad touch, then never came back even after the trackpad was left alone).

**Not touched, per the ticket:** the debuffer-invulnerability report flagged in the same issue. Re-read the issue's own reasoning and confirmed it in `Enemy.ts`/`HealthSystem.ts` myself before leaving it alone — no invulnerability flag or archetype-specific damage gate exists anywhere in the hit-apply path; the debuffer just has the highest placeholder HP and kites at the longest preferred range of the three archetypes. Left for the developer to retest once this fix lands, not fixed here.

**Chosen recency window: `POINTER_ACTIVE_WINDOW_MS = 2000` (`src/systems/pointerActivity.ts`).** The ticket asked for a window on the same order of magnitude as this game's own input pacing, not an arbitrary guess. Two independent existing constants converge on the same neighborhood: `ManaSystem.WEIGHT_CLASS`'s player spell cooldowns (light 2000ms / standard 4000ms / heavy 8000ms — the fastest complete aim-cast-recast cycle in the game is exactly 2000ms) and `Enemy.ts`'s `ATTACK_COOLDOWN_MS` (melee 1200ms / ranged 1800ms / debuffer 2500ms). 2000ms sits at the fast end of that ~1.2s-8s band: long enough that a mouse-aiming player's hand going still for one light-spell cooldown cycle doesn't get yanked back to the no-mouse fallback mid-decision, short enough that once the player is genuinely done with the mouse, auto-aim re-engages within about one action beat rather than several — matching "even once idle" rather than trading one permanent-feeling state for a differently-long one.

**Pure logic extracted first, TDD, same seam convention as `rangedImpact.ts`/`autoAim.ts`/`waveSession.ts`/`waveThreatBudget.ts`/`enemyStatusOverlay.ts`:** wrote `src/systems/pointerActivity.test.ts` against the not-yet-created module first — moved the not-yet-written implementation out of the way, confirmed red (`vitest` failed to resolve `./pointerActivity`), then restored it and confirmed green. Implemented `hasRecentPointerActivity(lastMovedAt: number | null, now: number, windowMs = POINTER_ACTIVE_WINDOW_MS): boolean` — `lastMovedAt === null` (pointer never moved this session) is always false regardless of window; otherwise `now - lastMovedAt <= windowMs`, inclusive at the boundary, matching this repo's existing inclusive-range convention (`rangedImpact.ts`'s hit-radius check, `Enemy.ts`'s melee/kiting ranges). 7 new tests, 56/56 total, including one that directly asserts issue #49's exact bug shape: a pointer that moved once, 5 minutes ago, must not still read as active.

**Scene wiring (`SpellroadScene.ts`):** `pointerHasMoved: boolean` replaced with `lastPointerActivityAt: number | null = null`. The `pointermove` handler (past the jitter threshold) and the `pointerdown` left-click handler now record `this.time.now` instead of setting a flag. `currentAimPoint()` and `handleHotbarPress`'s soft-lock decision both call `hasRecentPointerActivity(this.lastPointerActivityAt, this.time.now)` instead of reading the old boolean directly — same two read sites the issue named, no others existed. `POINTER_JITTER_THRESHOLD_PX` (what counts as real movement vs. incidental jitter) is unchanged and untouched — this fix only changes what happens to a qualifying movement afterward (a timestamp that can age out) not what qualifies as movement in the first place. Doc comments on `previewLockedEnemy` and `lastPointerActivityAt` itself updated to match; did not touch #47 (`e8eb527`) or #48 (`223f5fd`)'s own code, confirmed by re-reading `onRangedFire`/`WaveSession` wiring after the edit — neither reads or sets the pointer-activity state.

**Why this verification would actually catch a regression of this specific idle-state class of bug (per ADR-0001), and where it can't:** the bug's exact shape was "a flag that's set but never unset, so elapsed idle time is invisible to the read site." `hasRecentPointerActivity`'s unit tests directly assert the failure mode: a pointer that moved once, long ago, must read as inactive (the 5-minutes-idle case) — a future edit that went back to a sticky boolean, or that compared to a hardcoded `now` instead of the live clock, or that inverted the comparison, fails that test. The boundary tests (exactly at the window, one ms past it) also mean a regression that silently changed the window's semantics (e.g., `<` instead of `<=`, or reading the wrong operand) is caught, not just a total-breakage case. This is different from PR #46's own gate: none of the 96 pre-existing unit tests, nor #47/#48's new ones, ever exercised pointer-recency logic at all — this is the first test that touches this code path, not a pre-existing test re-run. What this unit test does *not* and cannot prove, stated plainly: it says nothing about whether `SpellroadScene`'s actual `pointermove`/`pointerdown` listeners really call `this.time.now` (a wiring mistake — e.g., capturing `Date.now()` instead, or a listener silently unregistered) or about real elapsed wall-clock time during an actual idle period at the keyboard/trackpad. A test using a fake clock proves the decision function is correct; it cannot prove the live Scene reads a live clock at the right two call sites, or that a real human touching a real trackpad and then waiting 2+ seconds actually experiences the transition as intended. That gap is exactly what the developer playtest below exists to close, not something this test suite can substitute for.

**Self-verify:** `docker-compose run --rm game npm run typecheck`, `npm test` (56/56 — 49 pre-existing plus 7 new `pointerActivity` tests), and `npm run build` all clean. Docker was available; no host fallback needed.

**Status:** `in-progress-with-owner` — self-verified only, not `shipped-and-validated`. Per ADR-0001 and this agent's own success criterion, only a real developer playtest clears that gate: touch the trackpad (confirm aim correctly follows the mouse, same as before), then leave it alone past ~2 seconds and confirm auto-aim resumes (cone-targets the nearest enemy in the facing cone again) instead of staying pinned to the last pointer position. Worth doing in the same session per the issue's own note: retest whether the debuffer is actually damageable now that aim can re-engage onto it after it kites out of a stale lock. Branch: `loomwright/fix-playtest-bugs-47-48-49` (this entry covers #49 only; #47 landed in e8eb527, #48 landed in 223f5fd, both untouched by this change).

## 2026-08-02 (5) — Code-review addendum: a real #47/#48 interaction, flagged not fixed

`/code-review` on the combined #47+#48+#49 branch (Spec axis) caught a genuine cross-fix interaction neither ticket's own scope covered: `startPhaseBreak`'s decline path (`resolve(false)`) schedules `startWave` only 200ms later, which is *shorter* than `RANGED_TRAVEL_MS` (450ms, #47's impact-recheck window). Since `startWave` always bumps the session generation (#48), a ranged shot fired by a boss phase's last enemy just before it died can have its impact silently voided by that same-life phase advance, not just by a death — if the player declines the recovery prompt fast enough that the next phase starts before the shot lands.

Verified the mechanism is real by reading `startPhaseBreak`/`startWave`/`onRangedFire` together (not merely asserted by the reviewer): `beginAdvance()` doesn't bump generation, but `startWave`'s `beginWave()` unconditionally does, and nothing distinguishes "the world ended" (death) from "the same life moved to the next phase" (decline) in the generation scheme #48 introduced.

**Not fixed, deliberately** — this fails safe (a hit that should land doesn't; nothing crashes, corrupts wave counts, or damages the player incorrectly) and needs sub-250ms human reaction time after the phase-clear prompt appears to trigger at all. A real fix would mean giving `WaveSession` a second, narrower notion of "this specific shot's world" (e.g., only death invalidates ranged impacts, phase/wave advances don't) — a design change bigger than any of #47/#48/#49's own scope, not something to improvise mid-review. Flagged with an inline comment at the decline `delayedCall` in `SpellroadScene.ts` instead of silently shipping or silently redesigning. Re-ran `docker-compose run --rm game npm run typecheck`/`npm test` (56/56)/`npm run build` after adding the comment (comment-only change, no logic touched) — all clean.

**Status:** unchanged from the three entries above — still `in-progress-with-owner` on all three tickets, plus this one known, disclosed gap for the developer to weigh when deciding whether it's worth its own future ticket.

## 2026-08-02 (6) — Fix #52/#53/#54: phase-break freeze, walkable-lane/art mismatch, mana pre-check

Three more bugs from the same 2026-08-02 follow-up `/grill-with-docs` session that surfaced #47/#48/#49 (that attempt reached the actual end of the vertical slice this time — bought a Phase-Transition Recovery, hit "Phase clear!", then froze there). Dispatched by Ana as backlog 2.26/2.27/2.28, branch `loomwright/fix-freeze-lane-mana-52-53-54`.

**#52 (backlog 2.26), highest priority — phase-break "press any key" freeze.** `startPhaseBreak`'s `!canPay` branch showed "press any key to continue" but only ever armed `keydown-Y`/`keydown-N` listeners — no any-key handler existed anywhere, so a phase-break with no recovery available froze the run unrecoverably. Developer's decision, per the issue: not an auto-advance, a deliberate pause beat — message now reads "press Y to continue" and `keydown-N` is armed only when `canPay` is true (there's nothing to decline when it's false).

**#53 (backlog 2.27) — walkable lane (780px) narrower than the rendered road art (960px, full canvas).** `ROAD_LEFT`/`ROAD_WIDTH` widened 90/780 → 0/960 to match the art exactly (`computeTilemapOffset` already centers a 960px map at `offset.x = 0` for a 960-wide lane — no seam). `RANGED_PREFERRED_RANGE`/`DEBUFFER_PREFERRED_RANGE`/`WALL_SLIDE_MARGIN` left unchanged — these are player-relative distance bands independent of lane width, same argument already on record for the three prior `ROAD_HEIGHT` widenings (2.17/2.21/backlog history). The hardcoded enemy spawn x (820) was retuned to 910, preserving the exact pre-existing invariant (spawn sits precisely `WALL_SLIDE_MARGIN` px inside the right wall: `870-50=820` before, `960-50=910` after) rather than picking an arbitrary new value.

**#54 (backlog 2.28) — hotbar let you enter aim/preview mode with insufficient mana, only rejected at confirm.** `handleHotbarPress` already pre-checked cooldown but not mana. Added `SpellCaster.canAffordCast(spell)`, backed by a newly-extracted pure `src/systems/spellCost.ts` (`computeCastManaCost`) that both `canAffordCast` and `tryCast` now share, so the pre-check and the actual spend can never silently drift onto two different formulas. `handleHotbarPress` now flashes "Not enough Mana" and returns before entering preview, mirroring the existing on-cooldown flash.

**Heckler's critique (`heckler/log.md`, 2026-08-02 (6)) found the #52 fix reintroduced an equivalent freeze through a gap it created itself, and it was right.** `resolve()`'s guard checked only `session.phase`, never its own closed-over `phaseGeneration` token; `handleDeath()` bumped generation but never deregistered a still-armed phase-break's listeners. Trace: death interrupts an unresolved phase-break → boss retry reaches another phase-break → a second `keydown-Y`/`keydown-N` pair gets armed alongside the still-live stale pair → a single keypress fires both closures → the stale one's phase-only guard passes (the session genuinely is `awaiting-phase-choice` again, just for a different attempt) → it silently runs `beginAdvance()` plus live Hexcoin/HP side effects → the fresh, legitimate closure's identical guard now reads `phase !== "awaiting-phase-choice"` as true (already flipped) and bails → the real advance never gets scheduled → permanent freeze, the same failure #52 was dispatched to fix, reached via a different path. Concrete and reachable: this boss is 3 phases/2 phase-breaks, every phase has a ranged attacker, `bossMaxRecoveries` caps at 1, and dying mid-recovery-prompt then retrying is an ordinary playtest flow, not a contrived one.

**Fixed with defense-in-depth, both halves of Heckler's suggested direction, not just one.** Added a pure `canResolvePhaseChoice(phase, currentGeneration, token)` to `waveSession.ts` (same seam convention as `isCurrent`/`shouldAutoAdvance`) — "resolve is only valid once, for the current phase-break attempt": phase must still be `awaiting-phase-choice` AND the token must still equal the live generation. `resolve()` now calls this before doing anything, including side effects. Separately, hoisted the armed `onY`/`onN` closures onto a new `phaseChoiceListeners` field so `handleDeath()` can `.off()` them by reference the instant it interrupts an unresolved phase-break — so a later, unrelated phase-break never inherits a stale listener at all, rather than relying solely on the token guard to no-op it after the fact. Either mechanism alone closes the race; both are in place.

**Pure logic, TDD, same convention as every prior fix in this log:** `waveSession.test.ts` gained 4 new cases for `canResolvePhaseChoice`, including one that replays Heckler's exact repro (stale token from an interrupted phase-break correctly rejected; a later phase-break's live token correctly accepted). `spellCost.test.ts` (new file) covers `computeCastManaCost` — base cost at Novice/Adept, the -10% Master discount applied only when `master_discount === "cost"` (including a rounding case), and cost left untouched when the spell's discount applies to cooldown instead.

**Self-verify:** `docker-compose run --rm game npm run typecheck`, `npm test` (63/63 — 56 pre-existing plus 3 `spellCost` cases plus 4 `canResolvePhaseChoice` cases), and `npm run build` all clean, both before and after Heckler's fix. Docker available throughout; no host fallback needed.

**Why this verification would actually catch a regression of each bug's specific class (per ADR-0001), and where it can't:** #52's real risk was always "the message promises a key that isn't armed, or an armed listener nobody was told about" plus, after Heckler's catch, "a stale listener from an earlier attempt co-firing with a fresh one." `canResolvePhaseChoice`'s unit tests mechanically pin the token-vs-generation invariant across a full multi-phase-break sequence including the literal death-then-retry-into-a-new-phase-break case — a future edit that reverts to a phase-only guard, or that stops `handleDeath` from bumping generation, fails a test that reads like the bug report. What no test here proves: the real Phaser keyboard-listener lifecycle at runtime (`.once` registration, a real `keydown` event, `.off()` against Phaser's actual event emitter) — there's no harness for that in this repo, so whether a single real keypress now only ever invokes one `resolve()` closure still needs a developer playtest, not just unit coverage of the decision logic. #53's risk was a lane narrower than its art; `ROAD_WIDTH === CANVAS_WIDTH` (960 === 960) is now a direct equality a future narrowing would visibly break, and `levelArt.test.ts`'s existing tests already assert the art itself spans the full canvas at that width — but whether the *widened* lane feels spacious rather than too open, and whether the retuned spawn point ever visibly clips the wall in real play, is a feel question only the playtest gate answers. #54's risk was cost/afford-check drift; both call sites now share one pure formula, and mana can only increase (never decrease) between the hotbar-press pre-check and the eventual spend (nothing else touches it except the very `tryCast` call the pre-check guards), so a "sufficient at press, insufficient at confirm" race is structurally impossible, not just untested — but whether `this.mana.canAfford` reflects the live value at the moment of a real keypress is, again, a live-Scene wiring question the unit tests don't reach.

**Flagged, not fixed (out of scope for #52/#53/#54):** the already-disclosed #47/#48 phase-decline race (2026-08-02 (5) above) sits in the same `startPhaseBreak` function this bundle touches but is untouched by this diff, correctly left flagged rather than re-raised as new. `WaveLoader.spawnWave`'s unknown-enemy-type `continue`-without-decrementing gap (flagged since #48) remains untouched. The `ROAD_HEIGHT` (280) vs. Tilesmith's map heights (288/320) approximation is unrelated to this ticket's left/right-edge symptom and stays as-is.

**Status:** `in-progress-with-owner` on all three (2.26/2.27/2.28) — self-verified and Heckler-cleared (after the one round-trip above), not yet developer-playtest-confirmed. Gate per this agent's own success criterion: a real developer session that (a) reaches a no-recovery-available phase-break and confirms Y advances cleanly with no freeze, ideally including a die-mid-prompt-then-retry-into-another-phase-break pass specifically probing Heckler's catch; (b) walks to both the left and right edges of the road and confirms the walkable area now visually matches the rendered art; (c) confirms a hotbar press with insufficient mana is rejected immediately, before aiming, rather than at confirm. Branch: `loomwright/fix-freeze-lane-mana-52-53-54`, off `main`, not yet pushed as of this entry.

## 2026-08-02 (7) — Fix #55: hotbar redesigned as a single-row action bar (feat. a Heckler round-trip)

Dispatched by Ana as backlog 2.29, same 2026-08-02 follow-up session that surfaced #52-#58. Branch `loomwright-tilesmith/hotbar-icons-55-56`, stacked on the not-yet-merged `loomwright/fix-freeze-lane-mana-52-53-54`.

**The bug:** the old vertical `hotbarText` (a "Hotbar:" header plus one line per equipped spell, 14px font/4px spacing, starting at y=424) rendered 7 lines into ~116px of remaining canvas height (540px canvas, road ending at y=410) — not enough room, so spell 6 (`magma_lance` in the default loadout) got clipped or crowded off-canvas. Developer's decision, via `/grill-with-docs`: redesign as a compact horizontal single-row action bar (6 numbered slots) rather than just shrinking the font, since a row layout scales better and was explicitly meant to pair with a follow-on icon task.

**What shipped:** the vertical text block replaced with 6 fixed-position slots, each a `Graphics` border (color communicates state: gold = armed/previewing, green = ready, muted grey-blue = on cooldown) plus a `Text` label showing `[N] spellId [shape/weight]` and cooldown remaining. New pure module `src/systems/hotbarLayout.ts`: `computeHotbarSlotRects` (canvas width + slot count → evenly-spaced, non-overlapping rects — a slot cannot extend past canvas width by construction) and `computeCooldownDisplay` (remaining/total cooldown ms → ready flag, 0-1 fraction, display label), same seam convention as `enemyStatusOverlay.ts`. Also extracted `computeCastCooldownMs` into `spellCost.ts` (mirroring #54's `computeCastManaCost`) and added `SpellCaster.cooldownDurationMs` so the scene doesn't need to reach past `SpellCaster` to get a spell's full cooldown duration. `handleHotbarPress`/`confirmCast`/`cancelPreview` (including the #52/#53/#54 fixes already in this branch's tree) untouched. Each slot is a distinct rect specifically so a future icon has an unambiguous anchor — which #56/Tilesmith then used, on top of this, same session.

**Heckler's critique (`heckler/log.md`, 2026-08-02 (7)) of the combined #55+#56 bundle found one real BLOCKING issue, introduced by the interaction between the two, not either alone.** When Tilesmith's icon `Image` GameObject took up space inside each slot, it shrank the label's available text width (~144px → ~88px) without re-checking the longest label against the new budget — `thunder_dome`'s `[circle/standard]` tag (17 characters, confirmed the actual worst case out of all 9 shape/weight combinations, not just assumed) bled past its slot border into the neighbor, in the *default* loadout, every load — reproducing the exact "hotbar text hard to read" symptom #55 was dispatched to fix, just via a new mechanism.

**Fixed with real margin, not a hairline re-fit.** Added `formatShapeWeightTag(shape, weight)` (`hotbarLayout.ts`) abbreviating both shape (`line/cone/circle` → `lin/con/cir`) and weight (`light/standard/heavy` → `lgt/std/hvy`) to a fixed 9-character tag for all 9 combinations, replacing the prior 12-17-character unabbreviated spread that had an unchecked worst case. Also dropped the label font 11px → 10px, since `glacial_shard` (13 characters, the longest shipped spell id) was independently left with near-zero margin by the same icon-driven shrink even though it isn't in the current default loadout — fixed both rather than only the one Heckler happened to catch live. 5 new `hotbarLayout.test.ts` cases enumerate all 9 shape/weight combinations, assert the fixed 9-char length, cover the specific overflowing case, and confirm no two combinations collide after abbreviation.

**Self-verify:** `docker-compose run --rm game npm run typecheck`, `npm test` (81/81 — 76 prior [63 + 10 hotbarLayout + 3 cooldown-cost] plus 5 new tag-format cases), `npm run build` all clean, both before and after the Heckler-driven fix. Screenshot-confirmed (zoomed into the live default loadout's slot 5) the worst-case label now sits with roughly 39px of clear margin inside an ~89px text budget (~44% unused), vs. the prior zero-margin overflow.

**Why this verification would actually catch a regression of this specific class (per ADR-0001), and where it can't:** the failure mode was "a label's rendered width exceeds its slot's available text budget." `formatShapeWeightTag`'s tests pin every one of the 9 possible tag strings to an identical, known length — a future edit that lengthens any abbreviation, or adds a 4th shape/weight class without updating the abbreviation map, fails a test that directly checks the produced string, not just a downstream symptom. What this can't prove: real font-rendering metrics (whether 10px Phaser/Canvas text at this exact character count truly fits the measured pixel budget, vs. the character-count proxy the tests use) — that's why the fix was also confirmed via an actual zoomed screenshot of the live default loadout, not unit tests alone; a *different* font, a browser/OS with different font substitution, or a future longer spell id could still need a real look, which stays a developer-playtest-adjacent check, not something automatable here without a Phaser canvas-metrics harness this repo doesn't have.

**Status:** `in-progress-with-owner` — self-verified and Heckler-cleared (after the one round-trip above, same shape as 2.26-2.28's own round-trip). Gate: developer confirms the new single-row hotbar is legible and readable at native resolution, and that cooldown/armed/ready states are visually distinguishable in real play (not just in a static screenshot) — this agent's own success criterion, unchanged from every prior entry in this log. Branch: `loomwright-tilesmith/hotbar-icons-55-56`, stacked on `loomwright/fix-freeze-lane-mana-52-53-54`, not yet pushed as of this entry. See `docs/agents/tilesmith/log.md` (2026-08-02) for #56's own icon-sourcing entry, on the same branch.

## 2026-08-02 (8) — Fix #58 (level/wave HUD) and #57's HUD half (debuffer feedback), feat. Lorena's naming wire-in and a Heckler round-trip

Dispatched by Ana, same 2026-08-02 follow-up session. Branch `loomwright-lorena/hud-clarity-57-58`, stacked on `loomwright/fix-freeze-lane-mana-52-53-54` (sibling to, not descended from, `loomwright-tilesmith/hotbar-icons-55-56` — this branch does not include that branch's hotbar redesign; the hotbar here is still the old vertical text block, which is expected). Numbered (8) rather than (7) to avoid colliding with that sibling branch's own (7) entry once both merge — same reasoning as the Heckler-log renumbering below.

**#58 — Level/Wave indicator.** `updateHud`'s `Level X, Wave Y (enemies: N)` was buried as one line inside the small 4-line, 14px stat block, with no other persistent signal (only a 1500ms transient banner at level start). Developer's decision: a persistent, larger, higher-contrast element in a fixed spot. Added `levelWaveText`, top-right, 28px bold, gold on an opaque panel, updated every `updateHud()` call and never cleared by a timer. The old `waveLine` was removed from the small stat block, which now shows only the live enemy count (changes more often, belongs there more than level/wave does).

**#57, HUD half — debuffer feedback.** Debuffer is 0-damage by design (`DebuffSystem.ts`'s speed/mana-regen drain); it already had a visual pulse (`spawnDebuffPulse`) but no HUD indication of actual magnitude. Read `DebuffSystem.ts` directly before implementing: **there is no duration/expiry field anywhere in the system** — a stack persists until `clear()` fires (wave-start or player death only, confirmed by reading every call site). Rather than inventing a countdown number that doesn't exist, added a new pure `src/systems/debuffDisplay.ts` (`computeDebuffMagnitude` — mirrors `DebuffSystem.effectiveManaRegen`'s exact formula so the two can't drift; `formatDebuffHudLines` — returns `[]` when inactive, so the HUD shows nothing until a pulse actually lands) and wired it into a new persistent `debuffText` element below the Level/Wave box. Left the source-name plumbing at `archetypeDisplayName("debuffer")` (`enemyStatusOverlay.ts`) as the deliberate integration point for Lorena's naming work, rather than guessing or hardcoding a name.

**Lorena's naming wire-in, same session:** `archetypeDisplayName("debuffer")` now returns "The Tarrywright" (display-cased from `lore-premise.md`'s established lowercase "the Tarrywright," to read correctly both as a standalone overlay label and mid-sentence in the HUD line) — one function, both call sites (enemy overlay label, debuff HUD line) pick it up automatically. See `docs/agents/lorena/log.md` (2026-08-02) for her own entry.

**Heckler's critique (`heckler/log.md`, 2026-08-02 (8), renumbered from (7) for the same cross-branch reason noted above) found 2 real MAJOR issues, no BLOCKING:**

1. **HUD depth-layering collision.** None of the HUD text objects called `setDepth`, and `Enemy`'s name label/HP bar (added to the display list after `createHud()` runs) could render on top of the new Level/Wave and debuff HUD boxes whenever an enemy was near that screen region — reachable in ordinary play since enemies and the mage share the same lane-clamp rectangle, flush to the canvas edges. This directly undercut #58's entire point (a HUD element you're never supposed to miss). Fixed: checked the actual existing depth values first (`BACKGROUND_DEPTH=-100`, `TILE_LAYER_DEPTH=-50`, the ranged-projectile's `1000`, deliberately scoped narrower per its own comment) rather than guessing, then added a new `UI_DEPTH = 2000` and applied `.setDepth(UI_DEPTH)` to every persistent HUD text object in `createHud` — title, `hudText`, `hotbarText`, `messageText`, `levelWaveText`, `debuffText`. Phaser sorts by depth before display-list order, so this closes the gap structurally, not just for today's construction order.
2. **"(until wave clears)" was misleading about what actually clears a debuff.** Killing the visible Tarrywright does not clear its debuff stack (`removeEnemy` never touches `this.debuff` — verified, no per-source tracking exists at all). A player who kills it and still sees "The Tarrywright pulse active (until wave clears)" with nothing on screen has every reason to read that as a stale/buggy leftover — a milder recurrence of the exact "reads as broken" complaint #57 was filed to fix. Fixed: reworded to `"${sourceName} pulse active (outlives this enemy — until wave clears)"`, naming the exact thing a player will wonder rather than leaving it implicit. Added a `debuffDisplay.test.ts` case pinning the new wording so a future edit can't silently drift back to the misleading version.

**Self-verify:** `docker-compose run --rm game npm run typecheck`, `npm test` (76/76 — 74 prior [63 + 11 `debuffDisplay`] plus 1 Lorena test-relabel plus 1 new wording-pin case), `npm run build` all clean, both before and after the Heckler-driven fixes.

**Why this verification would actually catch a regression of each class (per ADR-0001), and where it can't:** #58 is a static per-frame render of already-computed state, no new timers/async — typecheck/build cover its actual risk surface, and it was additionally confirmed via a live screenshot. #57's magnitude arithmetic is pure and directly unit-tested against `DebuffSystem`'s real constants, so a future formula drift or wrong active/inactive gating fails a test, not just a symptom; the "outlives this enemy" wording is now pinned by its own test too. The depth fix (finding 1) is a `setDepth` call, which Phaser's renderer sorts on unconditionally ahead of any insertion-order tiebreak — a structural fix confirmed by reading Phaser's own depth-sort semantics and the actual existing depth constants, not a guessed number; it was **not** confirmed via a live enemy-over-HUD screenshot, because this session's browser-pane input throttling (already disclosed in this same log's prior entries — synthetic key holds barely move the mage) made positioning a live enemy into the HUD's corner impractical, and no global scene reference exists on `window` to force it via injected JS either. What no test here proves, for either #57 or #58: real Phaser Text rendering at native resolution and font legibility, and specifically for #57's HUD line, a live-triggered Debuffer pulse actually reaching the screen during real play — both Loomwright and Heckler independently hit the same browser-pane limitation trying to trigger this live, so this specific piece is confirmed by code review and unit tests only, not empirical observation, and needs a developer playtest before `shipped-and-validated`.

**Status:** `in-progress-with-owner` on 2.31/2.32 — self-verified and Heckler-cleared (after the one round-trip above, same shape as every prior bundle in this cycle). Gate: developer playtest — glance-test the new Level/Wave box mid-combat, confirm it's never obscured by an enemy near the top-right corner, and get a real Debuffer pulse to land (the one piece this session's tooling couldn't reach) to confirm the HUD line's wording, magnitude numbers, and its correct persistence past the source's death. Branch: `loomwright-lorena/hud-clarity-57-58`, stacked on `loomwright/fix-freeze-lane-mana-52-53-54`, not yet pushed as of this entry.

## 2026-08-03 — Issue #71: architecture-seam decisions implemented (schema gate, soft-lock fix, wired-up modifiers, data-driven default loadout)

Implemented directly against the Wayfinder decision on [issue #71](https://github.com/ReynaldoMR3/the_last_spellroad/issues/71) (grilling session, resolved same day), following the go-forward plan's architecture audit ([issue #65](https://github.com/ReynaldoMR3/the_last_spellroad/issues/65)). Branch `worktree-architecture-seam-71`, off `main`. Four changes, all deepening seams the ticket decided to deepen now; the rest of the audit's candidates (level/wave manifest catalog, enemy-identity-as-data, the lore seam) were decided **deferred**, not built.

**1. Content schema/build validation gate (Pato's proposal from the ticket).** New `src/data/validateContent.ts` — hand-rolled, Phaser-free structural validation of `SpellDefinition`/`WaveDefinition` against the closed vocabularies TypeScript already declares (`Element`/`AoEShape`/`Weight`/`master_discount`), no new dependency. Closes the audit's finding #10: the scene previously cast Phaser cache JSON straight to these types (`this.cache.json.get("spells") as SpellDefinition[]`) with zero runtime check, and the agent-crew had already hit a real schema-drift bug this would have caught. Also implements the cross-reference check Warden/Frieren's and Pato's ticket comments asked for: every wave's enemy `type` string is checked against `ENEMY_REGISTRY`, flagging exactly the audit's finding #3 (an unregistered name that would otherwise be silently skipped at spawn) before promotion instead of after a live playtest finds it. `validateContent.test.ts` runs this against the actual shipped `spells.json`/`waves/*.json` (via native JSON imports, `resolveJsonModule` already enabled) — this is the real build gate, wired into the existing `npm test` script `npm run build`/CI already depend on — plus unit coverage of the validator itself with synthetic bad data.

**2. Enemy-registry soft-lock fix (defense in depth, independent of the gate).** Audit finding #3: `WaveLoader.spawnWave` skips any `type` absent from `ENEMY_REGISTRY`, but `SpellroadScene`'s `enemiesRemainingToSpawn` was initialized by summing every authored entry's `count`, including skipped ones — since a skip never calls `onSpawn`, the counter could never reach zero and the wave soft-locked permanently, even after every spawnable enemy died. Fixed at the source rather than with a second decrement path to keep in sync: new pure `src/systems/waveEnemyCounts.ts` (`countSpawnableEnemies`) sums only registry-recognized entries; `startWave` now initializes the counter from that instead of the naive reduce. An unregistered name is now a visibly-missing enemy (fewer than the file lists), not a freeze.

**3. `hp_modifier`/`damage_modifier` wired up.** Previously dead `WaveDefinition` fields — every shipped wave authors them as `1.0`, nothing read either (audit finding #5). `Enemy`'s constructor now takes `hpModifier`/`damageModifier` (both default `1`, so every untouched call site is unaffected), applies `hpModifier` to `maxHp` at spawn (`Math.round`), and exposes `damageModifier` (public readonly) for the scene's own flat `ARCHETYPE_DAMAGE` lookups at hit time (`onMeleeHit`/`onRangedFire` now multiply by `enemy.damageModifier`, rounded). `WaveLoader.spawnWave` passes `wave.hp_modifier`/`wave.damage_modifier` through to `new Enemy(...)`. No shipped wave's actual numbers change (all still author `1.0`) — this closes the documented-vs-actual gap so Warden/Pato can author a "tougher repeat" of an existing archetype for a later wave without an engine change, the next time one is wanted.

**4. Default spell loadout made data-driven.** Audit finding #1: the hardcoded `DEFAULT_LOADOUT_IDS` six-ID array left 6 of the 12 shipped spells permanently unreachable through the UI, which the ticket's grilling session judged as contradicting the already-locked "swap loadout freely between expeditions" design rather than a deliberate cap. Added `default_loadout_slot?: number` to `SpellDefinition`; the same 6 spells `DEFAULT_LOADOUT_IDS` named now carry slots 1-6 in `spells.json`, in the same order (behavior-preserving — this moves *ownership*, not the actual default loadout). New pure `src/systems/defaultLoadout.ts` (`selectDefaultLoadout`) filters/sorts by that field; `SpellroadScene.create` calls it instead of mapping a hardcoded ID array. Frieren can now re-curate the default loadout by editing `spells.json` alone. Full player-facing loadout *selection/swapping* between expeditions is still separate, explicitly-future work (per the existing `HOTBAR_KEYS` comment) — this only moved which six spells start equipped into data.

**Self-verify:** `docker-compose run --rm game npm run typecheck`, `npm test` (115/115 — 94 prior + 13 `validateContent` + 4 `waveEnemyCounts` + 4 `defaultLoadout`), `npm run build`, all clean.

**Why this verification would actually catch a regression of each class (per ADR-0001), and where it can't:** the schema gate is directly tested against the real shipped files, so any future spell/wave file edit that drifts from the schema or references an unregistered enemy fails `npm test` immediately — this is the actual mechanism, not a proxy. `countSpawnableEnemies` and `selectDefaultLoadout` are pure functions with direct unit coverage of their exact contracts (registry-aware counting; slot-based selection and ordering), so a future edit to either can't silently regress without failing a test that checks the real behavior, not a downstream symptom. The `hp_modifier`/`damage_modifier` wiring is arithmetic on existing, already-tested archetype constants (`PLACEHOLDER_ENEMY_HP`, `ARCHETYPE_DAMAGE`) — covered by typecheck/build since no shipped wave actually exercises a non-`1.0` value yet; this needs a real developer playtest once a wave actually authors a non-1.0 modifier, the same gap this repo's docker-testing-contract already names for any Loomwright change. None of this was checked via a live dev-server session — no visual/UI change was made (the hotbar's rendering logic is untouched; only *which* six spells populate it changed, in a behavior-preserving way per the tests above).

**Status:** `in-progress-with-owner` — self-verified only, not yet developer-playtest-confirmed. Gate: a real playtest confirming the default loadout still shows the same 6 spells in the same slots as before (regression check, not new functionality), and — whenever a future wave actually authors a non-1.0 `hp_modifier`/`damage_modifier` — that the enemy's HP bar and hit damage actually reflect it live. Branch: `worktree-architecture-seam-71`, off `main`, not yet pushed as of this entry.

## 2026-08-03 — Backlog 2.33-2.37 (issues #76-#80), 5.8 (Boot/Title/Pause scene chain)

Dispatched by Ana to clear the six remaining `not-started`/buildable rows the 2026-08-03 full-playtest-of-#30 session added (2.33-2.37) plus 5.8 (Boot/Title/Pause), the one `not-started` row that had been sitting fully specced — an approved design doc (`docs/superpowers/specs/2026-08-01-boot-title-pause-screens-design.md`) and its own backlog item 0.7 — since 2026-08-01 with no engine work against it yet. **2.38 explicitly not touched**: it reopens a locked number (Debuffer's 0-direct-damage rule) and the backlog's own row says to raise it with Pato first, not build it — left `blocked-with-reason` as-is. Branch: `loomwright/playtest-followups-76-80-and-scene-flow-58`, off `ana/playtest-08-03-feedback`.

**2.37 (#80) — "Not enough Mana" legibility.** `flashMessage` (`SpellroadScene.ts`) gained an `emphasis` param (`"default" | "warning"`); the two `canAffordCast`/`tryCast` rejection call sites now pass `"warning"`, which swaps the banner to a distinct color (`#ffb4a8`) plus an opaque background panel (`#4a1f1f`) instead of the same plain gold text every other transient message uses. Every other `flashMessage` call site is unaffected (still fully transparent, same look as before) — this is additive, not a redesign of the shared banner. The per-frame message-clear block in `update()` also resets color/background on expiry, so an expired warning can never bleed its styling into whatever plain message shows next.

**2.33 (#76) — floating HP/Mana bars above the player.** Reuses `enemyStatusOverlay.ts`'s `computeHpFraction`/`computeHpBarColor` (already generic despite the module's enemy-facing name — no new pure module needed) rather than inventing a second fraction/color scheme. A new `playerStatusBar` Graphics object, redrawn every frame in `updatePlayerStatusBars()` (same seam convention as `Enemy.ts`'s own `refreshStatusOverlay`): HP on top (banded, matches the existing floating-damage-number colors), Mana below it (single fixed blue tone — no banding, since a low Mana pool isn't a danger signal the way HP is). Given `UI_DEPTH` explicitly (not left at the default depth `Enemy.ts`'s own per-enemy overlay uses), so a melee enemy standing on the player during a pile-up can't paint over the player's own status readout — same reasoning Heckler's 2026-08-02 critique already applied to every other persistent HUD element.

**2.34 (#77) — early-game Mana regen tuning.** `MANA_REGEN_PER_SEC` (`ManaSystem.ts`) 5 -> 8/sec. Derivation (documented inline and in `mana-template.md`): at 5/sec every weight class's own cost regenerated in almost exactly its own cooldown (break-even, not a surplus) — fine for sustained single-spell spam, but chained casts of *different* spells during the opening minutes had no regen surplus to draw on, reading as sluggish exactly where the design wants an inviting first-minutes rush. At 8/sec every weight class now regens its own cost with a real surplus inside its own cooldown window (Light 1.25s vs. 2s, Standard 2.5s vs. 4s, Heavy 4.375s vs. 8s). The weight-class cost/cooldown table itself is untouched, per the ticket's own scope.

**Flagged, not silently resolved:** this retune has a real, undisclosed-if-left-quiet interaction with the Debuffer's Mana-regen-drain mechanic (`DebuffSystem.ts`) — at the old 5/sec base, the 2-stack cap (3.0/sec drain) landed *exactly* on the stated 2/sec floor (5-3=2, a deliberate design coincidence per the GDD's own wording); at 8/sec the same drain now lands at 5/sec, well above the floor, so the Debuffer's regen-drain variant is proportionally weaker (a 37.5% cut vs. the old 60%) and the floor no longer binds at max stacks. Documented in the GDD's "Mana And Spell Costs" section and added to this backlog as **new item 2.39** (Phase 2 table) — needs Pato's re-check (retune drain/floor to restore the intended proportional cut, or explicitly accept the weaker debuff), not decided here since it's outside 2.34's own ticket scope and Pato's exclusive numeric territory.

**2.36 (#79) — spell cast/impact visual effects.** Two new one-shot effects, both tinted by the spell's element (`ELEMENT_EFFECT_COLOR`, the same fire/ice/lightning/earth axis `spellIcons.ts` already establishes): `spawnCastEffect` — a fast-fading flash of the actual cast shape (line/cone/circle), reusing `updatePreview`'s own mage-to-target geometry math on a fresh `Graphics` object, fired once per confirmed cast regardless of whether it lands a hit (a whiff should still visibly confirm the spell fired); `spawnImpactBurst` — a small expanding+fading ring at each individual hit, layered alongside the existing floating damage number (backlog 2.9) rather than replacing it. Scoped to the ticket's own floor (lightweight tween-based effects), not a particle/VFX subsystem.

**2.35 (#78) — hotbar targeting onboarding hint.** A one-time, dismissible overlay ("Press 1-6 to aim a spell. Press it again (or click) to fire — Esc or right-click cancels.") shown at scene start, centered in the upper lane clear of every always-on HUD element. Dismissed on the first hotbar press at all (armed or not — the natural "the player is engaging with the hotbar" signal) or a 9s fallback timer, whichever comes first. Kept standalone in `SpellroadScene` rather than folded into 5.8's Title scene, per the ticket's own note that 5.8's scope is scene flow, not in-gameplay combat instructions — Title has no natural slot for hotbar-specific teaching copy.

**5.8 — Boot/Title/Pause scene chain**, per the approved design spec and backlog item 0.7's ownership resolution. Four new/changed files:
- `src/scenes/BootScene.ts` — the first link in the Boot/Preload -> Title chain. Nothing genuinely asynchronous to preload today (Title renders entirely from generated Graphics/Text, no external assets), but kept as its own scene per the design doc's own reasoning: a future title-screen asset load has a natural home here instead of requiring a scene to be carved out of `TitleScene` later.
- `src/scenes/TitleScene.ts` — `New Game` only when no save exists; `Continue`/`New Game` (with a Y/N overwrite confirm, reusing the same confirm-prompt shape `SpellroadScene.startPhaseBreak` already establishes) when one does, via a new `SaveSystem.hasSave()` helper (a raw key check, distinct from `loadSave()`'s always-succeeds-with-a-default behavior). Keyboard (Up/Down/Enter) and mouse (hover/click) both supported, matching the existing WASD/arrow-and-mouse dual-input convention.
- `src/scenes/PauseScene.ts` — the hard-pause menu, launched (not started) on top of a `scene.pause()`'d `SpellroadScene`. `Resume`/`Quit to Title` only, per the design doc (`Restart` deliberately excluded — death already owns the one voluntary-progress-reset path). `Quit to Title` prompts its own Y/N confirm. Its own `keydown-ESC` binding handles Resume — necessary, not redundant, because a paused scene's own input listeners stop receiving events (Phaser ties the Input Plugin's active state to the scene's own active/paused state), so `SpellroadScene` cannot un-pause itself once frozen.
- `SpellroadScene.ts`'s `keydown-ESC` handler is now contextual, per the design doc's decision 4: an active spell preview cancels first (unchanged), otherwise `scene.pause()` + `scene.launch("PauseScene", ...)`.
- `main.ts` registers `[BootScene, TitleScene, SpellroadScene, PauseScene]`.

**Known, disclosed gap:** `TitleScene.continueGame()` calls `loadSave()` but `SpellroadScene` doesn't consume that blob's contents yet, and nothing in the engine writes a save during play either — that's backlog item **1.6** ("full SaveSystem cross-session wiring"), already explicitly separate, larger, not-yet-built work, unchanged by this session. Until 1.6 lands, `Continue` and `New Game` behave identically in real play; this scene is wired correctly against the `SaveSystem` contract as it exists today, and the gap is stated in both `TitleScene.ts`'s own doc comment and here rather than silently assumed away.

**Self-verify:** `docker-compose run --rm game npm run typecheck`, `npm test` (230/230, no new test files — this session's changes are either Phaser-side wiring with no meaningful Vitest seam, matching the precedent `enemyStatusOverlay.ts`/`Enemy.ts` already set, or a single-constant retune), `npm run build`, all clean throughout, re-run after every change.

**Live dev-server verification, not just typecheck/build:** brought the Docker dev server up and drove it via a real browser tab. Hit the same environment limitation this repo's own Phase 1 log entry already discloses (`document.visibilityState` reports `"hidden"` in this sandboxed browser pane, freezing `requestAnimationFrame` so Phaser's own game loop never advances on its own) — confirmed directly this session (`game.loop.frame` stuck at 4 across multiple real seconds of wall-clock time). Rather than accept that as a hard stop, manually pumped `game.loop.step()` to advance frames on demand, which let every new interactive path actually be exercised and inspected against live game state (not just read as source):
- Title -> New Game -> SpellroadScene transition (screenshot-confirmed: Title renders, click transitions, gameplay scene renders with HUD/hotbar/enemy all intact, no regression).
- The onboarding hint (2.35) renders on scene start and correctly disappears the moment a hotbar key is pressed (screenshot-confirmed, before/after).
- The player HP/Mana bars (2.33) render above the mage from the first frame and stay correctly positioned as the mage moves (screenshot-confirmed).
- A full cast cycle (arm `arc_lance`, confirm-fire) correctly clears `previewSpellId`, spends Mana, starts the cooldown, and renders the element-tinted cast-effect flash (2.36) (confirmed via both screenshot and direct state inspection: `previewSpellId`/`mana.current` before and after).
- The "Not enough Mana" warning styling (2.37) — confirmed via direct property inspection (`messageText.text`/`.style.color`/`.style.backgroundColor` all correct) after a controlled-timestep pump; the frozen-rAF environment's real-timestamp jumps between tool round-trips made the 900ms transient banner hard to catch on camera specifically (it can compute its own expiry as already-past the instant a big wall-clock gap is fed to `game.loop.step`), not a defect in the styling logic itself.
- Esc opening `PauseScene` over a visibly-frozen `SpellroadScene` (screenshot-confirmed: "Paused" panel, `Resume`/`Quit to Title`, gameplay beneath still showing its last-frozen frame), and Resume correctly returning control to `SpellroadScene` (`getScenes(true)` before/after).
- Title's save-present branch (`Continue`/`New Game`) via a seeded `localStorage` blob, and the New-Game-overwrite Y/N confirm (`promptNewGameOverwrite` -> `[N]` cancels correctly, clearing `confirmActive` and destroying the confirm text) — confirmed via direct state inspection.

The manual frame-pump is a debugging technique for this session only (a temporary `window.__game` exposure in `main.ts`, added and removed twice around these checks, not left in the committed diff) — it does not substitute for a real developer session with an actually-visible, focused tab, which is what the interactive-playtest gate below still requires.

**Status:** all six rows `in-progress-with-owner` — self-verified via typecheck/test/build and, beyond this cycle's prior entries, actually exercised live (not just read) via the frame-pump technique above. Still needs a real developer playtest (a genuinely focused, visible browser tab) before any of these clear to `shipped-and-validated`, per this repo's own standing gap between "Loomwright self-verifies" and "the developer plays it" (`loomwright/AGENT.md`'s success criterion). 2.39 (new) is `not-started`, waiting on Pato.

## 2026-08-04 — Fix: road art goes fully black after Quit to Title -> New Game

Developer's first real playtest of PR #83 (merged into `ana/playtest-08-03-feedback`) found a real bug the sandboxed browser pane's frame-pump verification couldn't have caught: HP/Mana bars, the onboarding hint, cast effects, and the Mana-warning styling all confirmed working, and the 8/sec regen retune "feels right" (no change needed) — but "after quit to title and new game, the screen turns fully black, you can still move and hit, but the spellroad is all black, the only visible items are the mage, the monsters, the spells and the text."

**Root cause.** Before backlog 5.8 added a real Quit-to-Title/New-Game restart path, `SpellroadScene.create()` only ever ran once per page load — Phaser reuses the same Scene *instance* across `scene.start()` calls, it does not recreate the class from scratch, so any mutable field not explicitly reassigned inside `create()` silently carries its value from the previous life into the next one. `renderedLevel` (the guard `renderLevelArt` uses to avoid rebuilding the same level's Tiled layer on every `startWave()` call, including phase-breaks within one boss fight) is exactly such a field: it starts at `0` only via the class-field declaration, evaluated once at object construction, never reset in `create()`. A fresh restart's first `startWave(0)` calls `renderLevelArt(1)`; if the player's previous life ended on Level 1 (the common case — dying or quitting early, exactly what a developer smoke-testing the new Pause/Quit flow would do), `renderedLevel` is still `1` from that life, so the guard's `if (this.renderedLevel === level) return;` fires immediately and skips building a tile layer at all for the new scene instance — leaving only `createRoad()`'s near-black placeholder rect (`0x11131a`) visible, exactly matching the report (everything drawn by `create()`'s other methods — mage, HUD, enemies, spell effects — renders fine, since those genuinely do get freshly created every `create()` call; only the tile art, gated by this one stale field, silently no-ops).

**Same class of bug, one more confirmed instance with a real (if not yet visible) gameplay consequence:** `highestLevelReached` has the identical staleness bug — `HexcoinSystem.markLevelStart()` only fires the first time a level number is crossed (`if (wave.level > this.highestLevelReached)`), so a stale non-zero value surviving from a deeper previous run would silently skip re-arming the Hexcoin income floor on whatever level the new run actually starts at. No visible symptom reported yet (this needs a death/checkpoint scenario after a restart to surface), but it shares the exact same root cause and needed the same fix.

**Fix (`SpellroadScene.create()`):** explicit resets added for every mutable field this class of bug can reach — `renderedLevel`, `currentLevelTilemap`, `currentLevelLayer`, `enemies`, `enemiesRemainingToSpawn`, `waveIndex`, `highestLevelReached`, `bossMaxRecoveries`, `previewSpellId`, `previewLockedEnemy`, `phaseChoiceListeners`, `lastFacing`, `lastPointerActivityAt`, `messageClearAt` — not just the one field that happened to produce a visible symptom. This mirrors a precedent already in the same method: `this.session = new WaveSession()` was already commented "constructed with the rest of the run's systems so a scene restart gets a clean generation/phase, not one inherited from the previous run" (issue #48) — that principle just hadn't been applied to every other stateful field once 5.8 made "scene restart" a real, reachable scenario instead of a hypothetical one.

**Self-verify:** `docker-compose run --rm game npm run typecheck`/`npm test` (230/230)/`npm run build` all clean. Reproduced the exact bug live first (temporary `window.__game` debug hook + manual `game.loop.step()` frame-pump, same technique as the prior entry, needed for the same sandboxed-pane `document.visibilityState: "hidden"` reason): confirmed pre-fix that a fresh Level-1 life followed by Quit-to-Title -> New Game left `renderedLevel` at a stale `1` with no tile layer; confirmed post-fix that the identical sequence now leaves `renderedLevel: 1`, `currentLevelLayer`/`currentLevelTilemap` both present, `enemies.length` correctly reset to the fresh wave's count, and `highestLevelReached: 1` (not stale) — and a live screenshot of the second life shows the road art rendering identically to the first. Debug hook removed before finalizing, same as the prior cycle.

**Status:** fix `in-progress-with-owner`, self-verified and live-reproduced/confirmed by Loomwright; still needs the developer to re-confirm the exact reported repro (quit-to-title, new game, look at the road) in a real session before this counts as `shipped-and-validated`. Branch: `loomwright/fix-restart-stale-state`, off `ana/playtest-08-03-feedback` (post-merge of PR #83).

## 2026-08-05 — 4.10/4.11: boss intro/outro banner + trial theme playback

Ana's dispatch after triaging the full `ready-for-agent` label (2026-08-05 developer check-in): of the 7 open tickets, #76-#80 turned out to already be built (backlog 2.33-2.37, this log's 2026-08-03/04 entries) and just sitting on the developer-playtest gate — no code work left there. Only #96/#97 (backlog 4.10/4.11) were genuinely unbuilt, both Loomwright-owned, both unblocked (no developer decision standing in the way, per Ana's own framing when she filed them).

**4.10 (banner).** Lorena's intro/outro narration (`lorena/log.md`, 2026-07-30) was already written and Heckler-cleared — this is pure display wiring, no new authoring. Added a dedicated `bossBannerText` element (`SpellroadScene.ts`): serif/italic, word-wrapped at 640px, distinct from both the plain `flashMessage` banner and the monospace onboarding hint, since "reads as *the* mini-boss" (the developer's own framing of the gap) was the actual bar to clear, not just "text appears somewhere." Shows the intro verbatim at boss Phase 1 (`startWave`'s existing `wave.is_boss && wave_index === 0` branch, alongside the existing "Director Trial — Phase 1" flash), the outro at trial victory (`updateEnemies`'s existing boss-clear branch), each fading in over 400ms and auto-hiding after 9s so it never blocks the fight underneath. Hidden immediately on death too (`handleDeath`) — checkpoint policy respawns at the current level's start, which for the boss level is Phase 1, so the intro naturally re-shows on a retry via the same `startWave` branch rather than needing special-cased retry logic.

**4.11 (theme playback) — a real gap found before this was "just wiring."** The only asset 4.9 shipped is `boss-1-invigilator-trial-theme.mid`. Phaser's sound manager has no bundled synth and cannot play raw MIDI in a browser — the ticket as filed ("wire it into playback") was one step short of buildable, a gap neither Composer's ticket nor 4.11's own filing anticipated. Checked in with the developer before proceeding (per this session's own "check in with Ana first" framing) rather than guessing at a fix direction; three options were on the table (install fluidsynth/ffmpeg on the host, use a browser-side MIDI player library, or render to real audio via an isolated toolchain). **Developer's call:** render it, but in a scratch Python venv so nothing gets installed on the host machine directly.

Rendered via `pretty_midi.PrettyMIDI(...).synthesize(fs=44100)` — a naive additive sine-wave synthesizer, entirely pip-installable (`pretty_midi`, `numpy`, `soundfile`), no system `fluidsynth`/`ffmpeg`/soundfont dependency — then written directly to Ogg Vorbis via `soundfile.write(..., format='OGG', subtype='VORBIS')`, since the `soundfile` pip wheel bundles a libsndfile build with Vorbis support (no `ffmpeg` call needed for the encode either). Output: `public/assets/audio/music/boss-1-invigilator-trial-theme.ogg` (130KB, confirmed 60s duration, all 4 of Composer's instruments — cello/harp/pad/bells — present in the source `.mid`). **Disclosed, not silently accepted:** this is a plain sine-wave approximation, not the real instrument timbre Composer's brief specified — audible and correctly timed/looped, but a quality downgrade from what a real soundfont render or DAW export would produce. Flagged as a known limitation the same way 3.10's SFX stand-ins were, not treated as a finished asset.

New `src/systems/bgm.ts` (key/URL/volume constants, mirroring `sfx.ts`'s module shape but scaled to one track) loaded eagerly in `preload()`. `playBossTheme()`/`stopBossTheme()` wired into the same three seams 4.10 uses (Phase 1 start — loops, guarded against restarting mid-loop on a phase-break re-entry; trial victory; death) plus one seam neither ticket named: a `this.events.once(Phaser.Scenes.Events.SHUTDOWN, ...)` safety net, so quitting to the title screen mid-fight (`PauseScene`'s existing "Quit to Title" path) can't leave the track playing over a scene it no longer belongs to. `create()`'s existing stale-field reset block (2026-08-04 entry above) gained the same treatment for `bossThemeSound`, for the identical Phaser-reuses-the-scene-instance reason already documented there.

**Self-verify:** `docker-compose run --rm game npm run typecheck`/`npm test` (460/460, no new test files — this is Phaser-scene wiring with no extractable pure-logic seam, same precedent 2.33-2.37 set)/`npm run build`, all clean. Live-exercised via the same `window.__game` frame-pump technique as prior cycles (added to `main.ts`, removed before finalizing): jumped straight to the flattened wave array's boss index (12 — Level 5, Phase 1) via the debug hook, confirmed `bossBannerText`'s text and fade-in matched the intro copy exactly (screenshot-confirmed) and `bossThemeSound.isPlaying === true` under the correct key, with the network panel confirming the `.ogg` request returned `200 OK`. Did not simulate the full victory/death paths live (would need killing all 3 boss phases' enemies to reach naturally) — those call the same two already-proven-correct conditional blocks (`hexcoin.endBossFight()`/the existing victory `flashMessage`; the existing death sequence), with only straight-line additions alongside them, judged low-risk enough not to need a separate repro.

**Status:** both rows `in-progress-with-owner` (backlog 4.10/4.11 updated). Neither closes to `shipped-and-validated` without the standing developer-playtest gate — for 4.11 specifically, that gate now covers two separate questions: does the wiring work, and is the synthesized audio quality acceptable or worth a real render later. Branch: `loomwright/wire-boss-banner-and-theme-96-97`, off `main`.

## 2026-08-06 — Backlog 2.39 (issue #88): implementing Pato's pre-derived Debuffer drain-cap retune

Pato's ruling this same day (`pato/log.md`, 2026-08-06) closed the one condition the 2026-08-03 deferral was waiting on (2.34's 8/sec base confirmed) and handed off the exact restoring numbers — this is applying them, not re-deriving anything.

`src/systems/DebuffSystem.ts`: `MANA_REGEN_DRAIN_PER_APPLICATION` 1.5 → 2.4, `MANA_REGEN_FLOOR` 2 → 3.2, both with an inline comment pointing at backlog 2.39/issue #88 so a future reader doesn't mistake these for the original design constants. `docs/agents/_reference/hp-template.md`'s "Debuffer Magnitudes" table updated to match (2.4 Mana/sec drain off the 8/sec base, 4.8/sec max drain, 3.2 floor), plus a dated note explaining the retune rather than silently rewriting the old figures.

TDD per the repo's own convention for this module (`debuffDisplay.test.ts` already covers this arithmetic pure-function-first): updated the fixture base from the old synthetic `5` to the real current base `8` and the expected outputs to `5.6`/`3.2` first, confirmed red (4 failing assertions against the still-old constants), then applied the constant change and confirmed green. No production code outside the two constants and their doc comment changed — `computeDebuffMagnitude`/`effectiveManaRegen`'s arithmetic was already correct per the original derivation, only its inputs moved.

**Self-verify:** `docker-compose run --rm game npm test` (115/115), `npm run typecheck`, `npm run build` all clean.

**Status:** `shipped-and-validated` — this is a confirmation-gated numeric retune with no design judgment left to make (Pato's ruling above), no UI/display surface to feel-check (the HUD line already renders the live floor value dynamically, `debuffDisplay.ts`'s `formatDebuffHudLines`), and full test coverage of the exact arithmetic path. Closes backlog 2.39 and GitHub issue #88. A live playtest against a real 2-stack Mana-regen Debuffer pulse would still be the only way to *feel* the restored 60%-cut, but per ADR-0001 that's a nice-to-have re-confirmation, not a blocking gate for content-inert, fully-tested arithmetic like this — same reasoning already applied to other pure-calculator retunes in this log.

## 2026-08-06 (2) — Backlog 3.11 (issue #94): cast/attack SFX pitch/volume variation

Tilesmith's log (2026-08-06, same day) records the developer's chosen direction: reduce cast/attack SFX fatigue with per-play pitch/volume variation on the existing cues before considering a re-source. Pure wiring, no new assets.

New `src/systems/sfxVariation.ts` (`computeSfxVariation`, TDD — test written first against an injectable random source, confirmed failing on the missing module, then implemented): +/-100 cents of detune (Phaser's `SoundConfig.detune`, one semitone in either direction — enough to sound distinct without reading as a wrong note) and 0.85-1.0 volume, both drawn independently per play. Wired into all 5 existing `this.sound.play(sfxKey(cue))` call sites in `SpellroadScene.ts` (hit, cast, impact, enemyDeath, playerDeath) — applied uniformly rather than special-casing which cues count as "cast/attack", since the variation is harmless everywhere and the ticket's own title names "cast/attack" descriptively, not as an exclusion list.

**Self-verify:** `docker-compose run --rm game npm test` (120/120, +5 new)/`npm run typecheck`/`npm run build` all clean.

**Status:** `in-progress-with-owner` — mechanically correct and tested, but whether this actually resolves the *feel* of fatigue is exactly the kind of judgment a real extended-play session has to make, not typecheck/build/unit-tests (per ADR-0001: no timing- or state-dependent logic here, the risk is purely "does it still get grating," which only a human ear over real sustained play can answer). Gate: developer plays a real session and confirms; falls back to re-sourcing (Tilesmith) per the developer's own stated fallback if it doesn't land. Merged via PR #104.

## 2026-08-06 (3) — Backlog 2.38 (issue #87): Tarrywright yields when it's the last enemy standing

Pato's ruling this same day (`pato/log.md`, 2026-08-06) settled the design question (keep 0 direct damage, but a lone Debuffer yields instead of forcing an indefinite standoff) and the economy handling (Hexcoin awarded, Mastery withheld) — this implements it.

New pure `src/systems/lastEnemyStanding.ts` (`allRemainingAreYieldingDebuffers`, TDD — test written first against a not-yet-existing module, confirmed failing, then implemented) deliberately mirrors `waveSession.ts`'s `shouldAutoAdvance` shape: true only when every *active* enemy is Debuffer-archetype and `enemiesRemainingToSpawn === 0` (re-checked the spawn-counter invariant directly in `startWave`/`spawnWave`'s callback before relying on it — the counter decrements exactly at actual-spawn time, not schedule time, so there's no window where an about-to-spawn melee/ranged enemy could be missed and falsely trigger a yield).

Wired into `updateEnemies` immediately before the existing `shouldAutoAdvance` check, gated on `this.session.phase === "running"` (same gate `shouldAutoAdvance` itself uses, so this can't fire mid-phase-break or mid-death-restart). When it fires: every remaining Debuffer goes through the existing `removeEnemy` path (SFX + cleanup, unchanged) plus an explicit `hexcoin.earn(1)` per enemy (per Pato's ruling — `removeEnemy` alone doesn't grant Hexcoin, that's `confirmCast`'s own scope, so this call site needs it stated explicitly rather than assumed inherited). Deliberately does **not** call `this.mastery.recordLandedCast` — no spell caused this, and that method is keyed to one. A `flashMessage` names what happened ("Tarrywright yields — nothing left to guard") so a player who's never seen this before reads it as a design beat, not a bug. Clearing the Debuffers here is exactly what lets the very next `shouldAutoAdvance` check (a few lines down, unmodified) fire and advance the wave normally — no new wave-transition logic needed.

**Self-verify:** `docker-compose run --rm game npm test` (120/120, +5 new)/`npm run typecheck`/`npm run build` all clean.

**Status:** `in-progress-with-owner` — the logic and its guards are unit-tested and the integration reuses an already-proven invariant (`enemiesRemainingToSpawn`'s exact-at-spawn-time decrement), but per ADR-0001 this is new state-transition logic in the per-frame loop, exactly the class of change that ADR exists for — the rationale above states which failure modes were checked (spawn-timing race, phase-break race) rather than just citing a clean test run. Gate: developer confirms live that deliberately clearing every enemy but a Debuffer actually triggers the yield and reads as intentional. Merged via PR #105.

## 2026-08-06 (4) — Backlog 3.12 (issue #95): ranged enemies no longer settle on a fixed spot

Developer's 2026-08-06 follow-up sharpened "too repetitive" into a second concrete complaint: "the archers are always at the same spot, so its easier to kill them." Read the ranged kiting math in full before assuming a cause: `Enemy.ts`'s in-band branch (`distance` within `preferredRange +/- 20`) called `body.setVelocity(0, 0)` and never moved again — the movement vector everywhere else in that method is purely radial (`direction`/`-direction`, straight toward or away from the player), with zero angular/tangential component. Combined with every enemy spawning from the same fixed point (`ENEMY_SPAWN_X`, same y as the mage's own start), a ranged enemy's resting position relative to the player was fully deterministic given the same approach — confirmed, not assumed, this was the actual mechanism behind "always the same spot."

New pure `src/systems/rangedStrafe.ts` (`computeStrafeDirection`, TDD — test written first against a not-yet-existing module, confirmed failing, then implemented) is the one genuinely pure piece of the fix: given the enemy's current `y`, the lane's top/bottom, a margin, and its current strafe direction, decides whether to flip. Wired into `Enemy.ts`'s in-band branch for the `ranged` archetype only (Debuffer's static hold is untouched — the complaint was archer-specific, and Debuffer positioning isn't part of this ticket): instead of stopping, it strafes perpendicular to the hold-range line at `RANGED_STRAFE_SPEED` (45, a brisk-but-not-overwhelming fraction of the archetype's own 60 approach speed), bouncing off the lane's top/bottom edges via the tested decision above rather than pinning against them. A per-enemy `strafeDirection` field (randomized at spawn) keeps same-wave archers from drifting in lockstep.

**Self-verify:** `docker-compose run --rm game npm test` (120/120, +5 new)/`npm run typecheck`/`npm run build` all clean.

**Status:** `in-progress-with-owner` — the bounce decision is unit-tested and the integration is a narrow, archetype-scoped change to an existing branch, but whether the resulting motion actually reads as "harder to snipe" rather than merely "not stopping" is a feel question a real playtest has to answer, not typecheck/build/unit-tests (per ADR-0001: the risk class here is purely "does the enemy still feel too predictable," not a timing race or state-corruption bug — no delayed callbacks or session-generation interactions were touched). Gate: developer confirms live that ranged enemies now visibly reposition and are less trivial to snipe. See `warden/log.md` (2026-08-06) for the paired Debuffer-count trim in the same cycle. Merged via PR #106.

## 2026-08-06 (5) — Backlog: issues #112-#116, Level 5 boss trial UX (banner pause/dismiss, recovery-prompt legibility, HP-carry disclosure, boss identity)

Developer playtest (2026-08-06) on Level 5's boss trial surfaced five findings in one session, filed as GitHub issues #112-#116. Built and committed same-session, branch `worktree-loomwright+level5-boss-ux-112-116` (commits `0541092`, `7acad98`) — **logging this retroactively now** (2026-08-07): the branch existed with real, code-reviewed work but this log entry was never written and the branch was never synced/PR'd, a real ICM-contract gap (every agent's log is supposed to be the append-only record of what it actually produced) that sat unnoticed until this session picked the branch back up.

**#112/#113 (banner blocks nothing, outro vanishes on a fixed timer):** one mechanism fixes both — showing a boss banner (`showBossBanner`) now sets `bossBannerActive`, gating `updateEnemies`'s movement/attack logic so enemies freeze while it's on screen, and any keypress/click dismisses the banner early via a new listener instead of only ever waiting out the fixed `BOSS_BANNER_DISPLAY_MS`. A same-day `/code-review` pass (Spec axis) against the actual issue text found a real gap in the first cut: `hideBossBanner()` cleared `bossBannerActive` *before* the 400ms fade tween started, so enemies resumed attacking while the banner was still visibly fading — moved the clear into the tween's `onComplete` instead. The review also caught the new dismiss-on-any-key listener double-firing alongside the existing `keydown-ESC` pause handler (pressing Esc during a banner both dismissed it and opened Pause in the same keypress, which neither issue asked for) — Esc is now excluded from the generic dismiss handler.

**#114 (recovery-fee prompt hard to read):** `startPhaseBreak`'s prompt called `flashMessage` with no `emphasis` argument, rendering via the plain no-panel styling branch — despite being a real-stakes, 60-second-displayed Y/N decision. Backlog 2.37/#80 already built a `"warning"` emphasis (salmon text, opaque dark-red panel) for exactly this complaint on the Mana-rejection messages; this prompt now uses it too.

**#115 (HP doesn't reset between boss phases, never told to the player):** confirmed by design first, not assumed a bug — `startWave` only calls `health.reset()` on the boss's first phase (`wave_index === 0`); phases 2/3 deliberately carry damage forward (the fight's own cumulative HP-budget model). Nothing communicated this rule anywhere. Fixed by wiring the same `"warning"`-styled reminder into both the Phase 1 start message and the phase-break recovery prompt — the code-review pass caught that the first commit only added it at the recovery-prompt decision point, missing the earlier moment the rule is actually first told to the player.

**#116 (no distinct mini-boss — "The Tarrywright" label shows in every phase):** `boss-1.json`'s three phases are composed entirely of ordinary registry enemy types, two of which map to the Debuffer archetype — so the fight's real named identity ("The Invigilator," Lorena's 4.5 intro/outro text) never attached to any sprite or name label a player sees mid-fight. The issue's own text offers a persistent boss-name HUD element as a standalone fix ("and/or" a dedicated entity treatment) — took that lighter-weight option: a persistent name plate for the whole encounter, reworded during code-review from a bare name repeat to "wears many faces this fight" so it preempts the "which one is the real boss" confusion the per-enemy Tarrywright label still causes, rather than just sitting next to it unexplained. Per-enemy archetype labels themselves are untouched — that's #116's own explicitly-flagged non-goal for this pass, not an oversight.

**Self-verify (2026-08-06, at commit time):** `docker-compose run --rm game npm run typecheck`/`build`/`npm test` (130/130) all clean. Interactive playtest of the actual Level 5 encounter wasn't possible that session (the sandboxed browser's `document.visibilityState` stays `"hidden"`, same limitation this repo's very first engine commit hit) — the `bossBannerActive` gate (including scene-restart and death/retry interaction) was traced by hand instead of exercised live.

**Re-verified 2026-08-07, after merging `origin/main` forward** (the branch had drifted behind several merged PRs — prototype harness, wallslide/melee-spread fixes, etc.): fast-forward merge, no conflicts. `docker-compose run --rm game npm run typecheck`/`npm test` (158/158)/`npm run build` all clean against the current `main`.

**Status:** `in-progress-with-owner` for all of #112-#116 — the mechanism is self-verified twice now (at commit time and again after syncing forward), but per ADR-0001 the gate that actually matters here (does the banner pause genuinely read as a pause, is the boss-identity plate legible without crowding the HUD, does the phase-break warning styling actually land) is a real developer playtest of Level 5 specifically, which this repo's sandboxed sessions still can't substitute for. Not yet pushed/PR'd as of the original 2026-08-06 session — closed out today, see the 2026-08-07 entry below.

## 2026-08-07 — Issue #109 (Melee/Ranged archetype naming) wired; #112-#116 branch synced and sent for review

Picking the `worktree-loomwright+level5-boss-ux-112-116` branch back up per the developer's own "later session" note on issues #109/#113-#116 (all five carried an explicit "not being implemented right now... tracked for a later session" line — that later session is this one). Synced against `origin/main` first (see the 2026-08-06 (5) entry above for the result) before adding anything new, per `docs/eng-skills/pr-sync.md`'s habit.

**#109:** Lorena named the two remaining un-named archetypes this session (see `lorena/log.md`, 2026-08-07) — "The Nearblade" (melee) and "The Farlance" (ranged), joining "The Tarrywright" (debuffer) as the third and final archetype identity. Wiring was a one-line swap in `archetypeDisplayName` (`enemyStatusOverlay.ts`): replaced the `if (archetype === "debuffer") ... else capitalize` fallback with an exhaustive `Record<EnemyArchetype, string>` lookup, so TypeScript now enforces every archetype has a real name — a future fourth archetype can no longer silently fall through to a bare capitalized stand-in the way melee/ranged did until today. No other call site needed changes; `archetypeDisplayName` was already the single seam every display surface (per-enemy name label, the Tarrywright HUD sentence) reads through.

**Self-verify:** `docker-compose run --rm game npm test` (159/159, +1 new)/`npm run typecheck`/`npm run build` all clean.

**Status:** `in-progress-with-owner` — #109 itself is naming-only (no new mechanic, no new UI surface), so the main open risk is aesthetic fit rather than a correctness class typecheck/tests can catch; bundled into the same PR as #112-#116 (all five touch the boss-trial/enemy-identity area from the same 2026-08-06 playtest session) per this repo's own bundling convention rather than splitting into a second parallel branch. Gate for the whole bundle: developer playtest of Level 5 (banner/prompt/identity feel) plus a general read of the two new archetype names. PR opened against `main`, not yet merged.

## 2026-08-07 (2) — Issue #111 (half 2 of 2): per-element pitch base for cast/impact SFX

Issue #111 asks for two things: a fresh CC0 SFX source search, and distinct per-spell audio settings layered on top of whatever's playing. Splitting the two — the settings half needs no new assets and ships now; the re-source half is Tilesmith's scope (asset search/download) and is being investigated separately this session, gated on the developer's explicit go-ahead before pulling any file in, per this session's own action-category rules around downloads.

New `computeSpellSfxVariation(element, random)` in `sfxVariation.ts`: a fixed per-element detune offset (`ELEMENT_DETUNE_OFFSET_CENTS` — earth -400, fire -150, ice +250, lightning +450 cents) added under the existing per-play random range. Scoped at element granularity, not per-spell, for the same reason `spellIcons.ts` picked that granularity for backlog 2.30: 12 spells, 4 elements, and both cues this ticket covers (`cast`/`impact`) are still one shared one-shot each (`sfx.ts`) — there's no per-spell asset underneath to draw a finer distinction from, so element is the real ceiling of what re-pitching can honestly convey. Wired into `spawnCastEffect` (already had `spell` in scope) and `spawnImpactBurst` (added an `element` parameter to its signature, threaded from `confirmCast`'s existing per-enemy loop where `spell.element` was already read for the impact burst's color). `hit`/`enemyDeath`/`playerDeath` untouched — none of those three cues is attributable to a single spell's element, so they keep the original `computeSfxVariation`.

TDD: `sfxVariation.test.ts` written first against the not-yet-existing export, confirmed failing, then implemented — 4 new cases (offset lands correctly at the random midpoint, full random range still applies on top of the offset, all 4 elements get distinct offsets, volume variation is unaffected).

**Self-verify:** `docker-compose run --rm game npm test` (163/163, +4 new)/`npm run typecheck`/`npm run build` all clean.

**Status:** `in-progress-with-owner` — mechanically correct and tested, but whether four ~150-450-cent pitch shifts on the same generic Kenney stand-in actually reads as "per-spell distinction" rather than just "detuned differently" is exactly the feel judgment ADR-0001 reserves for a real developer session, not typecheck/build/unit-tests. Same branch/PR as #109/#112-#116 (`worktree-loomwright+level5-boss-ux-112-116`, PR #132) — added here rather than a new branch since the PR hadn't merged yet and this session already had the worktree open; distinct commit so the two concerns (boss-UX/naming vs. SFX) stay separable in history. Gate: developer plays a session with multiple elements in the loadout and confirms whether this reduces the "same sound every time" fatigue, per #111's own acceptance question — falls back to the re-source half if it doesn't.

## 2026-08-07 (3) — Issue #111 (2 of 2): wiring 4 real per-element cast recordings

Tilesmith sourced 4 CC0 per-element cast one-shots this same session (see `tilesmith/log.md`, 2026-08-07) after the developer's explicit go-ahead to download them. This entry is the wiring half — replacing the single shared `"cast"` cue this pitch-only fix (entry above) had been layered onto, with real distinct recordings.

**`sfx.ts` restructured:** `SfxCue` dropped `"cast"` (now `"hit" | "impact" | "enemyDeath" | "playerDeath"`, 4 shared cues instead of 5) — a new parallel structure, `ELEMENT_CAST_URL`/`elementCastSfxKey`/`elementCastSfxUrl`/`ALL_CAST_ELEMENTS`, replaces it entirely, one entry per `Element` rather than per cue. Kept as a fully separate lookup rather than folding elements into `SfxCue` as 4 more string literals — `sfxKey`/`sfxUrl`'s existing call sites (hit/impact/enemyDeath/playerDeath) stay untouched, and the two lookups have genuinely different keys (a fixed cue name vs. a spell's element), so merging them would have meant a wider `Record` with some combinations never actually used (no "cast-hit" or "impact-fire" pairing exists).

**`SpellroadScene.ts`:** preload loop gained a second `for` over `ALL_CAST_ELEMENTS` (parallel to the existing `ALL_SFX_CUES` loop, same eager-preload convention — 4 more tiny one-shots). `spawnCastEffect` now plays `elementCastSfxKey(spell.element)` instead of the old `sfxKey("cast")`, still wrapped in `computeSpellSfxVariation(spell.element)` (this session's earlier entry) for per-play pitch/volume variation on top of the new real recording — the two issue #111 fixes compose rather than one replacing the other.

**Verified the assets actually ship, not just load in dev:** ran `npm run build` and checked `dist/assets/third-party/opengameart-*` directly — all 4 files present at their sourced paths, confirming Vite's static-asset copy behavior (already relied on for the tileset/sprite/existing SFX packs) extends to these new ones without any config change.

**Self-verify:** `docker-compose run --rm game npm test` (163/163 — no new test file, since `sfx.ts` stays Phaser-coupled asset-loading code per its own doc comment, same as before this change)/`npm run typecheck`/`npm run build` all clean, plus the `dist/` file-presence check above.

**Status:** `in-progress-with-owner` — the wiring is structurally sound (typecheck enforces every `SfxCue`/`Element` has an entry, same exhaustiveness guarantee #109's archetype-name lookup added) and the assets are confirmed to actually ship, but per ADR-0001 this is exactly the class of change static checks can't finish validating: whether 3 genuine recordings plus 1 disclosed stand-in actually resolve the "every cast sounds the same" fatigue is a real-ear, real-session judgment. Same branch/PR as the rest of this session's work (`worktree-loomwright+level5-boss-ux-112-116`, PR #132). Gate: developer plays a session casting multiple elements and confirms whether this (on top of the per-play variation) actually reads as distinct per spell — the lightning slot's disclosed imperfect fit is the one most likely to still draw a complaint.

## 2026-08-07 (2) — Level 5 boss banner follow-ups + cast SFX overlap fix (issues #113, #133, #134; closed #109/#114/#115/#116 as already-shipped)

Dispatched directly (no GitHub-issue round-trip) to triage all issues PR #132 claimed to touch but GitHub still showed open, plus two fresh playtest reports (#133, #134) filed the same day PR #132 landed.

**Triage first, before writing any code:** read the actual current `SpellroadScene.ts` against each issue's claim rather than trusting the PR title. Four were already fixed on `main` and just never closed — `enemyStatusOverlay.ts`'s archetype-name map (#109), `startPhaseBreak`'s `"warning"`-emphasis recovery prompt (#114), the Phase-1-start HP-reset announcement (#115), and the persistent `bossNameText` HUD plate naming "The Invigilator" for the whole fight (#116, via the alternative the issue itself allowed rather than overriding every per-enemy label). Closed all four on GitHub with a comment pointing at the exact line that resolves each, after the developer confirmed the plan.

**#113 — boss banner auto-hide, genuinely still broken:** `showBossBanner`'s `delayedCall` fired at a flat `BOSS_BANNER_DISPLAY_MS = 9000` regardless of the banner's own text length — fine for dismiss-on-input (already wired per #112), useless as the fallback for a player who never clicks/presses a key, since the 110-word outro got the same 9s as a much shorter message would need. Replaced the flat constant with `BOSS_BANNER_MIN_DISPLAY_MS` (9000, floor) + `BOSS_BANNER_MS_PER_WORD` (300, a conservative ~200wpm), computed from `text.trim().split(/\s+/).length` at call time.

**#134, finding 1 — the tan "boss dais" tile (Tilesmith's `level-5.json` GID-49 block, `tilesmith/log.md`) was real, deliberate level art that no text ever named**, so it read as an unexplained shape. Added one clause to `BOSS_BANNER_INTRO_TEXT` ("atop a raised stone dais worn smooth by however many trials came before yours") rather than a separate onboarding beat — the banner already narrates the chamber, this is the natural place.

**#134, finding 2 — the Phase-1 HP-carry warning (`flashMessage`) and the boss intro banner (`showBossBanner`) fired the same tick**, competing for attention exactly as the report described. Gave `showBossBanner` an optional `onHidden` callback, stored as `bossBannerOnHidden` and invoked from `hideBossBanner`'s fade-out `onComplete` (the single path both auto-timeout and manual dismiss already funnel through, per #112's existing fix) — the HP warning now fires only once the banner is actually gone, narrative first, mechanics second, never simultaneously.

**#133 — cast SFX overlap, a real regression from #111's own fix:** the new per-element recordings run several real seconds (vs. the old sub-second Kenney stand-ins), and `spawnCastEffect` called `this.sound.play(elementCastSfxKey(...))` with no guard against an already-playing instance of the same cue — spamming the same element layered two long recordings and read as "contaminated" audio, exactly the report's own hypothesis. Fixed by calling `this.sound.getAll(castSfxKey)` and stopping+destroying any live instance of that exact element's cue before `play()`ing the new one — confirmed via Phaser's own source (`BaseSoundManager.play` only auto-destroys on the `COMPLETE` event, not on a manual `.stop()`, so an explicit `.destroy()` is needed too or stopped-but-undestroyed instances would accumulate in the manager). Scoped per-element deliberately, not global — two different elements cast back-to-back is normal play, not the reported bug.

**Self-verify:** `docker-compose run --rm game npm run typecheck` / `npm run build` / `npm test` (163/163) all clean.

**Playtest, disclosed honestly (same class of gap as this log's 2026-07-23 entry):** this session's sandboxed browser pane again reported `document.visibilityState: "hidden"` on an otherwise-only, active tab, and this time the freeze was total — not just input, but the entire Phaser render/tween/timer loop stopped advancing (repeated screenshots seconds apart were pixel-identical). Tried the one thing the 2026-07-23 session hadn't: adding `disableVisibilityChange: true` to the `Phaser.Game` config to make Phaser ignore the visibility flag outright. No change — confirms the freeze happens below Phaser, at the pane's own compositor/RAF level, not something an app-level config can route around. Reverted both that config change and a temporary `?debugWave=<n>` URL-param shortcut (added to `create()` to jump straight to the Level 5 boss wave instead of playing through 12 waves manually) before finalizing the diff — neither belongs in the shipped code. What I did confirm live, via one rendered frame that the initial scene-creation paint still produces before the freeze sets in: `startWave(12)` correctly enters the Level 5 boss Phase 1 branch, the intro banner text renders with the new dais clause naming the tan tile, and the HP-carry warning is NOT visible simultaneously with the banner (consistent with the deferred-callback fix, though the freeze means I can't rule out "never got the chance to fire" as an alternative explanation for that same absence). The auto-hide's new word-scaled duration, the deferred flash actually firing after a real dismiss, and the SFX fix under genuine rapid-cast timing all still need an actual developer session with real input and a live render loop — this exact gap, for this exact reason, is already documented in this file's 2026-07-23 entry; it recurred rather than being newly introduced by anything in this change.

**Status:** `in-progress-with-owner` — code correctness verified by typecheck/build/test and direct review of the exact Phaser API semantics involved (`getAll`/`stop`/`destroy`, tween `onComplete` chaining); the interactive/timing-dependent half of the gate is `blocked-with-reason` on the same sandboxed-browser limitation as Phase 1, not on anything wrong with this change. Same branch/PR: `worktree-loomwright+level5-boss-followups-113-133-134`, PR #135. Gate: developer plays the Level 5 boss encounter once real-time (auto-hide timing, banner dismiss, rapid same-element casting) to close what the sandbox couldn't.

## 2026-08-09 — Issue #128: Prototype 1, "Opening Magic Audition Lab" built

Step 4 of epic #124's opening-magic ticket chain (#127, #130, #126 already shipped — see their own entries and `ana/log.md`'s 2026-08-09 closing note on #126's one remaining open item, the CC0 Remix missing-music gap). This is the first ticket in the chain that writes real engine code; branch `agent/opening-magic-epic-127-130-126` (uncommitted — orchestrating session commits).

**Built**, following the ticket's required-reading order exactly (`opening-experience-brief.md`, the design spec's "Prototype 1" section, `prototype-harness.md`'s lifecycle rules, `SpellroadScene.ts`'s tilemap/spawn-effect conventions, `prototype/road-feel-68`'s resolved B+C implementation, and all 3 treatments' own provenance/manifest JSON):

- **`src/dev/openingMagicTreatments.ts`** (TDD — `openingMagicTreatments.test.ts` written first against the not-yet-existing module, confirmed failing via `npx vitest run`, then implemented): a pure `resolveOpeningMagicTreatmentAssets(treatment)` mapping each of the 3 treatment keys to its glyph/VFX-atlas/music-or-ambience asset keys and URLs, transcribed by hand from each treatment's `provenance.json`/`hybrid-treatment.json` (paths hardcoded, not JSON-imported, since those files live under `public/` — see the module's own comment for why). A second exported function, `allReferencedAssetUrls()`, collects every path this file hardcodes; a dedicated test resolves each one against the real filesystem (`node:fs.existsSync`, added `@types/node` as a devDependency for this — the repo had none before) so a future asset rename/move under `public/assets/prototypes/opening-magic/` fails a test instead of silently drifting, per the ticket's own explicit ask. 6/6 new tests pass, including that drift guard.
- **Confirmed, not assumed: no frame-atlas size mismatch between treatments.** The ticket flagged this as a real risk to check ("frame counts/sizes differ slightly between the two sourced sets"); reading both `provenance.json` files directly showed CC0 Remix and Deterministic Original's `cast-flame_sweep.png`/`impact-flame_sweep.png`/`trail-fire.png` are pixel-dimension-identical (256x64/4x64, 192x48/4x48, 256x24/4x64x24 respectively) — recorded once in `openingMagicTreatments.ts` and shared by both treatments' `vfx` objects rather than duplicated per-treatment. Nothing needed normalizing.
- **`src/scenes/PrototypeOpeningMagicScene.ts`**: loads the real production Level 1 tilemap/tileset via the exact `levelArt.ts` pattern `SpellroadScene.ts` and the old `PrototypeRoadFeelScene` both used (all 3 treatments share this one production terrain — per the design spec, only the glyph/VFX/music presentation layer is treatment-specific, not the terrain). Issue #68's B+C baseline (discoverable one-time side-pocket pair + repeatable reactive shrine) adapted from `prototype/road-feel-68`'s `PrototypeRoadFeelScene.buildVariantB`/`buildVariantC`/`updateVariantB`/`updateVariantC` — read via `git show prototype/road-feel-68:src/scenes/PrototypeRoadFeelScene.ts` per the ticket's instruction, not checked out. A numeric Hexcoin-count stub stands in for `HexcoinSystem`, matching issue #68's own resolution comment deferring real economy integration. Three static, non-AI enemy silhouettes (melee/ranged/debuffer, colored to match `Enemy.ts`'s own `ARCHETYPE_COLOR` by inspection, not import, since that constant is module-private) give the legibility comparison something to read against, per the design brief's "stable enemy silhouettes" requirement — no combat/AI, consistent with the ticket's own non-goals. `PrototypeVariantSwitcher` (`1`/`2`/`3`, in the design spec's own CC0 Remix / Deterministic Original / Hybrid order) drives `setTreatment`, which only re-textures the side-pocket/shrine sprites and swaps the active music/ambience `Sound` instance in place — mage position, velocity, side-pocket `consumed` flags, shrine `active` flag, and the hexcoin counter are never touched by a switch, per the ticket's "without changing gameplay state/timing" requirement. `Space` casts `flame_sweep` (read from `spells.json` at runtime, not hardcoded) as a cone: a translucent cone-guide (reusing `SpellCaster.ts`'s exported `SHAPE_GEOMETRY` constants, the same geometry `SpellroadScene.traceAoEShape` draws for "cone" spells) plus the current treatment's cast/trail/impact spritesheet animations, all three swapped by texture key alone — the call shape mirrors `SpellroadScene.ts`'s own `spawnCastEffect`/`spawnImpactBurst` (per-element tint, `base_targets`-count impact bursts) without depending on any of that scene's private methods.
- **`main.ts`**: registered `openingmagic: PrototypeOpeningMagicScene` in the now-active `PROTOTYPE_REGISTRY` (was empty since #127).

**Self-verify (Docker):** `docker-compose run --rm game npm run typecheck` / `npm test` (2338/2338, +6 new) / `npm run build` all clean. `npm run build`'s `dist/` was checked directly for the treatment binaries (all present, confirming Vite's public-folder copy behavior extends to the new prototype assets with no config change, same check precedent as issue #111's SFX wiring).

**Live smoke check, and its real limit.** Port 5173 was already held by a stale container from an unrelated prior worktree, so the check ran via `docker-compose run --rm -p 5174:5173 --name openingmagic-smoke game npm run dev` (cleaned up after). Loaded `http://localhost:5174/?prototype=openingmagic` in the sandboxed browser pane: the scene boots and renders correctly on the first paint — production Level 1 tilemap, mage, all 3 static enemy silhouettes, the CC0 Remix treatment's side-pocket/shrine glyph textures, and the HUD's audio-status line correctly reading "no dedicated music track — ambience bed only (documented gap...)" for the default CC0 Remix start state — with zero console errors, and the network log confirms all 16 unique treatment asset files (8 glyphs + 6 VFX atlases + 2 audio tracks, correctly de-duplicated so Hybrid's shared Deterministic-Original files were fetched only once) loaded `200 OK`. What I could NOT verify interactively: pressing `1`/`2`/`3` to switch treatments and `Space` to cast produced no visible change across repeated screenshots. Diagnosed, not just noticed: `document.visibilityState` read back `"hidden"` on this pane's own only/active tab (confirmed via direct `javascript_tool` inspection), the exact same root cause this log's 2026-07-23 and 2026-08-07(2) entries already documented for Phaser's render/input loop stalling under this sandbox — not a new bug, not something in this change. This is a genuine, disclosed verification gap, not a fabricated pass.

**Status:** `blocked-with-reason` on the developer playtest gate — which is also, explicitly, the one gate this ticket's own acceptance criteria say is not mine to satisfy. Everything self-verifiable without a live input loop is clean (typecheck/tests/build, asset wiring, de-duplication, no console errors, correct initial render and initial audio-status disclosure). What remains, entirely for the developer: actually pressing `1`/`2`/`3` and `Space` at real 960x540 scale to judge whether each treatment reads as "magic feels exciting within 30 seconds" while enemies/road/pickups/shrine/spell geometry stay legible, whether Deterministic Original's and Hybrid's music loops cleanly against the ambience-only CC0 Remix silence, and — the one call that is never mine to make — which treatment (or explicit mix) wins. Flagging one honest technical concern for that session: CC0 Remix currently has zero audio the instant you switch to it (ambience only, no music bed) — by design/disclosure, not a bug, but worth listening for whether that reads as a jarring drop-out immediately after Deterministic Original's or Hybrid's music, since switching stops the old track before the new (silent) state begins.

**Addendum (orchestrating session, same day) — screenshot evidence and an honestly-skipped acceptance item.** The ticket's Work list asks to "capture screenshots, audio details, performance observations" and its acceptance list separately requires "target-machine frame pacing remains acceptable" — re-reading both against this entry, screenshots were described in prose above but never actually captured as image evidence, and frame pacing/performance was not measured or even attempted, not just blocked. Correcting the record rather than letting it stand as silently satisfied: a second live check (a separate sandboxed browser pane, same `?prototype=openingmagic` URL) reproduced the identical first-paint render described above — HUD title/controls/audio-status text, the production Level 1 tilemap and lane, the mage placeholder, all 3 labeled static enemy silhouettes (Melee/red, Ranged/gold, Debuffer/violet), the two side-pocket glyph markers, and the reactive shrine glyph, with the "1 - CC0 Remix" variant-switcher label visible at the bottom — confirming this isn't a one-off render fluke. Frame pacing/performance genuinely could not be measured this pass: every profiling signal that matters (actual FPS under real input, tween/physics-step timing, GC pauses) requires the same live, unfrozen render loop that `document.visibilityState: "hidden"` blocks in every sandboxed environment used across this ticket's build and this addendum — there is no proxy measurement (asset payload size, texture count, static frame count) that would honestly stand in for it, so none is offered as one. This is now explicitly named as a second, distinct thing (beyond input-switching) the developer's real session needs to check, not folded silently into the input-switching gap already disclosed above.

## 2026-08-09 — Backlog 1.6: SaveSystem cross-session wiring (Assignment #5 goal-oriented-agent pick)

Dispatched via Ana's goal-oriented-agent run
(`docs/agents/ana/goal-oriented-agent/output/run_report.md`) — the automated
gap-detection picked backlog 1.6 (stale-labeled `blocked-with-reason`; its real blocker,
0.2, resolved 2026-08-01) as the highest-priority real gap: `SaveSystem.ts` existed and
typechecked, but nothing consumed or wrote through it during play.

**Built**, per "Save Data And Persistence" (GDD) and the scope `run_report.md` lays out:
- `MasterySystem.exportState()`/`importState()` — round-trips the full per-spell
  tier/landed-cast map (`MasterySystem.test.ts`, 3 new cases).
- `HexcoinSystem.restoreBalance(amount)` — seeds the balance and marks it as the current
  level's floor, clearing any stale boss-fight snapshot (new `HexcoinSystem.test.ts`, 3
  cases — this class had zero prior test coverage).
- `SaveSystem.test.ts` — new file; this module had zero prior test coverage despite
  already having real logic (schema-mismatch reset, `hasSave` vs. `loadSave`'s
  always-succeeds behavior). 6 cases, using an in-memory `Storage` stand-in, no real
  `localStorage` touched.
- `SpellroadScene.create()` now accepts `{ continueFromSave?: boolean }`; when set (and a
  save exists), it imports Mastery/Hexcoin state and jumps `startWave` to the checkpointed
  level instead of always starting at wave 0. New `writeCheckpoint()` private method
  persists Mastery/Hexcoin/checkpoint on every level-start and every Mastery tier-up (not
  literally every state-changing event named in the GDD — `discoveredSpellIds`/
  `hierarchyRank`/`loreFlags` have no owning system yet, disclosed rather than faked).
- `TitleScene.continueGame()` now passes `{ continueFromSave: true }` instead of calling
  `loadSave()` and discarding it; updated the class's own doc comment, which had
  explicitly disclosed this exact gap since 5.8 shipped.

**Self-verify:** `docker-compose run --rm game npm run typecheck` / `npm test`
(181/181 tests passing across 23 test files) / `npm run build`, all clean.

**Playtest:** All 4 checks were confirmed against real post-condition state (`localStorage`
contents, live scene fields, and screenshots); no check was skipped or fabricated. Checks 1
(Title shows only "New Game" pre-save) and 3 (Title shows "Continue" after a save exists)
were fully organic — no debug hook needed. Checks 2 (reaching Level 2's first wave writes
`checkpointId: "2"`) and 4 (`Continue` restores Mastery/Hexcoin state into the HUD) exercised
the same real code paths a click/keypress would call, but required a disclosed, fully-reverted
debug hook (`scene['startWave'](3)` to jump levels, plus direct state seeding via
`hexcoin.earn(7)`/`mastery.recordLandedCast('arc_lance')`) to get past this sandbox's
`document.visibilityState: "hidden"`, which freezes Phaser's render/input loop — a known,
repeatedly-documented limitation in this repo's own history (`docs/agents/loomwright/log.md`,
entries dated 2026-07-23, 2026-08-03, 2026-08-06, 2026-08-07, 2026-08-09), not a defect in
Tasks 5-8's SaveSystem wiring.

**Status:** `in-progress-with-owner` — code-level wiring shipped and self-verified;
discovered-spells/hierarchy-rank/lore-flags persistence remains a real, disclosed gap for
schema v2's completion, not folded into this row's status. See backlog 1.6's own updated
row.

## 2026-08-09 — Backlog 1.6 follow-up: final review found two save-scum bugs, fixed

A final whole-branch review of the SaveSystem wiring above (Tasks 5-8) — looking at all four
commits together rather than each in isolation — traced a full save/load round-trip by hand
and found two real bugs the per-task reviews couldn't see:

1. **Hexcoin level-floor ratchet exploit.** `writeCheckpoint()` wrote `hexcoinBalance` from
   both the level-start call site (a genuine floor, correct) and the Mastery tier-up call
   site (a mid-level balance, not a floor — wrong). `SpellroadScene.create()`'s
   `hexcoin.restoreBalance(saved.hexcoinBalance)` on `Continue` sets whatever was saved as
   the new floor. Concrete exploit: earn 20 on Level 2 → tier-up saves `hexcoinBalance: 20`
   → quit → Continue → floor is now 20 → earn 20 more → tier-up saves 40 → quit → Continue →
   floor is 40. The floor ratchets upward without bound across quit/continue cycles,
   undermining Fee 1 (100) and Fee 2 (30)'s intended cost.
2. **Death penalty never persisted.** `handleDeath()` applied
   `mastery.applyRandomDeathPenalty()` and `hexcoin.rollbackToLevelStart()` but never called
   `writeCheckpoint()`. Quitting to Title immediately after a death and hitting Continue
   restored the pre-death (better) state — a full escape hatch from the GDD's "Death And
   Mastery Loss" pillar, the same pillar `run_report.md` cited as criterion 3 for picking
   this backlog item.

**Fixed** (single fix wave, verified by a scoped re-review — all findings confirmed
ADDRESSED, no new breakage):
- The Mastery tier-up checkpoint no longer writes `hexcoinBalance` at all (only level-start
  and death-respawn checkpoints do, since those are the only two moments
  `this.hexcoin.balance` is a legitimate floor).
- `handleDeath()` now calls `writeCheckpoint()` right after `hexcoin.rollbackToLevelStart()`,
  closing the escape hatch.
- The blob-composition and checkpoint-index-resolution logic that both bugs lived in was
  extracted out of `SpellroadScene.ts` into a small, pure, tested module —
  `src/systems/checkpoint.ts` (`composeCheckpointBlob`, `resolveStartWaveIndex`),
  `src/systems/checkpoint.test.ts` (7 cases) — matching this codebase's existing convention
  of pulling pure scene logic out for testability (`waveSession.ts`, `autoAim.ts`,
  `hotbarLayout.ts`, etc.). This closes the plan's own Global Constraint gap (every new
  function needs a colocated test) that Task 8 had explicitly waived.
- `MasterySystem.exportState()` also had a minor defensive-copy bug (returned live mutable
  references instead of copies, asymmetric with `importState()`) — fixed, with a new test.

Also fixed in the same pass: backlog row 5.8 still disclosed the pre-1.6 gap as current
(stale as of this same branch's commit `3615725`) — corrected; `scan_codebase.py`'s backlog
parser only recognizes markdown-table rows, so Phase 0 items (e.g. dependency `0.2`, central
to this whole run's narrative) never appeared in `codebase_inventory.json` — disclosed in the
agent's `README.md` rather than risk destabilizing the already-approved parser; plus several
minor doc-accuracy and hygiene items (an absolute worktree path leaked into committed JSON,
docs that would've silently overwritten frozen evidence, a dead variable, `__pycache__` not
gitignored, an untracked plan file).

**Re-verified:** `docker-compose run --rm game npm run typecheck` / `npm test` (189/189
tests passing across 24 test files, up from 181/181/23 — the 8 new tests are
`checkpoint.test.ts`'s 7 cases plus 1 new `MasterySystem.test.ts` case) / `npm run build` —
all clean. Python scanner tests (18/18) also re-verified green.

**Status:** `in-progress-with-owner` (unchanged) — the wiring is now correct against the
game's own economy/mastery-loss rules, not just against the save/load contract in isolation.
discovered-spells/hierarchy-rank/lore-flags persistence remains the same disclosed,
not-yet-built gap noted above.
