# Heckler — Critique Log

Append-only, dated, one entry per critique run.

## 2026-07-21

Context store established. No critique runs logged yet. (The 2026-07-21 GDD review board at `gdd-review-kit/reviews/2026-07-21/` is a related but separate one-time run of the same six personas against the GDD itself, predating this standing log.)

## 2026-07-21 — HP/Hexcoin mechanic bundle critique (dispatched by Ana)

Critiqued the new HP pool (100, no in-combat regen, reset per wave/checkpoint), the three enemy archetypes (Melee 7/Ranged 4/Debuffer speed-or-Manaregen drain), and the Hexcoin-gated phase-transition HP recovery (15%/phase-break, fee = 50% of [BASIS FLAGGED]) against `hp-template.md` and `hexcoin-template.md`.

- **Systems designer:** BLOCKING — paying Hexcoin to recover up to 15%/phase-break (stacking across multiple phase-breaks in one boss fight) lets currency substitute for tactical play, undermining the stated "wins through positioning, not reflexes" pillar; also flagged the Mana-regen drain floor (2/sec) as unreachable dead code given the hard 2-application stacking cap (5 − 1.5×2 = 2 exactly).
- **Narrative critic:** MAJOR — the mid-fight pay-to-heal fee and the Debuffer archetype both ship with zero fictional framing (no lore hook, no explanation of what spending coin to heal mid-boss-fight means diegetically), reading as a bare monetization-style toggle bolted onto tactical combat.
- **Player psychologist:** BLOCKING — the recovery fee scales with Hexcoin *earned this expedition*, so players who performed well can easily afford it while players actually spiraling toward death (low HP, likely low Hexcoin) can least afford the mechanic meant to save them — inverting the stated purpose of preventing an "unavoidable death spiral."
- **Feasibility lead:** BLOCKING — the fee's basis (expedition-scoped vs. lifetime Hexcoin) is explicitly unresolved in both templates ("Do not implement against either reading until confirmed"), so this bundle cannot actually be built as-is despite being dispatched to close the top BLOCKING finding.
- **Adversarial QA:** MAJOR — no cap on total phase-recovery purchases within a single multi-phase boss fight (a 5-phase boss could recover up to ~60% of the pool across 4 breaks), and "computed fresh at the moment of that phase-break" leaves undefined whether mid-fight kills (minions/adds) feed the "earned this expedition" total, opening a possible self-funding fee loophole.
- **Business analyst:** BLOCKING — two Hexcoin sinks (the existing flat 100-fee spell-choice and the new 50%-of-earnings recovery fee) now draw on the same expedition-scoped pool whose reachability/farmability is *already* an open, escalated disagreement per `hexcoin-template.md`; the economy cannot be tuned or evaluated until that's resolved, and there's no stated cap preventing Hexcoin-rich players from systematically buying their way around the death-trigger Mastery-loss penalty across a whole run.

## 2026-07-21 — Re-critique of the finalized bundle: five decisions resolved, Pato's templates, Warden's validated boss composition (design/numeric layer only, engine deferred)

Re-ran the same six personas against the same bundle now that the developer has resolved all five items flagged above (`docs/superpowers/specs/2026-07-21-death-recovery-fee-decisions.md`), Pato has folded them into `hp-template.md`/`hexcoin-template.md` in final form, Warden has produced and Pato has independently gate-checked (verdict PASS) the vertical slice's first real boss composition (3 phases / 2 phase-breaks, 51 HP competent / 79 HP careless, 1 purchasable recovery), and the GDD carries all five decisions natively. Scope: design/content/numeric layer only — Loomwright's engine implementation is developer-deferred to a future planning cycle and is not reviewed here. Did not take Ana's or Pato's word that anything was fixed; checked each finding against the actual current field/rule text and, where relevant, against Pato's independent recomputation of Warden's numbers.

- **Systems designer:** MINOR (down from BLOCKING) — the core finding is resolved: the cap formula (`phase-breaks − 1`, hard-ceiling 3) plus Pato's 33%-of-competent-play-threat-budget money ceiling (`hp-template.md`, "Money ceiling") is the actual skill-over-money guarantee, and it isn't just asserted — Pato independently re-derived it against Warden's real boss (`pato/log.md`, entry (5)): 15 HP / 51 HP = 29.4% of budget, under the 33% ceiling with a real (if thin) 3.6-point margin, so currency recovers a documented minority of a well-played fight, not a majority. Left unresolved and un-mentioned by any of the five decisions: the Mana-regen-drain floor "2 Mana/sec regardless of stack count" is still dead code — the hard 2-application cap already produces exactly 5 − 1.5×2 = 2, so the floor clause can never actually bind before the cap does. This was bundled into the original BLOCKING verdict but was never in scope of the five-decision spec (which only touches the recovery-fee mechanic), so it's still sitting there unaddressed. Harmless (it can't cause an out-of-spec value), but it's redundant spec language nobody has cleaned up — downgraded to MINOR/cosmetic rather than reissued as BLOCKING.
- **Narrative critic:** MAJOR, unchanged — confirmed independently (not on Ana's say-so) that this is genuinely still open, not quietly fixed: `docs/agents/lorena/log.md` contains zero entries beyond "context store established," and the decision record itself defers this explicitly ("Lorena — narrative framing for the fee and the Debuffer archetype (still MAJOR, not blocking), once the mechanic is numerically stable enough to be worth narrating"). The fee and the Debuffer archetype still ship with no fictional framing whatsoever. Correctly sequenced (the mechanic needed to be numerically stable first), but "sequenced" isn't "resolved" — flagging so it doesn't fall off the list once the numeric bundle ships.
- **Player psychologist:** CLEAR (BLOCKING resolved) — the specific inversion (fee scaling to exactly match whatever a player currently has, so a thriving player always affords it while a spiraling player with little Hexcoin affords proportionally little) is gone: the fee is now a flat, fixed 30 Hexcoin regardless of performance (`hp-template.md`, "Fee — Pato's numeric call"; `hexcoin-template.md` Fee 2). Because the price no longer tracks the player's own balance, a struggling and a thriving player face the identical number, which is what the developer asked for. New/residual, not a re-issue of the original bug: a flat fee replaces a graduated inversion with a hard affordability cliff — a player with 29 Hexcoin gets zero recovery, identical to a player with 0, while a player with 30+ gets full access regardless of surplus. Not flagging this as a fresh BLOCKING finding because the template already discloses and accepts exactly this tradeoff in its own words ("A player who has coasted or died a lot... genuinely might not be able to afford it... That gap is the intended tension, not a flaw") — but noting it so "resolved" isn't misread as "no player ever gets shut out," which isn't true and was never claimed.
- **Feasibility lead:** CLEAR (BLOCKING resolved) — the fee's basis, explicitly unresolved before ("Do not implement against either reading until confirmed"), is now unconditionally settled: expedition-scoped, resetting at every checkpoint (`hp-template.md` "Basis — resolved, expedition-scoped"; `hexcoin-template.md` "Basis, resolved"; GDD "Phase-Transition Recovery," Basis bullet). All three sources agree verbatim on the same reading — this is buildable as specified, whenever Loomwright's engine work is actually scheduled.
- **Adversarial QA:** MAJOR (new finding; original MAJOR resolved) — the original two findings are both closed: the cap-formula + money-ceiling pair bounds total recovery per fight (confirmed against Warden's actual composition, not just the rule text), and the mid-fight-kill freeze (`hp-template.md` "Mid-fight-kill freeze," decision 4) closes the farm-to-afford loophole by snapshotting the eligible balance at fight-start. New hole the resolution itself opens: the whole flat-30-Hexcoin fee is priced against an assumption that per-expedition income is a hard, one-time bound ("forward-only... bounds per-expedition Hexcoin income to a predictable number," `hp-template.md`'s Fee reasoning) — but "forward-only" is only ever defined against *cleared* road segments (GDD, Gameplay Loop: "once a road segment is cleared, the mage cannot backtrack into it"). Nothing in the bundle says whether a boss segment counts as "cleared" before it's beaten, or what happens to the waves leading up to it on a retry: death sends the mage back to "the last road-segment checkpoint" (`hp-template.md` Death trigger; GDD Death And Mastery Loss), and if that checkpoint sits before the pre-boss waves rather than immediately at the boss door, dying and retrying could re-earn Hexcoin from the same not-yet-cleared waves each attempt — quietly un-bounding the exact income figure (~20-70 Hexcoin) Pato used to justify the flat-30 number. This doesn't invalidate Warden's already-validated composition, but it's a real gap in the economic model the five decisions never addressed, grounded in a specific unstated interaction (checkpoint placement relative to boss-retry) rather than a vague "farming feels possible" vibe.
- **Business analyst:** CLEAR (BLOCKING resolved) — forward-only (GDD, Gameplay Loop) directly closes the reachability/farmability dispute this finding depended on, and the two-fees-one-pool tension is now explicitly owned as intended design rather than left as an unresolved risk (`hexcoin-template.md`, "Resolved 2026-07-21 — the two-fees-one-pool tension": Fee 2 affordable against the ~20-70 estimate most of the time, Fee 1 deliberately not, spending on one is a real tradeoff against the other, and Pato explicitly declines to re-price Fee 1 to compensate). The "buying around the Mastery-loss penalty across a whole run" half of the original finding was always slightly overstated on inspection — Fee 1 never removes the mastery-tier loss, it only makes the loss non-random, so there was nothing to newly bound there. One dependency worth naming: this whole resolution's economic soundness (both the reachability closure and the flat-30 pricing) inherits the same open question the Adversarial QA persona raised above about checkpoint/retry income — if that gap turns out to unbound income, the "bounded, predictable" framing this business-analyst clearance rests on would need re-examination. Not re-issuing the original finding, but the two personas are pointing at the same seam.

**Overall:** four of six personas clear the original bundle outright (Feasibility Lead, Business Analyst) or resolve to a materially better state with only a disclosed, non-blocking residual (Player Psychologist, Systems Designer). One re-affirms an already-known, already-deferred gap rather than discovering a new one (Narrative Critic — MAJOR, sequencing not a defect). One surfaces a genuinely new, specific hole the resolution itself introduces (Adversarial QA — MAJOR: checkpoint/retry income bounding is unstated and the whole flat-fee price depends on it holding). No BLOCKING findings remain against this bundle at the design/numeric/content layer. The design/numeric/content layer (five decisions, both templates, one validated boss composition) is clean enough to mark `shipped-and-validated` for that layer specifically; the two open MAJORs (narrative framing, checkpoint/retry income bound) should stay tracked as separate, named follow-ups rather than silently closed, and engine implementation remains explicitly out of this critique's scope per the developer's own deferral.

## 2026-07-23 (2) — First playable vertical thread critique: engine build, 3 spells, 3 waves (dispatched by Ana)

Critiqued Loomwright's first real engine build (`src/systems/*`, `src/entities/*`, `src/scenes/SpellroadScene.ts`) plus Frieren's 3 spells and Warden's 3 Level-1 waves, both already Pato-validated. Read the actual code against `hp-template.md`/`mana-template.md`/`mastery-template.md` before writing anything, per my own standing rule of re-deriving rather than trusting a summary.

- **Systems designer:** BLOCKING — two confirmed code-level bugs, found by static reading alone, not speculation. (1) `HealthSystem.reset()` was called only from `handleDeath()`, never from `startWave()`, contradicting `hp-template.md`'s unconditional "full reset to 100 at the start of every wave" and silently letting HP debt carry across waves within a level. (2) `SpellCaster.tryCast` applied the Master-tier -10% discount to *both* cost and cooldown simultaneously, when `mana-template.md`/`mastery-template.md` both specify a per-spell choice of one or the other, never both — and `SpellDefinition` had no field to record that choice at all.
- **Narrative critic:** MAJOR, unchanged — no Lorena-authored content anywhere in this bundle; correctly sequenced (per my own 2026-07-21 entry), not newly broken.
- **Player psychologist:** MAJOR — the HP-reset bug is a felt-fairness problem, not just a systems bug: the HUD promises a clean slate each wave while secretly carrying damage forward. Separately, the entire preview-confirm cast loop had never been felt by a human hand under real pressure as of this critique — only reasoned about via code.
- **Feasibility lead:** MAJOR — `SaveSystem` is built but not wired into the scene (by the team's own disclosure); nothing persists across a reload, and the whole validated death/recovery economy has zero code exercising it yet (no boss wired in). Checkpoint/retry placement (backlog 0.2) remains open, with the current placeholder actively re-running already-cleared waves on death — the exact risk I flagged 2026-07-21.
- **Adversarial QA:** BLOCKING — the disclosed testing gap already cost something concrete: the HP-reset regression is exactly what a five-minute interactive pass would have caught, and wasn't, because it couldn't be run. Separately, real (validated) spell power was never cross-checked against placeholder enemy HP for time-to-kill: `arc_lance` needs 6 landed hits (~12s minimum) to kill one 18-HP Melee, against a wave that spawns 5 enemies at once — an untested seam between two independently-placeholder/validated numbers.
- **Business analyst:** CLEAR/MINOR — no economy decisions changed this drop; noted only that zero progress currently survives a page refresh, which is not-yet-wired rather than broken.

**Verdict, independently concurred with rather than rubber-stamped:** the two systems-designer findings are genuine BLOCKING code bugs, not playtest-dependent — they needed fixing regardless of whether a human ever touches a keyboard. Everything else is legitimately `in-progress-with-owner`, already disclosed, not hidden.

**Resolved same-session, immediately after this critique landed:**
1. `SpellroadScene.startWave()` now calls `this.health.reset()` and `this.debuff.clear()` at the start of every wave, matching `hp-template.md` exactly.
2. `SpellDefinition` gained a `master_discount: "cost" | "cooldown"` field; `MasterySystem.getScaling()` no longer returns a cost/cooldown multiplier at all (removed the double-dip trap at its source rather than special-casing around it); `SpellCaster.tryCast` now applies the -10% to exactly one of cost or cooldown, per each spell's own field. All three shipped spells assigned a value with stated reasoning (`arc_lance`/`frost_nova` lean cooldown — reinforcing "fast poke" and cutting the "long exposure after cast" weakness respectively; `flame_sweep` leans cost — sustaining the generalist role across a longer fight).

Both fixes typecheck and build clean. The untested-TTK seam (Adversarial QA) and the unwired SaveSystem (Feasibility Lead) are not fixed — correctly not fixable without inventing numbers the roster doesn't have yet (a real base-enemy-HP template, and the checkpoint-placement decision respectively) — and remain tracked in the backlog rather than patched with a guess.

## 2026-07-25 — Autonomous-session critique: kiting/wall-slide retune, aiming fallback, hotbar 3->6, hit-feedback, 9 new spells, Level 2/3 waves, Mastery rate 20/tier

Developer stepped away for a full autonomous session; Ana kept the roster producing without a real-time human sanity check. Read `ana/log.md`'s three 2026-07-25 entries, `warden/log.md`, `pato/log.md`, and `frieren/log.md`'s 2026-07-25 entries in full, then read the actual current code directly rather than trusting any summary: `src/entities/Enemy.ts`, `src/scenes/SpellroadScene.ts`, `src/systems/WaveLoader.ts`, `src/systems/MasterySystem.ts`, `src/systems/ManaSystem.ts`, `src/entities/SpellCaster.ts`, `src/data/enemyRegistry.ts`, `src/data/spells/spells.json`, `src/data/waves/level-1.json`/`level-2.json`/`level-3.json`, and `docs/agents/_reference/mastery-template.md`. Confirmed the wave JSON files match Warden's logged compositions exactly (no drift between log claim and shipped data). No Docker available in this environment; would have run `typecheck`/`build` per the docker-testing-contract if it were — flagging that substitution rather than letting it pass silently, per the contract's own instruction.

**BLOCKING**

1. **Systems designer — the wall-slide fix gets the sign backwards for the axis it wasn't specifically designed around.** `Enemy.ts`'s retreat-blocked branch picks a slide direction via `towardCenter = this.y >= this.lane.centerY ? -1 : 1`, then `velocity = perpendicular * towardCenter`. This is correctly tuned for the case Warden's spec actually names — pinning against the lane's *short ends* (left/right, `blockedRight`/`blockedLeft`) — I hand-verified all four left/right sign combinations and the enemy always slides toward the vertical centerline as intended. But the code applies the identical `towardCenter`-by-*y*-position logic when `blockedBottom`/`blockedTop` (the *long* walls) trigger instead, and there the correct escape direction depends on `direction.x`'s sign (the mage's horizontal offset), not the enemy's own y-position — a variable the formula never looks at. Concrete counterexample: enemy at (400, 300) near the bottom wall, mage at (600, 200) (up-and-right) → `direction≈(0.894,-0.447)`, `perpendicular≈(-0.447,-0.894)`, `towardCenter=-1` (y=300≥270) → final velocity ≈ `(0.447, 0.894)·speed`. The y-component is *positive* — the enemy is commanded to move further down, deeper into the wall it's already pinned against, the exact nose-pinning behavior this fix was built to eliminate. This isn't a rare corner case: `RANGED_PREFERRED_RANGE` (240) and `DEBUFFER_PREFERRED_RANGE` (150) both dwarf the lane's own ±80px vertical half-span (`ROAD_HEIGHT` 160), so a kiting enemy sitting near a vertical extreme is a routine outcome, not an edge case, and roughly half the time (whichever side of the enemy the mage happens to be on) the "fix" makes the pin worse instead of resolving it. Separately verified (so this isn't reflexively "everything's broken"): true two-wall corner blocks (e.g. `blockedRight`+`blockedBottom` together) are structurally self-resolving under this same formula — I derived all four corner sign combinations by hand and in each, exactly one axis escapes regardless of which sign `towardCenter` picks, so corners are fine. The bug is specific and isolated to the single-axis top/bottom trigger.

2. **Systems designer / Adversarial QA — the Mastery growth-rate sizing (backlog 0.4) is built on a premise the game's own shipped numbers already contradict.** `mastery-template.md` and `pato/log.md` (2026-07-25 (3)) both derive the 20-casts/tier number from "a 1-target spell can accrue up to ~1 landed cast per enemy killed" — i.e., 1 landed cast ≈ 1 kill. That's false against the numbers already in the repo: `Enemy.ts`'s placeholder HP (melee 18, ranged 14, debuffer 22) is 2-6x every Novice-tier 1-target spell's power (`stone_spike` 4, `magma_lance` 9) — no spell one-shots any enemy at Novice tier, so every kill costs multiple landed casts, not one. Recomputed against Level 2's actual, file-verified composition (melee 8 / ranged 7 / debuffer 6, 21 total): spamming `stone_spike` (power 4, the fastest-mastering spell that's actually default-equipped and reachable, see finding 4) exclusively yields `8×5 + 7×4 + 6×6 = 104` landed casts available in that single level alone — 2.6x the 40 casts needed for full Novice→Master, not "nearly two full levels" as the template states. `magma_lance` (power 9, the hardest 1-target hitter, not default-equipped) still yields 48 in Level 2 alone — still over the 40-cast full-mastery threshold, inside one level. The "ordinary varied play" side of the same derivation (~20.5 casts across all 3 levels, meant to show generalist play stays far short) carries the identical flaw — it also assumes a landed cast permanently clears `base_targets` enemies rather than needing several hits per kill. Both halves of the claimed asymmetry ("only actively-mained spells progress... most of the kit stays Novice/Adept") are therefore wrong in the same direction: real achievable landed-cast counts are dramatically higher than modeled, for both specialist and generalist play, which is exactly the failure mode ("caps most of the spellbook at Master within a single level") backlog 0.4 was created to avoid. The rate may still turn out fine, but the number wasn't actually checked against the numbers that exist in the same codebase.

**MAJOR**

3. **Player psychologist — the aiming fallback's permanent switch defeats the exact user it was built for.** `SpellroadScene.ts`'s `pointerHasMoved` flips true on the *first* `pointermove` event and never resets — there's no path back to last-move-direction aiming for the rest of the session. This flag was added specifically because of a "trackpad-difficulty complaint" (`ana/log.md` 2026-07-25 (2)), but a trackpad is exactly the input surface most prone to firing incidental `pointermove` events (a resting or twitching finger, not a deliberate aim) — meaning the player this fallback exists for is likely to trip the permanent mouse-only switch within seconds of starting, with no way back to the movement-based aiming they may have actually wanted to keep using.

4. **Feasibility lead — the default 6-spell hotbar loadout is an accident of file order, not a designed kit, and it orphans half the just-shipped content.** `equippedSpells = this.spells.slice(0, 6)` resolves to `arc_lance`/`flame_sweep`/`frost_nova` (originally shipped) plus `stone_spike`/`flare_jab`/`spark_ring` — the first 3 of Frieren's new 9, which are exactly that batch's 3 Light-weight entries, purely because of how the 3×3 grid happened to be enumerated in the log. Result: 4 of 6 equipped spells are Light-weight; earth and lightning never get a Standard or Heavy slot; and the other 6 spells — including all 3 Heavy spells (`magma_lance`, `frost_breath`, `tremor_field`) and 2 of the 3 Standard spells — are entirely uncastable in the current build (no loadout-swap UI exists to reach them). This directly contradicts Warden's own stated Level 2/3 escalation reasoning (`warden/log.md`: "Frieren's newer heavy-weight options... make the denser, more-simultaneous-target waves faster and safer to clear") — those spells cannot be doing that job today because no player can ever equip them. Nobody appears to have checked which 6 of the 12 spells this slice actually lands on before shipping it as the default.

5. **Feasibility lead — this session's actual engine work has no Loomwright log entry at all.** Lane clamping, the wall-slide behavior, the aiming fallback, the hotbar widening, and the `LANDED_CASTS_PER_TIER` constant swap are all in the running code (with in-code comments citing backlog 2.10 and Pato's validation), but `docs/agents/loomwright/log.md` has zero entries past 2026-07-23 (2). Every prior engine change in this project shipped with a dated Loomwright entry disclosing what changed and whether `typecheck`/`build` were run, per the docker-testing-contract; this batch breaks that pattern. There is no record anywhere of whether `npm run typecheck`/`build` were run against this exact code, or whether a human has ever watched the wall-slide, aiming fallback, or widened hotbar actually run — a real gap for a session with no developer present to catch it live.

**MINOR**

6. **Business analyst — `HexcoinSystem.resetExpedition()` exists but is invoked nowhere in `SpellroadScene`.** Hexcoin now silently accumulates across the full concatenated 9-wave/3-level run with no checkpoint reset. Not new — this is the same open item already tracked as backlog 0.2 — but worth re-flagging now that "expedition" is genuinely a 3-level, 60-enemy run in one continuous scene rather than the single-level sample it was last assessed against.

7. **Narrative critic — still zero Lorena-authored content, gap now wider.** Confirmed directly against `docs/agents/lorena/log.md`: no entries past 2026-07-21. Correctly sequenced per the standing decision (mechanics before narrative), not a new problem, but this session shipped 9 spells and 6 waves of content with no lore framing for any of it, same unaddressed gap as every prior critique, just bigger now.

8. **Adversarial QA — every enemy spawns at exactly the wall-slide tie-break value.** Spawn point is `{x:820,y:270}` and `lane.centerY` is also 270 — every fresh spawn sits precisely on the `this.y >= lane.centerY` boundary, so the tie resolves identically for every enemy at the moment it appears. Harmless today (Pato already confirmed spawn is far enough from any preferred range that the first move is always "approach," not retreat), but it's a latent shared-direction artifact worth naming rather than leaving as an unexamined coincidence.

**Overall:** two BLOCKING findings are confirmed by direct arithmetic against numbers already in the repository, not speculation — the wall-slide sign error is real and reproducible for a specific, non-rare geometry (top/bottom block with the mage on the "wrong" side), and the Mastery-rate sizing's core premise is falsified by the enemy-HP and spell-power values that already exist in the same codebase Pato's derivation was supposedly checked against. The three MAJOR findings (aiming-fallback lock, accidental default loadout, missing Loomwright log) share a theme: this was the first session with nobody in the loop to catch things live, and exactly the kind of interaction/curation/process gaps a real-time developer would likely have caught got through. Nothing here is a crash or a build break — the game almost certainly still runs — but "shipped a lot in one sitting" and "actually correct" are not the same claim, and at least two of these findings (1, 2) should be fixed, not just disclosed, before this batch is called done.

## 2026-07-30 — Narrative critique: phase-recovery prompt, Fee 1 flavor, the Tarrywright, the Invigilator intro/outro (backlog 4.6, partial — 4.1a/4.1b/4.2/4.5)

Critiqued Lorena's first real narrative pass (`docs/agents/lorena/log.md`, 2026-07-30 entry) against `docs/agents/_reference/lore-premise.md` (tone, ending-scope lock, originality, UI-space rule, and the two newly-appended Established Named Facts), `hp-template.md`, `hexcoin-template.md`, and `mastery-template.md` for mechanic accuracy, and the actual current code (`src/scenes/SpellroadScene.ts`) for the one piece (4.1a) that claims to be a drop-in UI replacement. Did not take the flavor text's own framing at face value — re-derived the phase-break numbers against the live constants and read `flashMessage`'s real implementation before accepting "matches the existing Y/N prompt shape" as true.

**BLOCKING**

1. **Feasibility lead — Piece 4.1a is not the drop-in it claims to be; it will render off-screen.** Lorena's log states the replacement "matches the existing Y/N prompt shape so it drops in without needing new UI layout." The actual call site (`SpellroadScene.ts:494-497`, `startPhaseBreak`) feeds this string straight into `flashMessage`, which does nothing but `this.messageText?.setText(text)` (line 675) on a single `Phaser.GameObjects.Text` created once at line 218-223: `this.add.text(480, 400, "", { fontFamily: "Georgia, serif", fontSize: "20px" })` with `setOrigin(0.5, 0.5)` and **no `wordWrap` option and no backing panel/rectangle**, centered on a 960×540 canvas (`src/main.ts:12-13`). Every existing call site is short (4-40 chars); the current longest, the phase-break prompt itself, tops out around 90-100 chars on one line (`` `Phase clear! [Y] Pay ${FEE_PHASE_RECOVERY} Hexcoin -> restore ${...} HP  /  [N] Continue` ``) and that line, at 20px Georgia, already runs close to the 960px canvas width unwrapped. Lorena's replacement is two lines — "The Director tallies the toll: thirty Hexcoin returns ten drops of what this phase took." (~91 chars) and "[Y] Pay 30 Hexcoin — the ledger updates, 10 HP returns  /  [N] Keep your coin, carry the wound." (~98 chars) — roughly double the existing single-line max, stacked via a bare unwrapped Text object. At this font/canvas, both lines will overflow past the left and right screen edges (and possibly overlap other HUD text), not "drop in" as claimed. This is not a taste call; it's a specific, falsifiable UI-space-fit violation (criterion d in the dispatch) — the piece needs either real word-wrap plus a backing panel, or a hard line-length cut, before it can ship as the literal replacement string.

**MAJOR**

2. **Adversarial QA — Piece 4.1a hardcodes the fee/restore numbers as English words instead of interpolating the live constants, and this exact pair of numbers has already changed twice.** The replacement text says "thirty Hexcoin returns ten drops" and "[Y] Pay 30 Hexcoin... 10 HP returns" as static prose, not `${FEE_PHASE_RECOVERY}`/`${Math.round(MAX_HP * PHASE_RECOVERY_HP_FRACTION)}` like the string it's replacing. `hp-template.md` shows this fee was already revised twice (50%-of-earnings → flat 30 Hexcoin; 15% restore → 10% restore) before landing on today's 30/10. If Pato re-tunes either number again — the file's own "Tiering by boss/expedition: not introduced now" note flags this as likely once a second boss exists — this flavor string goes silently stale and starts lying to the player about the actual cost/reward unless whoever edits the constant remembers to hand-edit the prose in lockstep. The line it replaces was correct by construction; this one is correct only by vigilance.

3. **Narrative critic — the Invigilator's destruction imagery leans "unraveled/outwitted" rather than "destroyed," right where the ending-scope lock most needs it not to.** The outro's key sentence: "The Invigilator's geometry comes apart the way frost leaves a window — not shattered, just no longer held together." The lock (`lore-premise.md`) requires the mini-boss trial to read strictly as a *combat* resolution, with "outwitted" reserved as unresolved long-term promise. "Not shattered, just no longer held together" is deliberately non-violent, melancholic imagery — tonally right — but it reads more like a puzzle coming undone or a spell being dispelled (outwitted-flavored) than something beaten down in a fight (destroyed-flavored). No sentence anywhere literally claims the Invigilator was outwitted, and the piece's own appended scope note is explicit about what did and didn't happen — so this isn't a hard rule violation — but the central image is doing double duty (melancholy + ambiguity about *how* the win happened) in exactly the spot where the brief asked for the ambiguity to be squeezed out, not preserved. Recommend a pass that keeps the non-triumphant mood but makes the mechanism of defeat read as force/combat rather than dissolution, so a future piece extending toward "outwitted" doesn't have to fight this one's imagery for the distinction.

4. **Business analyst — the two-fees-one-pool tension (`hexcoin-template.md`, "Resolved 2026-07-21") still has zero diegetic framing, and this dispatch was the natural moment to give it one.** Both 4.1a (Fee 2, 30 Hexcoin) and 4.1b (Fee 1, 100 Hexcoin) now have flavor text in isolation, but neither piece — nor anything else in the repo — acknowledges that spending on one directly starves the other from the same bounded, expedition-scoped pool. A player who pays the phase-break toll mid-fight and later can't afford the mastery-choice mercy gets no in-fiction nod that this was the tradeoff the economy is explicitly designed to create (`hexcoin-template.md`: "a real, felt tradeoff, not a design defect"). Not a defect in what shipped — out of scope for 4.1/4.2/4.5 as dispatched — but worth naming now so it doesn't quietly fall off the backlog the way the Debuffer's narrative gap did for four straight critique cycles before 4.2 finally closed it.

5. **Player psychologist — 4.1a asks a player mid-boss-fight to read a metaphor before they can find the button.** Independent of the overflow bug above: at a phase-break, the player is paused with elevated stakes and wants the mechanical choice fast. The replacement puts the flavor line ("The Director tallies the toll: thirty Hexcoin returns ten drops of what this phase took") *before* the actionable Y/N line, meaning the first thing rendered is a sentence that requires parsing a metaphor ("drops of what this phase took" = HP) before the reader reaches the part that tells them which key does what. The original single-line prompt put the mechanical instruction front and center with zero decoding required. This is a pacing cost the dispatch's own "genuinely short enough for an on-screen Y/N prompt" criterion is meant to catch, separate from and in addition to the raw character-count/overflow problem.

**MINOR**

6. **Systems designer / Adversarial QA — the Tarrywright flavor text's "never both at once" phrasing is compatible with, but doesn't disambiguate, the actual fixed-per-instance mechanic.** `hp-template.md`: "Debuffer drains either speed or Mana regen per instance (Warden's choice per encounter, not both from the same instance)" — confirmed at the data layer by `src/data/enemyRegistry.ts:18-19`, which hard-codes two separate enemy-type entries (`murmur_wisp` → `mana_regen`, `creeping_bramble` → `speed`) rather than one entry that can switch. So the real rule is: a given Tarrywright is permanently one variant or the other for its whole existence, never alternates. Lorena's line — "a pulse that pulls either the strength from your legs or the warmth from your Mana, never both at once" — is literally true but reads equally well as "alternates pulse-to-pulse, just never simultaneously," which is not the mechanic. Since this is one shared flavor entry meant to cover both `murmur_wisp` and `creeping_bramble` (per Lorena's own framing, "wherever the Debuffer gets a bestiary/encounter-log entry," singular for the archetype), a player reading it gets no signal about which drain *this specific enemy* applies — probably fine if some other in-fight cue (icon/color) carries that information, but worth confirming rather than assuming, since the prose alone under-specifies it.

7. **Narrative critic — the "mercy" motif is used three times across three of the four pieces** (Tarrywright: "it can only spare you half a mercy"; Fee 1: "buys one mercy the Director rarely grants"), plus the recurring ledger/toll accounting motif (4.1a "tallies the toll," "the ledger updates"; Invigilator outro "somewhere in the ledger a line closes"). The ledger echo across 4.1a and 4.5 is a genuine, well-earned thematic thread (credit where due — it's the strongest cross-piece consistency signal in this drop, and directly satisfies the tone-consistency criterion in the dispatch). The doubled "mercy" framing is thinner: two uses in two separate pieces isn't a problem by itself, but it's the only recurring emotional beat besides the ledger motif, and a future piece reaching for a third "mercy" callback would start to read as a tic rather than a deliberate through-line. Not a defect in what's shipped today.

8. **Narrative critic (originality) — "the Invigilator" as a testing/grading AI overseer sits inside a well-worn trope (GLaDOS-style test administrators, Danganronpa-style graded "trials"), even though no name or specific character is copied.** The originality rule as written ("never introduce named factions, characters, spells, or lore that copies an existing published work") is about proper nouns and specific IP, and "Invigilator" the word, plus the Director's established "beautiful, convincing prison" framing, isn't a copy of any single named thing — so this doesn't violate the letter of the rule. Flagging as a MINOR concept-derivativeness note, not a rule violation: the "curious, unhurried AI that frames captivity as an exam" shape is recognizable enough that it's worth Lorena knowing the trope space she's standing in, especially since this is the piece most likely to get expanded when "outwitted" content eventually gets written.

**CLEAR**

9. **Systems designer — 4.1a's and 4.1b's numbers are correct against the templates.** 30 Hexcoin / 10 HP restore matches `hp-template.md`'s "Fee — Pato's numeric call" and `hexcoin-template.md`'s Fee 2 exactly; 100 Hexcoin in 4.1b matches Fee 1 and correctly frames it as "the choice of which spell forgets you first... it still insists on taking something," matching `mastery-template.md`'s actual rule (choice replaces the *random* roll, it never removes the tier loss itself). No invented numbers, no misrepresented mechanic.

10. **Adversarial QA — re-read the Invigilator outro sentence-by-sentence against the ending-scope lock and found no line that actually claims resolution of "outwitted" or "transformed."** The hedges are explicit and load-bearing: "this was also, once, someone's careful work," "you do not know yet whether the Director notices, or minds, or is already writing the next trial," "The road ahead stays exactly as endless as it was an hour ago." Combined with Lorena's own appended scope note, this piece does the job the lock requires at the level of literal claims (see finding 3 for the one place the *imagery*, not the claims, cuts it close).

11. **Business analyst — Fee 1's flavor text does not misrepresent or invent UI/mechanics that don't exist yet (criterion f).** "A hundred Hexcoin — a hundred small deaths dealt to earn it" correctly reflects the 1-Hexcoin-per-kill earn rate and the GDD's own "roughly the return from 100 defeated enemies" framing (quoted in `hp-template.md`); the text is written for a screen Lorena explicitly discloses doesn't exist yet, and doesn't claim any interaction (a spell-picker list, a confirm button, specific wording) beyond the one thing the mechanic actually does — let the player choose which equipped spell takes the loss.

**Overall verdict:** hold 4.1a specifically — the BLOCKING finding (1) is a concrete, falsifiable claim ("drops in without needing new UI layout") that is false against the current `flashMessage`/`messageText` implementation, and shipping this exact string as the literal file replacement will put unreadable, off-screen text in front of a player at the single highest-stakes decision point in the vertical slice (a boss phase-break). Do not swap this string into `SpellroadScene.ts:496` until either the line is cut substantially shorter or `flashMessage` gains real word-wrap/paneling — whichever the developer prefers, but pick one before this ships. 4.1b, 4.2, and 4.5 have no BLOCKING findings and can ship as-is; the three MAJOR notes (2, 3, 4) are real but not gating — 2 is a maintenance-debt risk worth a one-line fix (interpolate the constants instead of spelling them out) whenever convenient, 3 is a stylistic nudge for whoever writes the eventual "outwitted" content rather than a defect in what exists today, and 4 is a scope gap to track on the backlog, not a flaw in the two pieces that do exist. The MINOR notes (6, 7, 8) are disclosure-only. Tone consistency (criterion a) and the ending-scope lock at the level of literal claims (criterion b) both pass; originality (criterion c) passes the letter of the rule with one trope-awareness note; internal mechanic consistency (criterion e) passes; Fee 1's flavor text does not over-promise (criterion f). The one real gate is criterion d, and it's specific to 4.1a alone.

## 2026-08-01 — Narrative critique: rescuable-adventurer dialogue + item flavor text, Levels 1-4 (backlog 4.6, closing the 4.4 half)

Critiqued Lorena's 2026-08-01 log entry (four new NPCs — the Latchkeeper, the Hushmantle, the Rootbound, the Longwaiter — plus eight item flavor-text entries, two per level) against the full `docs/agents/_reference/lore-premise.md` (tone, ending-scope lock, originality, UI-space rule, and all six Established Named Facts predating this batch), the GDD's Gameplay Loop section (`the-last-spellroad-design.md:33-49` — the forward-only rule and the "some adventurers prefer captivity" thread step 5 explicitly invites), and `docs/agents/warden/log.md`'s actual Level 1-4 enemy-roster entries, to check each item's enemy-grounding claim against the real data rather than taking Lorena's own "Grounding note" at face value. Ran exact character counts on every dialogue line and item description with a script rather than eyeballing length, since the UI-space rule is exactly the kind of thing that's checkable, not a vibe. Also confirmed directly against `src/` and the GDD (not from Lorena's disclosure alone) that no dialogue-box or item-pickup system exists anywhere in this codebase yet.

**BLOCKING**

None. I checked every category this dispatch names as BLOCKING-worthy — "outwitted"/"transformed" resolvability, copied published lore, a broken melancholic tone, a UI-length overflow, or a contradiction of a previously-established named fact — specifically, and none is violated as written. This is a materially different situation from the 2026-07-30 batch, not a softer read of the same one: that pass's BLOCKING finding existed because 4.1a made a literal, falsifiable claim ("matches the existing Y/N prompt shape so it drops in without needing new UI layout") against code that already existed and was checkably false at that exact call site. Nothing in this batch makes an equivalent claim — Lorena's own "Status" line discloses that none of this is wired into any UI, scene, or `src/data/` file, and I independently confirmed there is no existing dialogue-box or item entity anywhere in `src/` for any of these eight items or four NPCs to overflow against, and that the GDD itself still calls items/relics "still undefined" (`the-last-spellroad-design.md:533`). There is no render target to falsify a claim against, and no claim is made that one exists.

**MAJOR**

1. **Adversarial QA — the Latchkeeper's closing line is ambiguous against the GDD's own permanent forward-only rule, and the ambiguity isn't resolved on the page.** The line: *"If you find the way out, mage, don't come back for me out of guilt. Come back because you remembered where the door was. That's the only rescue that'll ever hold."* The GDD states the forward-only rule in absolute terms: "once a road segment is cleared, the mage cannot backtrack into it. This is a permanent rule, not a vertical-slice simplification" (`the-last-spellroad-design.md:47`) — and explicitly, the same NPC-in-the-same-spot cannot be revisited ("forward-only constrains revisiting a specific cleared segment, not reusing a character or location type"). Read literally, "come back for me" promises the mage will physically return to this exact Level-1 location to free this specific NPC — something the game's own permanent rule forecloses. A charitable reading exists and is plausible on a close read — "the only rescue that'll ever hold" could mean finishing the whole road (ending the Director's hold generally) rather than literally retracing steps into a cleared segment, which would make the line consistent rather than contradictory — but the text doesn't disambiguate between "I will walk back to Level 1 for you" and "solving the road at large is the only real rescue," and a player is at least as likely to take the first, mechanically-impossible reading as the second. Not rewriting this myself, since the charitable reading is real and defensible and I'd rather flag the seam than guess at Lorena's intent — but this should get a one-line clarifying pass before this NPC is ever extended (e.g. a return visit is scripted, or a sequel line is written) so a future piece doesn't have to resolve an ambiguity this one left open.

2. **Player psychologist — the Rootbound's third line is a length outlier with no established per-line budget to check it against, and it's the exact kind of thing that's cheap to catch now and expensive once wired.** Exact character counts (script-verified, quotation marks and trailing periods excluded): Latchkeeper's three lines run 107/151/164 chars; Hushmantle's three run 111/113/168; Longwaiter's four run 164/214/230/185. The Rootbound's third line — *"Out there I was tired in a way that had no shape and no end. In here, at least the tired has a rhythm to it. You'll think that's the road talking. Maybe it is. Some mornings I've stopped being able to tell the difference, and the not-telling feels like the first peace I've had in years."* — is 289 characters, roughly double every other single line in this batch and 26% longer than the next-longest line anywhere in it (Longwaiter's 230). No dialogue-box UI exists yet to check this against (unlike 4.1a's `flashMessage`, which did exist and did overflow), so this isn't a falsifiable render bug the way that finding was — it's an internal-consistency observation: seven other beats in the same batch settled into a ~100-230-char single-line rhythm, and this one line breaks that rhythm by a wide margin, for the beat most likely to be read as a considered emotional centerpiece (it's also the philosophical crux of the "prefers captivity" thread). If/when a dialogue UI gets built, whoever wires it should decide this line's fate deliberately (split it, trim it, or confirm the box can hold ~290 chars) rather than discovering the outlier for the first time at wiring.

**MINOR**

3. **Narrative critic — two of the eight items open with the identical word "Pulled" and share a "trophy taken from a defeated enemy, still faintly retaining a residual effect" template, a smaller-scale echo of the "mercy" tic already flagged 2026-07-30.** Bramble-Thorn Needle: "Pulled from a creeping bramble's coiled length, still faintly slow to the touch." Voidfang Tooth: "Pulled still-humming from a stalker's jaw." Both are enemy-trophy items (correctly grounded — see CLEAR finding 5) using the same opening verb and the same "removed from the creature, but a trace of its effect lingers" shape. Two uses across eight items isn't a problem by itself (same standard the 2026-07-30 entry applied to "mercy"), but it's worth naming now, before a Level 5 item reaching for a third "Pulled... still [residual]" trophy reads as a formula being filled in rather than a distinct object.

4. **Narrative critic (originality) — the "captive NPC who dispenses lore and explicitly declines rescue" shape, especially the Rootbound, sits in a recognizable trope space (bound/imprisoned NPCs who talk the protagonist out of freeing them appear across the genre, e.g. Dark Souls- and Hollow Knight-style captive environmental storytelling), even though no name, line, or specific character is copied.** Same call as the Invigilator's 2026-07-30 MINOR note: this doesn't violate the letter of the originality rule (proper nouns and specific IP, not shape), and the Rootbound's actual content — grounded in the GDD's own explicitly-invited "some adventurers prefer captivity" thread rather than borrowed from an external work — earns its place here. Flagging as trope-awareness for whoever extends this thread later, not a defect in what shipped.

**CLEAR**

5. **Systems designer — every enemy-grounded item description matches the real roster and the real archetype-to-drain-variant mapping, not an invented one.** Cross-checked against `docs/agents/warden/log.md` and the mapping confirmed in this log's own 2026-07-30 entry (finding 6: `creeping_bramble` → speed, `murmur_wisp` → mana-regen, both in `src/data/enemyRegistry.ts:18-19`): the Bramble-Thorn Needle's "still faintly slow to the touch" correctly ties to the speed-drain variant, the Murmur-Glass Vial's "murmuring drain" correctly ties to the mana-regen variant, the Reaver's Cracked Visor correctly ties to `dread_reaver` (confirmed new to Level 3 wave 2 in `warden/log.md`), and the Voidfang Tooth correctly ties to `voidfang_stalker` (confirmed new to Level 4 wave 2). No item invents a drain type, archetype, or level placement the actual data doesn't support.

6. **Business analyst — no item or dialogue beat invents a mechanic, price, or UI surface that doesn't exist, matching criterion f from the 2026-07-30 precedent.** The Root-Cellar Key ("Opens nothing on this stretch of road") stops short of implying a lock exists elsewhere for it to open; the Chalked Ledger Scrap references "Hexcoin sums" only in the abstract, inventing no specific number; none of the four NPCs offer a rescue button, a Y/N prompt, or a branch — matching Lorena's own scope note verbatim and the GDD's stated invulnerable/no-rescue-mechanic constraint (`the-last-spellroad-design.md:49`).

7. **Feasibility lead — item lengths pass the "not a paragraph" rule with real numbers, not an eyeballed judgment.** All eight item descriptions run 96-152 characters (script-counted), 1-2 sentences each, a tight and consistent band clearly distinct from a paragraph — contrast this with the Tarrywright's ~470-character, 4-sentence bestiary-entry paragraph (2026-07-30, correctly cleared under the *encyclopedia*-entry UI space, not the *item-description* one). No item in this batch risks being mistaken for the wrong UI-space category.

8. **Adversarial QA — cross-checked all four new Established Named Facts appended to `lore-premise.md` against the actual log text and found them accurate, not a rubber-stamped summary.** The Latchkeeper fact ("Fastened in place by the road's wards, not by the player"), the Hushmantle fact ("ambivalent about wanting it back"), the Rootbound fact ("explicitly does not want rescuing... handled narratively only; no rescue-choice mechanic exists this slice"), and the Longwaiter fact ("speaks obliquely... without naming the Invigilator or implying 'outwitted'/'transformed' is resolvable") all match their source dialogue exactly, with no overstatement or omission introduced in the compressed fact-file version.

**Overall verdict:** no BLOCKING findings — a genuine result, not a softened one, since I specifically re-ran the same checks that caught 4.1a's render bug and Fee 1/Fee 2's numeric drift, and this batch simply doesn't make the kind of falsifiable claim those findings depended on. The two MAJOR notes are real but neither is gating: (1) is a defensible-but-ambiguous line worth a one-line clarifying pass whenever this NPC is next touched, not before this closes, and (2) is a length outlier worth deciding on deliberately at whatever point a dialogue UI actually gets built, not a defect in prose that isn't rendered anywhere yet. The two MINOR notes (3, 4) are disclosure-only, tracked for future passes the same way the 2026-07-30 entry tracked its own MINOR notes. Tone consistency and the ending-scope lock both pass at the level of literal claims across all four NPCs; originality passes the letter of the rule with one trope-awareness note; the UI-length rule passes for both the items (checked against real character counts) and the dialogue (checked against the batch's own internal rhythm, with one outlier flagged as a follow-up, not a violation); no previously-established named fact is contradicted, and the four newly-appended facts accurately reflect their source text. **Closes the 4.4 half of backlog 4.6** — 4.1a/4.1b/4.2/4.5 cleared their gate 2026-07-30, and 4.4 clears it here, with no outstanding BLOCKING debt on either half.

## 2026-08-01 (2) — Critique of backlog 0.2/0.3/0.5's implementation (branch `ana-checkin-0.2-0.3-0.5-decisions`, uncommitted at review time)

Read the actual diff against `HEAD` rather than trusting the dispatch's own description, and independently re-derived every claimed number instead of spot-checking it. Ran `docker-compose run --rm game npm run typecheck`, `npm test` (89/89), and `npm run build` myself — all clean against the working tree as it stood.

**BLOCKING**

1. **Business analyst / Systems designer / Adversarial QA (joint) — 0.2's checkpoint-reset, combined with forward-only progression, makes Fee 2 permanently unaffordable for any death in Level 4 or later.** `hexcoin-template.md`'s claim that a level's own kill count matches "the same 20-70 Hexcoin-class estimate" the fees were priced against is false for Level 4 specifically: I counted every enemy in `level-1.json` through `level-4.json` directly (16/21/21/25) — the 20-70 estimate was explicitly derived from *multiple* levels' income on the approach to the boss, not one level's own total. Since `handleDeath()` called `resetExpedition()` (zero the balance) unconditionally and forward-only means a cleared level's enemies can never be re-earned, any death in Level 4 (25 max Hexcoin, below Fee 2's 30 cost) or later permanently caps every future attempt at that boss fight below the fee — deterministically, not probabilistically, and for the single most plausible late-run death scenario. This is a new failure mode the prior placeholder code (which never called `resetExpedition()` and always respawned at absolute wave 0) could not produce; today's own fix introduced it while asserting the opposite in its own write-up.

**MINOR**

2. **Narrative critic — Lorena's log states her replacement flash line is 59 characters; it's actually 57 (script-counted).** Harmless either way (both are comfortably under the ~90-100-char budget this log established 2026-07-30), but the one number she chose to self-verify was slightly off.

3. **Systems designer — the 0.5 residual's "self-limiting" claim is real but softer than a first read suggests: a player can equip a second, already-non-Novice "decoy" spell to dilute the death-roll's odds of hitting the spell being ground**, rather than accepting a guaranteed hit. Doesn't contradict anything written (the template already disclaims an airtight bound), but worth naming as a concrete instance of the gap already flagged as needing "a probabilistic look... if it matters later."

**CLEAR**

4. **Systems designer — the 24-kills/tier arithmetic is correct, independently re-derived from the real wave files, not fudged.** Level counts (16/21/21/25), the 48/25≈1.92x margin against Level 4, and the 48/83≈58% four-level reachability figure all check out exactly as claimed.

5. **Adversarial QA — the respawn-index lookup correctly resolves to the boss's phase 0 for a death at any of its three phases**, since `this.waves` concatenates level files in order and `findIndex` returns the first match, which is always phase 0 for `level: 5`. Hand-traced all three death timings.

6. **Feasibility lead — `HexcoinSystem`'s fight-snapshot/recovery-count bookkeeping stays sane across every death timing traced**, including mid-boss-fight deaths — the respawn always routes back through the `wave_index === 0` branch, so `startBossFight()` and `bossMaxRecoveries` are always recomputed cleanly on the next attempt.

7. **Narrative critic — Lorena's replacement flash line fits the established Director/Invigilator "testing, not cruelty" register**, keeps the mechanical clause ("no Mastery lost") legible ahead of the flavor clause, and is not cryptic to a player reading it in the moment.

8. **Business analyst — the new flash-message text invents no number, mechanic, or fee that could drift**, unlike the 2026-07-30 Fee-1/Fee-2 draft this log already caught hardcoding prose numbers.

**Overall verdict:** one genuine BLOCKING finding, found by direct arithmetic against the resolution's own written claim — 0.2's checkpoint design was sound for the over-earning direction it was built to close, but its own doc entry asserted an equivalence that's false specifically for Level 4, producing a deterministic Fee-2 lockout. Everything else — the 24-kills/tier arithmetic, the phase-0 respawn lookup, the fight-snapshot bookkeeping, and Lorena's tone — checked out cleanly. **Reported back to Ana same session; fixed with a floor mechanism (`markLevelStart`/`rollbackToLevelStart`) rather than a revert, re-verified clean (typecheck/test/build), before this bundle closed `shipped-and-validated`** — see `docs/agents/ana/log.md`, 2026-08-01 (3) for the fix itself.

## 2026-08-02 (6) — Critique of Loomwright's #52/#53/#54 fix bundle (branch `loomwright/fix-freeze-lane-mana-52-53-54`, uncommitted)

Read the actual `git diff` against the working tree (nothing committed), `gh issue view` on all three issues, and `docs/agents/loomwright/log.md` in full (needed the 2026-08-02 entries on #47/#48/#49 specifically — this bundle touches the exact same `WaveSession` generation/phase machinery and the exact same `startPhaseBreak` function those entries already flagged one cross-fix interaction in). Ran `docker-compose run --rm game npm run typecheck`, `npm test` (59/59, including the 3 new `spellCost.test.ts` cases), and `npm run build` myself — all clean. Brought up `docker-compose up -d game`, confirmed `http://localhost:5173` serves (200, correct `index.html`) and loads with zero console errors beyond Phaser's own banner log, and screenshotted the running game to visually confirm the widened lane actually renders edge-to-edge with the tile art (no visible seam/gap), then `docker-compose down`. Independently re-derived every arithmetic claim in the diff's own comments (spawn-x-to-wall-margin invariant, kiting-band-vs-lane-width independence) rather than trusting them, and hand-traced the full keyboard-listener lifecycle of `startPhaseBreak` against a death-during-the-prompt scenario rather than only checking the literal reported repro.

**BLOCKING**

1. **Systems designer / Adversarial QA (joint) — the #52 fix arms `keydown-Y`/`keydown-N` `.once` listeners per `startPhaseBreak` call, but `handleDeath()` never removes a still-pending phase-break's listeners, and `resolve()`'s only guard is a bare phase-string check with no generation/token check of its own — so a death that interrupts an unresolved phase-break, followed by a boss retry reaching a phase-break again, silently reproduces the exact freeze #52 was dispatched to fix, via a different path.** Concrete trace, grounded in the actual code (`SpellroadScene.ts:718-778`, `HealthSystem.ts:24-33`, `waveSession.ts`): every phase in `boss-1.json` includes a `hexbow_skirmisher` (ranged), and `onRangedFire`'s 450ms delayed-damage callback (`SpellroadScene.ts:797-821`) is gated only on `session.isCurrent(fireGeneration)` — a value `beginPhaseChoice()` never bumps, by design (comment at `waveSession.ts:73-77`: phase-break resolution timers are supposed to survive it). So a shot fired just before the phase's last enemy dies can land *during* the following `awaiting-phase-choice` window and, on a cumulative-damage multi-phase fight, kill the player right there — `HealthSystem.applyDamage` calls `onDeath()` (`handleDeath()`) synchronously the instant HP hits 0 (`HealthSystem.ts:30-32`). `handleDeath()` calls `session.beginDeath()` (bumps generation, phase → `dead`) but never touches `this.input.keyboard` at all — the `once("keydown-Y", onY)` (and `once("keydown-N", onN)` if `canPay` was true) armed by the interrupted `startPhaseBreak` call stay registered on the shared keyboard plugin. The death restart replays the boss from phase 0 (`handleDeath`'s `levelStartIndex` lookup always resolves to the boss's own phase 0 for a level-5 death, confirmed in this log's 2026-08-01(2) entry, finding 5 — still true, unchanged by this diff); when the retried attempt reaches the *same* phase-break again, `startPhaseBreak` arms a **second**, independent pair of `once` listeners for the same two events. Both the stale and fresh listeners fire on a single keypress (Phaser's `once` only deregisters the individual listener that fired — it does not prevent other listeners on the same event from also being invoked in the same emit). The stale closure's `resolve()` runs first (registration order): its guard (`session.phase !== "awaiting-phase-choice"`) *passes*, because the check only reads current session state, never compares against its own closed-over `phaseGeneration` — so it unconditionally executes `session.beginAdvance()` (flipping the *current, legitimate* phase-break's phase to `advancing`), and, since it reads `this.bossMaxRecoveries`/`this.hexcoin` live rather than from stale closures, can silently pay a real, current Hexcoin fee and restore real HP the player never consciously chose for *this* attempt. Only its own trailing `time.delayedCall`'s `isCurrent(phaseGeneration)` check (the *one* place this function checks a token) correctly no-ops, so no double `startWave` fires from the stale side — but the damage is already done: when the fresh listener's `resolve()` runs immediately after, its own identical guard now reads `session.phase !== "awaiting-phase-choice"` as **true** (the stale call already flipped it), so it returns immediately, and the real, intended advance is never scheduled at all. The session is left permanently parked in `advancing` with no pending callback anywhere that will ever move it out — a dead stop, mechanically identical to the original #52 freeze, just reached via a boss-retry-after-mid-prompt-death instead of the literal `canPay === false` repro. This is not a rare corner case for this specific boss: it's a 3-phase, 2-phase-break fight where every phase carries a ranged attacker, `bossMaxRecoveries` caps at 1 (`totalPhases - 2 = 1`), and dying mid-recovery-prompt then retrying is an entirely ordinary developer-playtest flow, not a contrived sequence. **Fix direction, not prescribing the exact code:** `resolve()` needs to check the *token* (e.g., `session.isCurrent(phaseGeneration)`), not just the phase string, before doing anything — including its side effects (Hexcoin spend, HP restore) — and/or `handleDeath()` needs to explicitly `.off()` both `keydown-Y`/`keydown-N` (using the actual listener references, which would require hoisting `onY`/`onN` or an equivalent handle) whenever it interrupts a still-`awaiting-phase-choice` state, so a later, unrelated phase-break never inherits a stale, still-registered listener.

**MAJOR**

2. **Business analyst — the same bug's silent side effect is an unconsented Hexcoin spend against this specific boss's tightest possible economy.** When the stale listener's `resolve(true)` fires, it can pay `FEE_PHASE_RECOVERY` (30 Hexcoin) and restore 10% HP using the *current* attempt's live balance/cap — a transaction the player never knowingly authorized for the attempt they're actually in, executed as a side effect of a bug, not a deliberate choice. Against `boss-1.json`'s own `bossMaxRecoveries` of exactly 1 for this fight (`totalPhases − 2 = 1`), this can consume the *only* recovery this boss ever offers before the player has knowingly used it once, then leave them softlocked on top of that — a worse outcome for the exact economy Pato/Heckler previously fought hard to keep bounded and intentional (see this log's 2026-07-21/2026-08-01(2) entries).

3. **Player psychologist — if this fires, it reproduces the precise felt-experience #52 was written to eliminate, at the identical UI moment, and adds a layer the original bug didn't have.** The player sees a prompt promising a specific key does something, presses it, and nothing visibly happens — the exact "message and reality disagree" complaint from the original bug report. Worse here: their Hexcoin may have silently dropped and HP silently risen a moment before the freeze, with no flash message tied to the keypress they actually made (the `flashMessage(\`Recovered ...\`)` call, if it fires, is attributable to the stale resolve, not something the player will connect to "I just pressed Y and got nothing").

4. **Feasibility lead — no test in the current 59-case suite touches `startPhaseBreak`'s keyboard-listener lifecycle at all, and there's no obvious pure-logic seam to extract one from.** Every other piece of `waveSession`'s generation/phase logic has a Vitest seam (`waveSession.test.ts`, 14 cases) precisely because it was pulled into a Phaser-free module; the `.once("keydown-Y"/"keydown-N", ...)` registration-and-cleanup dance lives entirely inside `SpellroadScene.ts` itself, coupled to `this.input.keyboard`, so nothing in the existing test architecture would have caught this, and it's not obvious how to add a check without either a Phaser-keyboard-mock harness or restructuring `startPhaseBreak` to expose its listener bookkeeping as pure state (e.g., "does this closure's token match the session's current one" as an extractable predicate, mirroring the `isCurrent`/`shouldAutoAdvance` pattern already used everywhere else in this same file).

**MINOR**

None beyond what's already tracked. The pre-existing, already-disclosed #47/#48 decline-path race (`loomwright/log.md`, 2026-08-02 (5): a same-life phase advance inside 250ms can void an in-flight ranged shot) sits in the same function this bundle touches but is untouched by this diff and remains correctly flagged rather than fixed — not re-raised here as a new finding.

**CLEAR (probes specifically asked for, checked and passed)**

5. **Systems designer — `ENEMY_SPAWN_X = 910` is arithmetically sound against the new 960px width, independently re-derived, not taken on the diff's own word.** `WaveLoader.spawnWave`'s jitter is ±40px on x (`Phaser.Math.Between(-40, 40)`), so max spawn x = 950; `WALL_SLIDE_MARGIN` (`Enemy.ts`) is 50, so the wall-slide trigger threshold is `960 − 50 = 910` — a max-jitter spawn (950) sits 10px past that threshold, identical in magnitude to the pre-fix arrangement (`870 − 50 = 820`, old max-jitter spawn 860, also 40px past/10px of clearance from the wall by the same math) — not a new source of spawn-time wall-pinning, and confirmed (per the 2026-07-25 critique already on record) that the mage/enemy spawn separation is still large enough that an enemy's first move is always "approach," never "retreat," so this proximity is inert at spawn regardless.

6. **Systems designer — the kiting-band-independence argument for leaving `RANGED_PREFERRED_RANGE`/`DEBUFFER_PREFERRED_RANGE`/`WALL_SLIDE_MARGIN` untouched holds up:** these are player-relative distance bands (240±20 / 150±20, a fixed 50px gap) with no dependency on lane width in their own definition; a wider lane can only reduce how often either wall is reached, never compress the bands toward each other. Same reasoning this log already independently accepted for the `ROAD_HEIGHT` 160→220→280 widenings.

7. **Feasibility lead — the widened `ROAD_WIDTH` (780→960, now edge-to-edge with the 960px canvas) does not break any other geometry I could find, verified by grep and live screenshot, not just by reading the diff's own comment.** Grepped `src/` for any other reference to the old `780`/`870`/`820` literals — none exist outside the two files the diff already touches. `computeTilemapOffset`'s horizontal centering now resolves to exactly `x = 0` for every level (map width 960 = canvas width 960), matching `ROAD_LEFT = 0` exactly — no seam. The preview-clip mask, HUD text positions, and mage start position are all unaffected (checked each explicitly). Brought the dev server up and screenshotted it directly: the tile art now visibly spans the full canvas width with no bare strip on either side, confirming the fix's actual visual claim rather than trusting the comment.

8. **Systems designer — `SpellCaster.canAffordCast` cannot drift from `tryCast`'s own mana check, and there is no race between the pre-check and the eventual spend.** Both read `computeCastManaCost` (the same pure function, shared, not duplicated) against the *same* live `ManaSystem`/`MasterySystem` instances. Mana can only *increase* between the hotbar-press pre-check and the eventual `confirmCast` (passive regen; nothing else decrements it except the very `tryCast` call this pre-check guards), so a "sufficient at press-time, insufficient at confirm-time" race is structurally impossible, not just untested. Mastery tier likewise cannot change out from under a pending preview (`recordLandedCast` for a given spell id only fires from that same spell's own confirmed kill, and only one spell can be mid-preview at a time). Checked all 12 shipped spells in `spells.json` for a `master_discount` value (none missing) — the shared formula has no unhandled-field gap either. `tryCast`'s only two rejection paths (cooldown, mana) are now both pre-checked in `handleHotbarPress` in the same order tryCast itself checks them; no third rejection path exists in `tryCast` that the pre-check misses.

**Overall verdict: this bundle does NOT clear the gate.** #53 (lane widen) and #54 (mana pre-check) are both clean, verified by direct arithmetic, grep, and a live dev-server screenshot rather than by trusting the diff's own comments — no notes beyond disclosure-worthy MINORs that don't rise to blocking. #52, however, is fixed for its own literal repro but reintroduces an equivalent freeze through a gap the fix itself creates: `resolve()`'s guard checks phase, never the generation token, and `handleDeath()` never cleans up a phase-break's still-armed keyboard listeners — an ordinary die-mid-prompt-then-retry sequence against this exact boss (every phase has a ranged attacker; the fight is short enough that retrying into the same phase-break is the default flow, not a contrived one) reproduces the original bug's user-visible symptom, with an added silent Hexcoin/HP side effect on top. Hand this back to Loomwright with finding 1 above — the fix needs `resolve()` to check `session.isCurrent(phaseGeneration)` before doing anything (not just before its final `startWave` call), and/or `handleDeath()` needs to actively deregister any still-pending phase-break listeners it interrupts.

## 2026-08-02 (7) — Critique of the Loomwright/Tilesmith #55+#56 hotbar-redesign + icon bundle (branch `loomwright-tilesmith/hotbar-icons-55-56`, uncommitted)

Read the actual `git diff` (nothing committed) covering `SpellCaster.ts`, `SpellroadScene.ts`, `spellCost.ts`/`.test.ts`, plus the new `hotbarLayout.ts`/`.test.ts` and `spellIcons.ts`, and the 4 new PNGs under `public/assets/spell-icons/`. Read `gh issue view 55`/`56` and `docs/agents/tilesmith/log.md`'s new entry in full. Ran `docker-compose run --rm game npm run typecheck`, `npm test` (9 files, 76 tests, all passing, including the new `hotbarLayout.test.ts`'s 10 cases and `spellCost.test.ts`'s 3 new `computeCastCooldownMs` cases), and `npm run build` myself — all clean. Brought up `docker-compose up -d game`, confirmed it serves with zero console errors, and drove the actual running game (armed a spell via a real keydown, cast it, watched it go on cooldown) rather than only screenshotting the idle state — then `docker-compose down`. Independently re-fetched two of Tilesmith's own cited art-sourcing sources rather than trusting the log's summary of them.

**BLOCKING**

1. **Adversarial QA / Systems designer (joint) — the per-slot `[shape/weight]` label text overflows past its own slot's right border into the neighboring slot for `thunder_dome` (`[circle/standard]`), confirmed by direct magnified screenshot of the running game, not inferred from the diff.** `HOTBAR_TEXT_PADDING` (6) + `HOTBAR_ICON_SIZE` (40) + `HOTBAR_ICON_PADDING*2` (16) = 62px text-left offset inside a `slotWidth` of `(960 − 8×7)/6 ≈ 150.7px` (`computeHotbarSlotRects`, `hotbarLayout.ts`), leaving roughly 88px of text width per slot — a budget that was sized against the pre-icon layout and never re-checked against the narrower post-icon one. At 11px monospace, `[cone/standard]` (flame_sweep, 15 characters incl. brackets) fits inside its slot's border with visible margin; `[circle/standard]` (thunder_dome, 17 characters) does not — the closing `d]` renders on top of and past the green border line, bleeding into slot 6's box. This is the *default* 6-spell loadout (`DEFAULT_LOADOUT_IDS`), not an edge case — it happens on every single load of the game, in exactly the region Tilesmith's icon change narrowed. It's also a direct recreation of the symptom class issue #55 exists to fix ("the hotbar text gets mixed and difficult to read"), just relocated from a vertical clip to a horizontal cross-slot bleed. Screenshots taken at `scale(6) translate(-760px,-415px)` CSS zoom on the live canvas (raster-faithful, not an artifact of the zoom) show the overlap directly. **Fix direction:** either shrink the font for this one text block, wrap the shape/weight tag onto its own narrower sub-line, truncate/abbreviate it (e.g. "std"/"lgt"/"hvy"), or reclaim a few px from the icon padding — not prescribing which, but this needs to be re-balanced against the actual post-icon text budget, which nobody did.

**MAJOR**

2. **Business analyst / spec-compliance — issue #56's own Decision text says "Commission real per-spell icon art (over a cheaper element-color-only placeholder)," but what shipped is per-*element* icons (4 total, each reused across up to 3 spells), a middle ground between the option the ticket explicitly rejected and the one it explicitly asked for.** Tilesmith's log is transparent about this substitution and reasons it through in detail (shape is already shown via cast-preview geometry + the existing `[shape/weight]` text tag, so element-level closes the actual reported gap), and — independently verified against `src/data/spells/spells.json` — it's arithmetically sufficient: no two of the 12 shipped spells share the same `(element, shape, weight)` triple, so every previously-ambiguous same-shape/same-weight pair (not just the reported arc_lance/stone_spike one) is now visually distinguishable by icon alone. But "sufficient in practice" isn't the same as "what was decided" — the ticket's own `/grill-with-docs` decision explicitly named per-spell commissioning as the chosen (more expensive) option over element-only, and this bundle quietly delivered the cheaper option instead without that substitution going back through the same decision process (or at minimum an explicit flag to Ana/the developer that the scope was narrowed from what the ticket says was decided).

**MINOR**

3. Mastery tier is no longer shown anywhere in the hotbar (previously `(tier, cdLabel)` in the old 7-line block) — called out honestly in-code as a legibility trade-off (`updateHotbar`'s doc comment), not silent data loss, and Mastery-up is still surfaced via the existing flash message at the moment it changes. Still, a player now has zero at-a-glance way to check a spell's current tier mid-run. Worth a developer nod, not a blocker.
4. `CooldownDisplay.fractionReady` (`hotbarLayout.ts`) has direct unit-test coverage (`hotbarLayout.test.ts`) but zero live callers — `SpellroadScene.ts` only ever reads `.isReady`/`.label`. Documented up front as forward-looking API for a future cooldown-wipe visual, so not a real defect, just flagging the dead runtime surface so it doesn't get mistaken for a already-wired feature.

**CLEAR (probes specifically asked for, checked and passed)**

5. **All 6 hotbar slots render fully within the 540px-tall canvas, verified live, not just via the diff's own arithmetic comment.** `docker-compose up -d game` + screenshot at `http://localhost:5173` shows the full row (`top = 130+280+14 = 424`, `bottom = 424+96 = 520`) sitting comfortably above the 540px canvas floor, with the numbers matching `SpellroadScene.ts`'s own fits-within-canvas comment. This is the exact re-check issue #55's own acceptance bar demands.
6. **The reported pair — arc_lance vs stone_spike — is now visibly different, confirmed by screenshot of the actual running game, not by reading `spellIcons.ts`'s doc comment.** arc_lance (slot 1) renders a yellow lightning-bolt icon; stone_spike (slot 4) renders a brown rock-cluster icon. Same `[line/light]` tag, same cast-preview geometry, now clearly distinguished at a glance — the literal issue #56 symptom is gone.
7. **Icon/hotbar integration has no texture-drift, stale-icon, or z-order bug across ready/armed/cooldown states — verified by actually driving the game, not just reading the code.** Dispatched a real `keydown-1` to arm arc_lance (border correctly switched to the cream `HOTBAR_BORDER_ARMED_COLOR`, preview beam + auto-aim ring rendered correctly), then a real pointerdown to cast it (mana dropped 100→90, slot flipped to the muted `HOTBAR_BORDER_COOLDOWN_COLOR` with a live `1.9s`→`2.0s`-style countdown). The lightning-bolt icon stayed correctly visible and correctly textured through all three states — `icon.setTexture(iconKeyForSpell(spell))` is keyed only on equipped-spell identity, never on cooldown/armed state, so it structurally can't go stale, and there's no depth/z-order clash since `hotbarGraphics` (background/border) → `hotbarSlotIcons` → `hotbarSlotTexts` are added to the display list in that order every time.
8. **Loadout is fixed at scene-create time (`DEFAULT_LOADOUT_IDS`, no runtime loadout-swap or `scene.restart()` path exists anywhere in this codebase), so the "icon never repositioned/resized when slot rects are recomputed" risk flagged in the dispatch brief doesn't apply — checked, not assumed.** `hotbarSlotRects` is computed once in `createHud()` and never recomputed; `hotbarSlotIcons` are created once against those same rects and never need repositioning within this vertical slice's scope.
9. **The art-sourcing story for the 4 hand-authored icons is a genuine investigation, not a token effort — independently re-verified two of Tilesmith's own cited sources rather than trusting the log summary.** Re-fetched `kenney.nl/assets/game-icons` directly: it is in fact a 105-icon CC0 pack of gamepad/joystick/UI-prompt icons, not elemental iconography — Tilesmith's characterization is accurate. Re-fetched `opengameart.org/content/12-elemental-type-symbolsicons` directly: it is in fact CC-BY 4.0 ("you must give credit to me... please just link this page" — Tilesmith's log quotes this near-verbatim) despite covering the right element set — a real, correctly-read disqualifying reason, not an assumed one. This clears the bar this probe exists to catch: Tilesmith named specific packs, read specific pages, and found specific, checkable disqualifying facts before falling back to hand-authoring, satisfying the Art Sourcing Contract's step 1→4 exhaustion requirement.
10. **`computeCastCooldownMs` (mirroring #54's `computeCastManaCost`) introduces no drift between what `SpellCaster.tryCast` actually arms the cooldown timer with and what the hotbar displays as the total.** Diffed the old inline ternary in `tryCast` against the new extracted function — byte-for-byte equivalent logic (same `WEIGHT_CLASS` base lookup, same `masteryTier === "master" && spell.master_discount === "cooldown"` gate). The 3 new `spellCost.test.ts` cases correctly exercise all three relevant combinations (base at novice/adept, -10% at master+cooldown-discount, no discount at master+cost-discount).
11. **No new interaction with the already-disclosed #47/#48 phase-decline race or the #52/#53/#54 bundle underneath this branch — checked by reading the diff against those systems, not assumed absent.** This bundle touches only `SpellCaster.cooldownDurationMs`/`computeCastCooldownMs` (a pure read-only mirror of the already-shipped mana-cost pattern) and the hotbar's own rendering code in `SpellroadScene.ts`. Grepped the diff for any touch to `session.*`, `phase`, `generation`, or keyboard-listener registration — none. `handleHotbarPress` is byte-for-byte unchanged beyond what #54 already shipped. The #47/#48 decline-path race and the #52-adjacent `startPhaseBreak` listener-lifecycle gap both live entirely in code this diff never opens.

**Overall verdict: this bundle does NOT clear the gate.** The hotbar redesign (#55) and icon integration (#56) both land the specific, literal fixes their tickets asked for — verified live, not just read off the diff: all 6 slots render on-canvas, and arc_lance/stone_spike are now visually distinct. But the icon change narrows the hotbar's text budget without re-checking it against the row's actual longest label, and it reintroduces a text-overlap defect (thunder_dome's `[circle/standard]` bleeding into slot 6) in the *default* loadout, every load — a fresh instance of exactly the "hard to read hotbar" class of bug #55 was dispatched to eliminate. That, plus the undisclosed-to-Ana narrowing of #56's own decided scope (per-element instead of the ticket's stated per-spell commissioning), are both real enough to send back before this ships. Neither is architecturally deep — a font/width rebalance and a one-line scope disclosure — but both are concrete, verified, and unresolved as this diff currently stands.

## 2026-08-02 (8) — Critique of Loomwright/Lorena's #57/#58 HUD-clarity bundle (branch `loomwright-lorena/hud-clarity-57-58`, uncommitted)

**Ana's numbering note:** this branch is a sibling of `loomwright-tilesmith/hotbar-icons-55-56` (both stacked on `loomwright/fix-freeze-lane-mana-52-53-54`, not on each other), so this branch's copy of this log only contained entry (6) when Heckler wrote this entry — the entry above (originally the other branch's own "(7)") only appears in this merged sequence now that PR #61 was merged into the shared base and this branch (PR #62) was reconciled against it. Renumbered Heckler's own entry from (7) to (8) at the time to avoid a same-numbered collision that later did occur once the branches merged; no content changed, only the heading number.

Read the actual `git diff` (nothing committed) across all five touched/added files, `gh issue view` on #57 and #58, `docs/agents/_reference/lore-premise.md` in full, and grepped `docs/agents/*/log.md` and `docs/agents/ana/backlog.md` for every "Tarrywright" occurrence to trace the naming decision's actual history rather than trusting the diff's own summary of it. Ran `docker-compose run --rm game npm run typecheck`, `npm test` (75/75, including the 11 new `debuffDisplay.test.ts` cases), and `npm run build` myself — all clean. Brought up `docker-compose up -d game`, confirmed it serves, screenshotted the running game (default no-debuff state) to check the new top-right HUD's real on-screen placement, then attempted to fight through to a live Debuffer pulse to check the debuff HUD line for real — same practical wall Loomwright already reported (see MINOR 8 below) — then `docker-compose down`. Read `DebuffSystem.ts`, `Enemy.ts`, and `enemyStatusOverlay.ts` in full (not just the diff hunks) to check the "no duration field" and "casing consistency" claims against the actual classes rather than the diff's comments about them.

**BLOCKING**

None.

**MAJOR**

1. **Player psychologist / Systems designer — the enemy's own floating name label can render on top of the brand-new top-right HUD, in ordinary play, not just a contrived edge case.** Neither the new `levelWaveText`/`debuffText` (nor the pre-existing `hudText`/`hotbarText`) ever calls `setDepth` — the codebase already has the convention for layering (`BACKGROUND_DEPTH = -100`, `TILE_LAYER_DEPTH = -50`, `SpellroadScene.ts:95-96`) but it was never extended to the UI text layer. Phaser resolves equal-depth (default 0) ties by display-list order, and every `Enemy` (with its `nameLabel`/`statusBar`, `Enemy.ts:145-151`) is constructed — and added to the display list — after `createHud()` already ran, so an enemy's overlay paints over the HUD wherever the two overlap on screen, not the other way around. That overlap is reachable by ordinary player movement, no bug required: the mage is clamped to `LANE_RECT` (`ROAD_TOP=130`..410, `ROAD_LEFT=0`..960 — flush with the canvas edges, `SpellroadScene.ts:40,54-55,86`) and enemies are clamped to the identical rect (`Enemy.ts:141`), so a player who simply hugs the top of the lane pulls whatever enemy is closing on them (Debuffer included) up to the same region. `STATUS_LABEL_OFFSET_Y = -28` (`Enemy.ts:98`) puts that enemy's name label at `y ≈ 92–102` when the enemy itself is at the lane's literal top edge (`y = 130`) — squarely inside `levelWaveText`'s box (`x 750–944, y 16–62`) and, if a debuff is active, `debuffText`'s occupied width too (right-aligned from `x = 944`, up to roughly 350px wide for the full sentence). Independently confirmed this isn't leaning on the wall-slide sign bug this log flagged 2026-07-25 (finding 1, that entry) — re-read current `Enemy.ts:245-262` and that bug is already fixed (the perpendicular-sign pick is now correct for top/bottom walls), so this doesn't need a live bug at all, just a player choosing to fight near the top-right corner. Issue #58's whole stated goal was "large, high-contrast... never miss it"; an enemy label painting over exactly that box during the moment a player is most likely to be watching a nearby threat undercuts that goal. Fix direction: give the HUD text objects (and/or the enemy overlay) an explicit `setDepth` so the UI layer always wins, matching the depth-layering convention this file already established for background/tiles.

2. **Narrative critic / Player psychologist — "(until wave clears)" is timing-accurate but omits the one thing a player will actually wonder: killing the Debuffer that applied a stack does not clear it.** Verified via `removeEnemy` (`SpellroadScene.ts:926-929`): it only filters the enemy out of `this.enemies` and calls `enemy.destroy()` — nothing touches `this.debuff` at all, and `DebuffSystem` has no per-source tracking, only two aggregate counters zeroed exclusively by `clear()` (called only from `startWave()` and the death handler, per MINOR 6 below). So a player who kills the visible Tarrywright, then keeps reading `"The Tarrywright pulse active (until wave clears)"` on a HUD line with no Tarrywright left on screen, has a completely reasonable, wrong read: "this is a stale/buggy leftover," not the actually-intended "the drain outlives its source until the wave itself ends." That's a milder recurrence of the exact "reads as broken, not intentional" complaint issue #57 was filed to fix — the fix solves the "nothing is happening" half but reopens a smaller version of the same complaint once the source enemy is gone. Cheap fix: one clause, e.g. `"(persists after this enemy dies, until wave clears)"`.

**MINOR**

3. **Systems designer — "wave clears" is accurate against the data model but the only in-fiction language the player actually sees for a boss fight's per-phase transition never says "wave."** `boss-1.json` puts a debuffer in every phase of the game's one boss fight (`creeping_bramble` in Phase 1 and Phase 3, `murmur_wisp` in Phase 2). `startWave()` calls `this.debuff.clear()` unconditionally on every call, phase transitions included (`SpellroadScene.ts:745`), while the same function explicitly does *not* reset HP across boss phases ("the boss/trial damage-threat budget is cumulative across phases," `SpellroadScene.ts:724-727`) — so during the Director Trial the debuff actually clears *more* often (every phase) than HP does (never, until the fight ends or death). That's fine mechanically (and a low-stakes direction of surprise — the debuff clears sooner than a player might expect, not later) but the only banner text shown for that same transition is `"Director Trial — Phase 2"` (`flashMessage`, line 734) — the word "wave" never appears anywhere the player can see it during a boss fight. Not a bug, just a wording seam worth a one-word patch ("next phase/wave clears") if this line gets touched again.

4. **UI/design — `debuffText`'s visual treatment (no background panel, 13px monospace) doesn't match `levelWaveText`'s (opaque `#1c1330` panel, bold 28px Georgia serif) directly above it in the same fixed column**, despite the code comment describing them as one persistent group. Arguably correct on purpose — secondary info shouldn't compete with the primary readout — but if a later pass wants the two to actually read as one HUD block, they'll need a shared panel treatment; noting it so it's a deliberate choice rather than an overlooked one.

**CLEAR (probes specifically asked for, checked and passed)**

5. **`computeDebuffMagnitude`'s mana-regen arithmetic is a verified exact mirror of `DebuffSystem.effectiveManaRegen`, not just claimed to be.** Read both side by side: `debuffDisplay.ts:45-46` (`baseManaRegenPerSec - manaRegenStacks * MANA_REGEN_DRAIN_PER_APPLICATION`, floored at `MANA_REGEN_FLOOR`) against `DebuffSystem.ts:48-51` (identical expression, identical imported constants, no duplicated literals anywhere to drift). No gap.

6. **"No duration field" is true, verified by reading the class, not taken on faith.** `DebuffSystem.ts` has exactly one state-clearing method, `clear()` (lines 26-29), zeroing both stack counters — no timer, no per-stack expiry, no third field anywhere in the class. Grepped `SpellroadScene.ts` for every call site: exactly two — `startWave()` (`:745`, every wave *and* every boss phase transition) and the death handler (`:1051`). Loomwright's claim holds exactly as stated.

7. **"The Tarrywright" casing is structurally guaranteed consistent, and doesn't clash with anything a player can actually see.** Both real render sites — `Enemy.ts:145`'s `nameLabel` and the new `debuffText` line via `formatDebuffHudLines` — call the identical `archetypeDisplayName("debuffer")`, a single source of truth; they cannot independently drift even under a future edit. `lore-premise.md` itself already bolds the fact as `**The Tarrywright**` (capital "The") in its own Established-Named-Facts heading — the lowercase `"the Tarrywright"` mid-sentence form only appears in agent-only prose (Lorena's log, lore-premise's body text, Ana's log), never anything player-facing; grepped `src/` for "bestiary"/"encyclopedia" and found neither, confirming (per Ana's own log, 2026-08-02 dispatch entry) no in-game surface displays that prose verbatim yet. The two casings can't both be visible to a player in the same session.

8. **Live-verification attempt on the debuff HUD line: independently hit the same practical wall Loomwright already disclosed, rather than assuming it away.** Tried WASD movement and hotbar-select-plus-click casting against the live dev server to reach a Debuffer pulse; across repeated attempts (40+ key presses, a targeted cast click on a visible Melee enemy) the mage sprite never moved, mana never decremented (the cast didn't register), and the enemy count/wave clock didn't advance between screenshots — consistent with the same browser-pane input/frame-rate throttling Loomwright reported. Agreeing plainly rather than rubber-stamping: this specific piece (live debuff-pulse HUD rendering under real input) needs an actual developer playtest. What this pass *could* verify live — the default/no-debuff-active top-right HUD's placement and legibility — is covered by finding 9.

9. **Level/Wave HUD placement (issue #58's literal ask) confirmed by a live screenshot, not just source-reading.** In the default state, the new top-right box is large, high-contrast, opaque-paneled, sits entirely above the lane (`levelWaveText` spans `y=16` to `~62`; `ROAD_TOP=130`), and visibly doesn't collide with the top-left title/stat block or the below-lane hotbar block. The ticket's literal ask is met in the common case — see MAJOR 1 for the case (enemy near the lane's top-right corner) where it isn't.

**Overall verdict: this bundle does NOT cleanly clear the gate.** The engineering fundamentals are solid — typecheck/tests/build all pass, `computeDebuffMagnitude` is a verified exact mirror of `DebuffSystem`'s real formula with no drift risk, the "no duration field" claim is true, and "The Tarrywright" casing is structurally guaranteed consistent via a single shared function with no player-visible clash against the lore doc. But both MAJOR findings above go directly to what these two tickets were actually filed to fix: #58 wanted a HUD element that's impossible to miss, and an ordinary top-of-lane fight can paint an enemy's own name label right over it (finding 1, a missing `setDepth` on the UI layer against an existing but never-extended depth convention); #57 wanted the Debuffer encounter to read as intentional rather than broken, and it still can, in the specific and unremarkable case of killing the Debuffer before its wave ends (finding 2, a one-clause wording gap). Neither is a crash or a functional regression — both are "ships but doesn't fully deliver the clarity the ticket asked for" — send back to Loomwright (depth layering) and Lorena/Loomwright jointly (HUD wording) rather than merging as-is. Separately, and not this bundle's fault: the live debuff-pulse HUD rendering itself still has zero real-input verification behind it (mine included) — flag that to the developer as a required manual playtest step before considering #57 fully done, not just typechecked.

## 2026-08-04 (9) — Critique of Composer's first track, backlog 4.9: the mini-boss/Director trial ("the Invigilator") combat loop

New lens on the panel, per `docs/adr/0002-unblock-audio-scope-add-composer-agent.md`: Composer's music, checked against Lorena's brief, never against Composer's own self-report. Read Lorena's brief in full (`docs/agents/lorena/log.md`, 2026-08-04 entry) and Composer's log entry (`docs/agents/composer/log.md`, 2026-08-04 (2)) for what's *claimed*, then independently re-derived every checkable number from the actual delivered file, `public/assets/audio/music/boss-1-invigilator-trial-theme.mid`, rather than trusting Composer's arithmetic. Used both `mido` and `music21` (`pip install`ed into my own scratch venv, not reusing Composer's) so two independent libraries' parsers agree, not just one.

**Independent re-derivation, not taken on Composer's word:**
- `mido.MidiFile`: type 1, 5 tracks (1 tempo/meta + 4 instrument), `ticks_per_beat` 10080. `set_tempo` meta message = 625000 µs/beat → `mido.tempo2bpm(625000)` = exactly **96.0 BPM**, matching the brief's "96 BPM, straight 4/4" with no rounding ambiguity.
- Per-track `note_on` counts, recomputed from the brief's own stated structure rather than copied from Composer's log: Violoncello 59, Harp 192 (= 24 bars × 8 eighth-notes/bar, exact), Warm Pad 48 (= 24 bars × 2-note drone chord, exact), Tubular Bells 3 (once per 8-bar cycle × 3 cycles, exact). All four match what Composer's log claims — independently confirmed, not just re-stated.
- Every `note_on` has a matching `note_off`/zero-velocity `note_on` across all five tracks (**0 unmatched events**) — the file is structurally well-formed, not just nominally parseable. Pitch range 38–70 (D2–Bb4), velocity range 34–58: both inside valid MIDI bounds (0–127) and consistent with the brief's low-dynamics, unhurried instruction — nothing pegged at max velocity anywhere in the file.
- `music21.converter.parse` round-trip: 4 parts recovered, instrument names Violoncello/Harp/Sampler×2 (the generic warm-pad instrument, expected — GM program 89 has no dedicated music21 class)/Tubular Bells — 4 distinct instrument voices, matching the brief's four-part instrumentation (solo cello, plucked ostinato, sustained pad, sparse bell).
- **Length:** file reports `mid.length` = 60.625s, not the 60.0s the notation implies. Traced the discrepancy to its actual source rather than shrugging at a rounding note: every one of the 5 tracks' `end_of_track` meta event carries a uniform 10080-tick (exactly 1 quarter-note / 0.625s at 96 BPM) trailing delta — a boilerplate padding `music21`'s MIDI writer appends after the last real event on *every* track, not a musical difference between tracks. Confirmed by summing raw tick deltas per track directly (bypassing both libraries' higher-level `.length`/`.duration` convenience properties): every track's last real note-off lands at tick 967680 (quarter 96) except Tubular Bells (quarter 68, correctly — its last of 3 bell hits ends there and nothing plays after). MINOR, not BLOCKING: this is uniform silent padding, not missing or shifted content, and 60.625s is still inside the brief's stated 45–75s loop-length target either way.
- **Loop-point check (the brief's own stated goal — "could keep going indefinitely," "no big finale swell"):** the harmonic cycle ends on VI (Bb–D–F) and loops back to i (D–F–A) — these triads share two of three tones (D, F), so the harmonic jump at the loop seam is a genuine near-resolution, not an audible hard cut. The melody's final phrase ends on a held D4 (the tonic), and the looped repeat's first note is also D4 — the melody itself resolves onto the same pitch it restarts on. The pad drone (D3/A3) never changes across the whole file, so it carries through the loop seam with zero discontinuity. This is a real, checkable loop-friendliness property, not an assumed one.

**BLOCKING**

None.

**MAJOR**

None.

**MINOR**

1. **Systems designer — the harp ostinato's degree pattern is a fixed 4-step cycle (`[0,1,2,1]`) repeated identically across every bar of all 24 bars**, so while the *pitches* change with the underlying chord, the *rhythmic/contour shape* of the ostinato never varies for the full 60 seconds. Matches the brief's "steady... pulse... mechanical, precise, ticking" instruction on its face, but a boss fight a player may hear many times per playtest session risks this reading as repetitive rather than deliberately ritualistic past the first minute or two. Not a defect against the brief as written — flagging as a possible future-pass refinement (e.g. a subtle pattern variation per cycle, the way the cello melody already gets three related-but-distinct phrases) rather than a blocking gap today.
2. **Feasibility lead — the reported/actual length mismatch (60.0s notated vs. 60.625s as the file actually plays) is real, checkable, and worth naming precisely rather than waved off as "close enough,"** even though it's harmless here (uniform end-of-track padding on every track, see above, not a musical add). Recommend Composer strip or account for this padding in any future track's stated length, since a future track without this exact analysis attached could let a real content bug (an unintended extra bar) hide behind the same "eh, it's basically the notated length" assumption this one turned out to deserve.

**CLEAR (specifically checked, not just asserted)**

3. **Narrative critic — mood matches the Invigilator's established "testing, not cruelty" characterization, not a generic boss-fight bombast track.** No brass hits, no choir climax, no chromatic/raised-leading-tone harmonic-minor "horror" color anywhere in the chord list (i-iv-v-VI all diatonic natural-minor triads, verified against the actual pitch classes used, not just the brief's chord labels) — consistent with `lore-premise.md`'s Invigilator being "too smooth, too attentive, more curious than cruel," and with the outro's "you do not feel triumphant so much as tired" register the brief explicitly asked the track to echo.
4. **Player psychologist — the track's flat, non-escalating energy across all three phase-implying cycles is the felt experience the brief actually wanted, not an oversight.** The brief asked for a track that "could keep going indefinitely" rather than building to a finale, matching the Invigilator's clinical, ungoaded testing posture rather than a typical escalating-danger boss theme; the composed piece delivers exactly that (verified: velocity and instrumentation stay within the same narrow bands across all three 8-bar cycles, no per-cycle intensity ramp).
5. **Adversarial QA — deliberately tried to find broken data rather than assume a clean self-report, and found none.** Checked for the failure modes that actually break a MIDI file in practice: unmatched note on/off pairs (0 found), out-of-range pitch/velocity values (none found), degenerate zero-length notes (none — every duration in the generation script is a defined positive `Duration`), and tempo/time-signature drift between the 4 parts' individually-embedded marks (all 4 agree, 96 BPM / 4/4, no part silently diverging). Nothing here would fail to load in a standard MIDI player or DAW.
6. **Business analyst / Feasibility lead — target-naming constraint is met, not a style-note violation.** Composer's log and the delivered file both name one specific, already-shipped target (`src/data/waves/boss-1.json`'s 3-phase mini-boss/Director trial) — satisfies Composer's own AGENT.md constraint ("a track with no named target is a constraint violation, not a style note"). Generation method is deterministic/notation-based via `music21` (script checked in and independently re-runnable), not a raw-audio or notation-as-text model call — satisfies the ADR's generation-method constraint.

**Overall verdict: this track clears the gate.** Every number in Composer's self-report was independently reproduced from the file itself using a separate toolchain (mido, not just music21) rather than trusted at face value, and every one checked out: 96 BPM exactly, 4 distinct instrument voices, a well-formed MIDI structure with zero broken note-pairing, and a loop point that's genuinely smooth by a checkable harmonic/melodic-tone-overlap criterion, not just asserted to be. The two MINOR findings (a static ostinato pattern that may wear thin over repeated listens, and an unexamined 0.625s padding artifact in the file's reported length) are both real but neither blocks this from being an honest, brief-matching first track — track record this for a future revisit, not a send-back. No BLOCKING or MAJOR findings; nothing sent back to Composer this round.

## 2026-08-07 (2) — Issue #126 (epic #124): the three "Opening Magic" resource treatments (Deterministic Original, CC0 Remix, Hybrid), pre-#128 gate

Critiqued the three parallel Tilesmith/Composer treatment packs under `public/assets/prototypes/opening-magic/` against `opening-experience-brief.md`, the design spec's own acceptance criteria, and `art-sourcing-contract.md`, before #128 wires any of them into the Active Prototype scene. Neither generating agent reviewed the other's output or a human's; this is the first independent check. Read both `provenance.json` files, the hybrid pairing manifest, and both agents' 2026-08-07 log entries in full, then went past self-report: recomputed sha256 on 6 of the 10 recorded output files myself (all matched exactly — no hash drift found), pulled `linuxserver/ffmpeg` via Docker to run real `ffprobe`/`volumedetect`/`astats` passes on both audio files (neither agent had `ffprobe` available end-to-end for the *rendered* deliverable, only for source inspection), and built pixel-accurate nearest-neighbor contact sheets plus per-glyph average-RGB/HSV measurements for all 8 runes and all 3x2 VFX atlases rather than eyeballing small PNGs directly. Did not accept either agent's own "reads fine" framing without an independent check, and looked past their own disclosed gaps for anything neither caught.

**BLOCKING**

1. **Adversarial QA — Deterministic Original's shipped `.ogg` is not the length either agent verified, and the gap is exactly the kind that breaks a loop.** Composer's log verifies `mido.MidiFile(...).length` = 45.469s on the **source `.mid`** and stops there; nobody ever measured the actual **rendered `.ogg`** — the file the brief calls "the browser-ready render" and the one a scene would actually load. `ffprobe` (via `linuxserver/ffmpeg` in Docker) reports the real file at **48.617s**, not ~45s — a 3.6s/8% gap unexplained by the already-disclosed 0.469s notation-padding artifact. Segment-by-segment `volumedetect` traces exactly where the extra time goes: mean volume holds steady around −41dB from 0–45s (the actual composed music), then drops to −61.8dB for 45–46s and to −87 to −90dB (noise floor) from 46–48.6s — a FluidSynth instrument-release/reverb tail, not extra musical content, but audio nonetheless. Looped naively (`loop: true` on the whole file, the standard Phaser pattern), this produces roughly 3.6 seconds of near-silent dead air between every repeat of the loop — the opposite of the brief's "must loop/mix cleanly" requirement and the design spec's "Audio loops cleanly... does not clip" acceptance line. This is a real, unverified, checkable defect in the one treatment whose music is original — not a subjective taste call.
2. **Business analyst / Feasibility lead — CC0 Remix is missing a required asset role the design spec itself defines for this treatment, and it fell through the crack between both agents.** The design spec names the treatment's contents explicitly: "existing Kenney terrain plus individually verified CC0 runes, particles, ambience, **and a temporary music reference**." No such file exists anywhere under `public/assets/prototypes/opening-magic/cc0-remix/` (confirmed by directory listing — only `glyphs/`, `vfx/`, and `audio/forest-ambience.mp3`). Composer's log explicitly disclaims it — "the CC0 Remix treatment's temporary music reference (not a Composer track)... out of scope for this entry" — implying someone else owns it, but Tilesmith's own CC0 Remix entry never produced or mentions one either; only 3 downloads (Rune Pack, Particle Pack, Forest Ambience) are logged. Net result: CC0 Remix ships with **zero music**, only ambience — a materially thinner audio role than the other two treatments (both of which carry a full composed loop), which breaks the "same asset roles, switchable without changing gameplay" acceptance criterion and makes any 3-way "which treatment communicates magical excitement within 30 seconds" comparison (Prototype 1's own acceptance bar) uneven by construction, not by design intent.
3. **Systems designer — CC0 Remix breaks a 100%-consistent repo convention with no disclosure.** Every one of the 9 existing sourced third-party asset directories in this repo (`kenney-roguelike-rpg-pack`, `kenney-tiny-dungeon`, `kenney-impact-sounds`, `kenney-digital-audio`, `tiny-creatures`, `opengameart-fireball`, `opengameart-freeze-spell`, `opengameart-earth-element-magic-spell`, `opengameart-electricity-game-sound-pack`) bundles its own `License.txt` (verified by direct `find`). `public/assets/prototypes/opening-magic/cc0-remix/` sources from 2 Kenney packs and 1 OGA asset — the exact category this convention exists for — and has no `License.txt` anywhere under `public/assets/prototypes/opening-magic/`. The license *information* is present in `provenance.json` (see CLEAR finding 6 below, that part is genuinely fine), but the bundled-file convention itself, which every prior sourced asset in this codebase follows without exception, was silently dropped and neither agent's log flags the deviation.

**MAJOR**

4. **Player psychologist — Deterministic Original's `impact-flame_sweep.png` fades to near-invisible in its own back half, a readability defect Tilesmith's self-check couldn't have caught because it only viewed the atlas against a neutral checkerboard.** Compositing the same 4 frames over a black background (approximating a shadowed terrain patch, since the real Level 1 art has darker stone/forest passages) shows frames 3–4 of the ring reading as a near-solid dark blob with only a faint reddish rim — consistent with the earlier finding that these frames are a fading dark-red stroke, not a bright ring. Measured: frame 1 renders as a crisp bright ring; by frame 3–4 the stroke's own color has drifted close to black, so fading *alpha* and fading *toward black* compound instead of one canceling the other's visibility cost. By contrast, CC0 Remix's equivalent ring VFX (built from `circle_03.png`) stays a bright warm-orange outline at full apparent brightness through all 4 frames (it only grows and fades alpha, never darkens in hue) — the opposite of what Tilesmith's own comparison note implied ("softer/more pastel" was framed as strictly worse; for this specific VFX, CC0 Remix's impact ring is the more reliably readable of the two across its full animation, which neither agent's report credits it for). This directly touches the brief's "preserve clear silhouettes... decoration must not... obscure" line for the one moment (a spell impact) most likely to occur near or on top of an enemy.
5. **Systems designer — CC0 Remix's rune glyphs are a measurably compressed palette, not just a different style, against the brief's own instruction to concentrate saturation in runes.** Average-pixel HSV across the 4 glyphs: Deterministic Original spans 55–77% saturation (cyan 58%, gold 77%, violet 55%, ember 75%); CC0 Remix spans only 32–54% (cyan 32%, gold 38%, violet 35%, ember 54%) — every single color is less saturated than its Deterministic Original counterpart, not a mixed bag. Pairwise RGB distance between the two closest colors (gold/ember, the classic warm-hue confusion risk) is 58.3 in Deterministic Original vs. 53.8 in CC0 Remix — closer together in the treatment that's already the more washed-out of the two. Tilesmith's own log disclosed a *style* difference ("tinted stone tile... not a glowing drawn rune"); this is the same root cause but a sharper, checkable claim: all 4 CC0 Remix glyphs also share one identical outer silhouette (a rounded-square tile) and differ only by a thin, low-contrast engraved mark near the same luminance as its own background (confirmed visually at 8x nearest-neighbor zoom) — meaning silhouette alone, which the brief says must read "immediately," carries almost no distinguishing information in this set; only the faint engraving and the (now-quantified-as-weaker) tint do the work Deterministic Original's silhouette shape alone already accomplishes.
6. **Feasibility lead — the Hybrid manifest's "no new assets needed" framing is procedurally true but substantively incomplete, because it silently inherits both defects above.** The manifest is well-formed (parses clean, is pure path pointers, duplicates no binary — verified), so the *manifest itself* has no defect. But because Hybrid reuses Deterministic Original's glyphs/VFX/music files byte-for-byte, findings 1 and 4 above (the `.ogg`'s undisclosed loop-breaking tail, and the impact VFX's fade-to-near-black back half) are not "Deterministic Original problems Hybrid avoids by using CC0 terrain" — they ship into Hybrid unchanged. The manifest's own text ("the referenced Deterministic Original files are visually/technically fine standing alone") is asserted, not verified anywhere in either agent's log; this critique is the first time anyone actually checked that claim, and it does not fully hold.

**MINOR**

7. **Narrative critic / Adversarial QA — Deterministic Original's `trail-fire.png` shows almost no inter-frame variation.** All 4 frames of the 4-frame atlas render as visually near-identical dot-with-halo shapes at 8x zoom (unlike the cast and impact atlases, which visibly progress). This may be an intentional static "reused every tick" sprite for a moving emitter rather than a self-animating one (Tilesmith's log frames it as generalizing `spawnRangedProjectile`'s existing dot-with-stroke convention, which is itself static), so not flagged as a defect — but it means the "trail" role's *visual* interest is coming entirely from engine-side motion, not from the asset, worth confirming intentional at #128's wiring stage rather than assuming.
8. **Business analyst — Forest Ambience's ID3 tags name a different individual than the provenance record's declared creator.** `provenance.json` and the OGA page both attribute the track to "TinyWorlds"; the file's own embedded ID3 tags (read via `ffprobe`, not checked by either agent) list `artist: "Rick Hoppmann"`, `album: "Ludum Dare 29"`, `date: "2014"`. Almost certainly the real name behind the "TinyWorlds" handle (consistent with the Ludum Dare framing already in Tilesmith's notes) and CC0 waives attribution regardless, so this is not a license problem — but it's an unreconciled discrepancy in the record that a more thorough license audit than either agent ran would have surfaced.

**CLEAR (specifically checked, not just asserted)**

9. **Systems designer — every provenance hash spot-checked reproduces exactly.** Recomputed sha256 independently for `deterministic-original/glyphs/rune-cyan.png`, `deterministic-original/vfx/trail-fire.png`, the `.mid`, and the `.ogg` (all 4 matched `provenance.json` exactly), plus `cc0-remix/glyphs/rune-cyan.png` and `cc0-remix/vfx/trail-fire.png` (both matched). No hash drift found anywhere sampled; the reproducibility claims both agents made check out under independent recomputation, not just re-reading their own script.
10. **Adversarial QA — neither audio file clips.** `ffmpeg -af volumedetect` on the full length of both files: Deterministic Original's `.ogg` peaks at −27.9dB, CC0 Remix's `.mp3` at −28.2dB — both comfortably under 0dBFS, consistent with "no clipping" for the acceptance criterion's clipping half specifically (the loop-point half is where finding 1 lives).
11. **Feasibility lead — CC0 Remix's own disclosed "not a full seamless-loop proof" for Forest Ambience holds up under a tighter re-check, and isn't hiding a worse problem.** Re-ran the edge comparison with `astats` over the first/last 1 second (not just 100ms) and got RMS levels of −51.0dB (start) and −53.4dB (end) — a ~2.4dB gap, comparable-order-of-magnitude and not a red flag, but still not proof of a phase-matched seamless loop. This corroborates, rather than resolves, Tilesmith's own disclosed limitation: a real crossfade/perceptual check still needs actual browser playback, exactly as already flagged — no worse and no better than self-reported.
12. **Business analyst — `cc0-remix/provenance.json`'s license fields are genuinely complete, not placeholder.** All 3 sources (`kenney-rune-pack`, `kenney-particle-pack`, `oga-forest-ambience`) carry populated `spdx`, `url`, `declaredOn`, `attributionRequired`, and `humanVerifiedBy` fields — none null, none boilerplate-copied (the OGA entry correctly differs in `publisher`/`assetPage` shape from the two Kenney entries). `deterministic-original/provenance.json`'s toolchain fields (base image digest, built image ID, Python/Pillow versions, exact command, per-file sha256+dimensions) are equally complete for an "originated" (not "sourced") asset kind.
13. **Narrative critic — Deterministic Original's 4 glyph silhouettes are genuinely distinct without relying on color**, confirmed by viewing them at 8x nearest-neighbor zoom independent of hue: a frost-cross, a hooked ring-with-notch, a diamond/eye shape, and a flame-diamond are all distinguishable in outline alone, which is what the brief's "clear silhouettes" line is actually asking for. This one is a genuine, unqualified strength, not a disclosed-and-verified gap.

**Overall verdict, per treatment (ADR-0001 status vocabulary):**

- **Deterministic Original: `blocked-with-reason`.** The generation pipeline, determinism proof, and glyph/cast-VFX quality are all genuinely solid (findings 9, 13) — but finding 1 is a concrete, unverified, loop-breaking defect in the one deliverable this treatment is supposed to prove out (an original composed loop), and finding 4 is a real readability gap in the impact VFX's second half. Neither is a rewrite-everything problem: 1 needs either a proper loop-length trim/crossfade at the render step or an explicit non-looping duration correction, and 4 needs the fade curve to stop drifting toward black. Not shippable to #128 as-is without at least a documented fix or an explicit accepted-risk note for both.
- **CC0 Remix: `blocked-with-reason`.** License provenance is genuinely clean (finding 12) and the impact VFX is quietly the more durable of the two treatments' rings (finding 4's flip side) — but finding 2 (a required asset role entirely absent) and finding 3 (a silently broken repo-wide convention) are both concrete process gaps, and finding 5 upgrades Tilesmith's own disclosed style concern into a measured, brief-relevant saturation/silhouette deficit. None of these are unfixable, but none are minor either.
- **Hybrid: `blocked-with-reason`, contingent rather than independent.** The manifest itself is clean (finding 6's first half) and needs no rework of its own, but its "expected winner" status inherits Deterministic Original's two unresolved defects (findings 1 and 4) wholesale, undocumented. Hybrid cannot be marked validated ahead of — or independently of — Deterministic Original's own fixes; treating it as a separate, cleaner treatment because it borrows CC0 terrain is not currently justified by the actual files it points to.

No treatment in this bundle clears to `shipped-and-validated` yet. All three carry fixable, specifically-scoped issues; none are stylistic disagreements dressed up as findings.

## 2026-08-09 — Issue #125: CC0 Remix VFX applied to the real Level 1 opening (independent review of Loomwright's integration)

Critiqued the actual production integration — not the throwaway `?prototype=openingmagic` audition scene, which this same change removes — against issue #125's acceptance criteria, `opening-experience-brief.md`, and the developer's own #128 playtest verdict (comment on issue #128, 2026-08-09: VFX direction confirmed, CC0 Remix's softer/pastel look preferred; all 3 audio tracks rejected for combat). Read the diff to `SpellroadScene.ts`/`main.ts`, the new `src/systems/openingVfx.ts` + its test, the relocated `public/assets/vfx/opening-magic-cc0-remix/` directory, and independently re-derived several claims rather than trusting Loomwright's own log at face value: recomputed sha256 on all 3 relocated PNGs against `provenance.json` (match), read `SpellCaster.ts`'s actual `SHAPE_GEOMETRY` constants rather than trusting the code comments' stated numbers, and enumerated every fire-element spell in `spells.json` directly rather than assuming only `flame_sweep` exists.

**MAJOR (caught in Loomwright's own self-review, fixed before this pass, independently re-verified here)**

1. **Systems designer — the fire trail's travel distance was hardcoded to the cone shape's own radius regardless of the casting spell's actual shape, which would have made `magma_lance` (element `fire`, shape `line`, real reach 220px per `SHAPE_GEOMETRY.LINE_LENGTH`) visibly undershoot by up to ~94px.** `spawnOpeningVfxCast`'s first version capped `travelDistance` at `SHAPE_GEOMETRY.CONE_RADIUS` (180px) unconditionally — correct for `flame_sweep`/`flare_jab` (both `cone`), wrong for `magma_lance`. This wouldn't have broken the actual hit/damage math (`SpellCaster.tryCast`'s own shape-correct `hitTest` is untouched by this change) but would have made the new decorative trail visibly stop short of where the spell's real cone/line boundary and the existing `traceAoEShape` flash both extend to — a legibility mismatch between the new VFX layer and the spell's real reach, on the one element this ticket was supposed to get right. Independently re-derived the fix rather than trusting the log's claim: confirmed `SHAPE_GEOMETRY.LINE_LENGTH = 220`, `CONE_RADIUS = 180`, `CIRCLE_RADIUS = 90` directly in `SpellCaster.ts`, confirmed the shipped code now branches on `spell.shape` before computing `reach`, and live-exercised it via the sandboxed pane's frame-pump technique: cast `magma_lance` at a target 300px away (past both radii), stepped the tween to completion, and confirmed the trail sprite is cleanly destroyed only after tweening toward `220 * 0.7 = 154px` past the mage, not the old buggy `180 * 0.7 = 126px` cap (observed position 261.7 absolute-x mid-tween, already past the old cap's 306 absolute-x target well before completion, i.e. inconsistent with the old formula and consistent with the new one). This is disclosed as a MAJOR finding on the merits — it's a real, checkable defect class — even though it was already fixed before this review ran; Heckler's job is to check the actual shipped diff, not accept "I found and fixed my own bug" as a substitute for independently confirming the fix.

**MINOR**

2. **Player psychologist — the fire-only VFX scope quietly covers 3 spells, not the 1 the developer actually reviewed.** `spells.json` has exactly 3 fire-element spells: `flame_sweep` (cone, the one the #128 playtest showcased), `flare_jab` (cone), and `magma_lance` (line). `spawnOpeningVfxCast`/the impact-sprite branch in `spawnImpactBurst` both gate on `spell.element === "fire"`, not `spell.id === "flame_sweep"`, so `flare_jab` and `magma_lance` get the same CC0 Remix sprite treatment automatically. This is a defensible, disclosed interpretation (this codebase's own existing convention, `ELEMENT_EFFECT_COLOR`/`spellIcons.ts`, already treats *element* as the visual-identity axis, not individual spell id — Loomwright's code comments name this explicitly), and finding 1's fix means it no longer misrepresents reach for the line-shaped one. Still worth naming plainly: the developer's own playtest verdict is textually scoped to "flame_sweep's cast/impact/trail effects," and this integration is a deliberate, reasoned extrapolation one step beyond that literal scope, not a re-confirmation of it. Not blocking — flagging so nobody later cites the #128 playtest as having reviewed `flare_jab`/`magma_lance` specifically, because it didn't.
3. **Adversarial QA — the new fire impact sprite (48x48 per frame) is larger than the pre-existing tinted burst it layers on top of (max diameter 36px, `IMPACT_BURST_DURATION_MS` radius 18px) and, like that existing burst, briefly renders above the enemy's HP bar** (`Enemy.ts`'s status overlay has no explicit depth, defaulting to 0; the new sprite is depth 11). Checked whether this is a new problem or a matter of degree: it's the latter — the pre-existing burst already sat at the enemy's exact position with no depth coordination with the HP bar, so this widens an already-accepted tradeoff rather than introducing a new one. Not blocking (the animation is ~250ms, matching the existing burst's own `IMPACT_BURST_DURATION_MS` of 260ms, so the occlusion window doesn't materially lengthen), but worth recording as a real, measured size difference rather than asserting "no change" outright.
4. **Business analyst — the acceptance criterion "Music lifecycle and gameplay SFX mix are correct" is only partially addressable by this ticket, and the log is honest about which part.** Verified directly: no line in the diff touches `sfx.ts`, `bgm.ts`, or any `this.sound.play`/`this.sound.add` call site — the existing per-element cast SFX, the shared `hit`/`impact`/`enemyDeath`/`playerDeath` cues, and the boss theme's lifecycle are all byte-for-byte unchanged (confirmed via `git diff` showing zero hunks in those files/call sites). So the "gameplay SFX mix" half of this criterion is trivially satisfied by non-regression, but the "music lifecycle" half — a combat-appropriate cue for first enemy contact — cannot be satisfied at all yet, since #142 (the ticket that owns sourcing/composing that cue) hasn't landed. Confirmed #142 is still `OPEN` via `gh issue view 142`. This is correctly disclosed as a residual in Loomwright's own log rather than papered over with one of the 3 already-developer-rejected calm tracks, which is the right call — but it does mean this specific acceptance line cannot honestly be marked satisfied by this PR alone, only "not regressed, and not silently faked."

**CLEAR (specifically checked, not just asserted)**

5. **Systems designer — the 3 relocated production VFX files are byte-identical to the developer-reviewed originals, not regenerated.** Recomputed sha256 for all 3 (`cast-flame_sweep.png`, `impact-flame_sweep.png`, `trail-fire.png`) under `public/assets/vfx/opening-magic-cc0-remix/` and matched them against both the new `provenance.json`'s recorded hashes and the original `public/assets/prototypes/opening-magic/cc0-remix/vfx/` files' hashes recorded in git history (commit `83e0ea9`) — all 3 match exactly on both sides. What the developer actually reviewed at `?prototype=openingmagic` is pixel-for-pixel what now ships in the real scene, not a re-derivation that could have quietly drifted.
6. **Adversarial QA — every non-fire element's cast/impact visuals are provably untouched.** Diffed `ELEMENT_EFFECT_COLOR`, `spawnCastEffect`'s flash/SFX block, and `spawnImpactBurst`'s tinted-ring block line-by-line against the pre-change version: zero changes outside the two new `if (element === "fire")` / `if (spell.element === "fire")` additive branches. Live-exercised directly (not just read): cast `frost_nova` (ice) and its impact burst produced only the pre-existing translucent circle flash and thin tinted ring, with no fire sprite bleeding in — screenshot-confirmed against the fire-element cast/impact screenshots taken in the same session, which visibly show the extra sprite layer fire gets and ice does not.
7. **Feasibility lead — the Active Prototype cleanup actually follows `prototype-harness.md`'s own lifecycle, not just a claim that it does.** `main.ts`'s `PROTOTYPE_REGISTRY` is confirmed empty (`{}`), `PrototypeOpeningMagicScene.ts` and `dev/openingMagicTreatments.ts`/`.test.ts` are confirmed absent from the working tree, and `public/assets/prototypes/opening-magic/` is confirmed fully removed (`find` returns nothing) while the exact same commits (`83e0ea9`, `f52c65d`, `20eddb7`, `94e1349`) that produced those files remain reachable in `git log --all`, satisfying "evidence preserved in history, live throwaway removed" rather than one half without the other. Live-loaded the built game with no `?prototype=` param and confirmed Title -> New Game -> Level 1 boots exactly as before (screenshot-confirmed, zero console errors, correct HUD/hotbar/enemy render) — the registry-emptying didn't silently break the normal boot chain.
8. **Narrative critic — nothing in this change touches, reopens, or preempts issue #70's lore-delivery-density question**, which issue #125's own scope note explicitly guards against. Confirmed no new lore/flavor text was added anywhere in the diff, and confirmed (via `gh issue view 68`'s own resolution comments) that the side-pocket/shrine mechanic itself was never implemented in `SpellroadScene.ts` in the first place — so there was no shrine/lore feature in production for this change to touch, silently gate behind #70, or accidentally ship ahead of that decision. The absence of any side-pocket/shrine code in this diff is the correct outcome here, not an oversight.

**Overall verdict:** clears to `shipped-and-validated` at the code/asset level Heckler can independently check (per ADR-0001) — the one MAJOR finding was a real defect class, but it was already caught and correctly fixed before this review, and re-derived independently rather than taken on faith. Findings 2-4 are disclosed scope boundaries, not defects: the fire-only, 3-spell-wide extrapolation is a reasoned, named judgment call; the impact-sprite size increase is a measured, non-blocking widening of an already-accepted tradeoff; and the music-lifecycle acceptance criterion is honestly reported as unsatisfiable until #142 lands, not quietly declared done. The one gate this cannot certify is the same one Loomwright's own log already leaves open: "the developer reports the beginning feels exciting and magical" needs an actual developer playtest with a real, focused, visible tab — no amount of frame-pump verification or independent re-derivation substitutes for that.

## 2026-08-09 (3) — Issue #142 (epic #124): the new ordinary combat-encounter loop, checked against Lorena's brief

New track, new brief, independent pass — read Lorena's brief in full (`docs/agents/lorena/log.md`, 2026-08-09 entry "Issue #142... music brief for the ordinary combat-encounter loop") and Composer's log entry (`docs/agents/composer/log.md`, 2026-08-09 entry) for what's *claimed*, then re-derived every checkable number from the actual delivered files directly, in my own fresh scratch venv (`mido`, `music21`, `pretty_midi`, `soundfile` — not reusing Composer's environment, same standing practice as the 2026-08-04(9) boss-1 critique). Read `opening-experience-brief.md` and re-pulled the two prior tracks' own logged numbers for a direct side-by-side, since the entire point of this ticket is contrast against them, not just conformance to a brief in isolation. Also grepped `src/` for any reference to the new asset — none found, confirming the "do not wire playback" constraint held.

**Independent re-derivation, not taken on Composer's word:**
- `mido.MidiFile`: type 1, 5 tracks (1 tempo/meta + 4 instrument). `set_tempo` → `mido.tempo2bpm` = **136.0 BPM** (reported 136.0001, tick-rounding noise, not a real deviation) — matches the brief's 132-140 BPM range with room on both sides, not just barely inside it.
- Per-track `note_on`/`note_off` counts, structural integrity: Viola 128/128, Low Tom 48/48, Horn 16/16, Violin 256/256 — **zero unmatched note on/off pairs** across all four tracks. Pitch range 43-64 (G2-E4), velocity range 30-82: both inside valid MIDI bounds and inside the brief's own "restrained dynamics" intent — nothing pegged near max velocity (127) anywhere.
- **Channel-routing check, specifically because this exact bug already bit the opening-magic script once** (`composer/log.md`, 2026-08-07 entry, `Woodblock()` auto-routed to MIDI channel 10 where a GM program-change is meaningless): the "Low Tom / Frame Drum" part sits on channel 1 (0-indexed), program 116, `is_drum=False` per `pretty_midi`'s own read. This is the check that matters most for this specific part — `pretty_midi.Instrument.synthesize()` returns **all zeros** for any instrument flagged `is_drum=True` (confirmed by reading the installed library's own source, not assumed) — so if this part had been silently misrouted the way the opening-magic script's woodblock was, the entire percussion pulse the brief calls "a constant rhythmic signal, not decoration" would have rendered as total silence with no error or warning anywhere. It didn't: the fix Composer's own header comment cites (a generic `instrument.Instrument()` with an explicit `midiProgram` instead of a percussion-class instrument) genuinely holds here.
- `music21.converter.parse` round-trip: 4 parts recovered, instrument names Viola/["Low Tom / Frame Drum", "Taiko", "Taiko"]/Horn/Violin (the double "Taiko" labels are `music21`'s own GM-program-116 naming convention leaking through, not a duplicate-instrument bug — only one part, one instrument track). Viola/Low Tom/Violin each report 64.0 quarter-notes duration (= 16 bars, exact); Horn reports 60.0 — checked, not just noted: this is the same explained-not-a-bug pattern the boss-1 critique already established for its Tubular Bells part (MIDI has no trailing-rest event, so a part's readback duration ends at its last real event, here the final stab-plus-same-bar-rest at bar 14, not the standalone trailing rest occupying bar 15 with no further event after it) — confirmed by recomputing the Horn part's actual last note offset (56.0) plus its stab duration and immediately-following in-bar rest (0.75 + 3.25 = 4.0) landing exactly on 60.0, not a coincidence.
- **Rendered `.ogg`, read fresh with `soundfile`, independently of Composer's own render script's printed numbers:** mono, 44.1kHz, **28.585s**, peak **0.9069 (-0.85 dBFS)**, **0 samples** at/above 0.999 — no clipping, genuine headroom, not just asserted. Windowed 100ms RMS scan: last window with audible content (>-50dB) starts at t=28.2s; the file ends at 28.585s — a **0.285s** gap between last audible content and file end, which is the deliberate fade window, not dead air. This directly checks the exact defect class Composer's own log entry says it found and fixed on this render (and separately found, unfixed, on the already-shipped boss-1 file) — independently reproduced, not trusted.

**BLOCKING**

None.

**MAJOR**

1. **Adversarial QA — every check above is structural/numerical; none of it can certify the one claim this entire ticket exists to test: does this actually sound like combat, not exploration.** Issue #142 exists because the prior three tracks were rejected by *ear*, in real playback, despite passing every prior file-level self-verification pass Composer ran on them (tempo/BPM/note-count/instrument checks all passed for the rejected opening-magic loop too — see `composer/log.md`, 2026-08-07 entry). `pretty_midi.synthesize()`'s naive additive sine-wave rendering (the same disclosed limitation the boss-1 track already carries, per `loomwright/log.md` 2026-08-05: "a plain sine-wave approximation, not the real cello/harp/pad/bells timbre") means the actual timbral identity of "viola," "taiko," "horn," and "violin" is not really present in the rendered audio at all — every part is the same sine-wave voice at different pitches/rhythms, distinguished only by which pitches and rhythms play, not by any real instrumental color. The brief's mood language ("alert," "tactically live," "on-edge") is fundamentally a *timbral and textural* judgment as much as a structural one, and a sine-wave render cannot honestly stand in for that judgment. Every number above checks out; none of them proves the track clears the actual bar the developer's playtest set. This is not a reason to block the file-level deliverable — it is a reason this cannot be marked `shipped-and-validated` off this critique alone, and the report back to the developer should say so plainly rather than let a clean numeric pass read as "problem solved."

**MINOR**

2. **Systems designer — the loop seam's harmonic overlap is real but weaker than both prior tracks' loop points, and that's worth naming precisely rather than assuming "ends on a non-tonic chord" was sufficient on its own.** The vamp ends on iv (D-F-A) and loops back to i (A-C-E): exactly **one** shared pitch class (A), versus the boss-1 loop's VI→i seam sharing two of three tones (D, F) and the opening-magic loop's V→I seam being a textbook dominant-to-tonic resolution. A one-tone overlap is still a legitimate "circling" seam, not a hard clash (no diminished/tritone relationship, no chromatic clash), and the brief explicitly deprioritized seamlessness for this specific track (it "starts and stops many times... sometimes cut short mid-phrase," unlike the trial's sustained multi-minute use) — but it is measurably the thinnest loop-continuity of the three tracks this repo has now shipped, and a future ear-check should specifically listen at the seam rather than assume the anti-cadence technique alone guarantees smoothness the way it did for the other two.
3. **Feasibility lead — the target-naming and non-wiring constraints are both met, checked directly rather than assumed.** Composer's log entry names one specific, bounded target ("the ambient combat loop for ordinary (non-boss) monster-engagement waves, Level 1-4... not the mini-boss/Director trial theme") — satisfies Composer's own AGENT.md constraint. `grep -rn "combat-encounter-loop"` across `src/` returns nothing; `git status` shows only the two new asset files, the two new scripts, and the two log edits — no playback wiring anywhere, satisfying this ticket's explicit "do not wire playback" instruction.

**CLEAR (specifically checked, not just asserted)**

4. **Player psychologist — the horn stabs' voicing (root + top voice only, no third) is modally neutral by construction, so there's no risk of an accidental major-key brightness leak on the "VII" bar (G-D, a bare fifth) reintroducing the exact "too happy" quality the developer's playtest rejected.** A perfect-fifth dyad carries no major/minor third, so none of the four stab chords can read as bright regardless of which underlying triad they're drawn from — checked directly against the actual pitches used (A3/E4, G3/D4, A3/E4, D4/A4), not assumed from the chord labels.
5. **Narrative critic — the brass reintroduction stays within the brief's own guardrail and doesn't read as a triumphant fanfare by any checkable measure.** 8 stabs across a 28.5s loop (roughly one every 3.5s), each occupying 0.75 of a 4.0-quarter bar (~19%), constant velocity (70, no crescendo across repeats), no sustain past the stab's own short duration — no swell, no build, no dynamic arc toward a climax anywhere in the data. Consistent with `lore-premise.md`'s established tone (the mage's fights are effortful and melancholic, not heroic set-pieces) and with the brief's explicit "short and clipped rather than sustained or swelling" instruction.
6. **Business analyst — direct side-by-side against both existing tracks confirms this is a structurally distinct third identity, not a variation on either.** Tempo 136 BPM vs. opening-magic's 128 and boss-1's 96 (a real, checkable gear-change in both directions); key A minor vs. opening-magic's D major and boss-1's D minor (the only track of the three in a genuinely different key center, not just a mode-flip of the same root); percussion is a continuous syncopated pulse (48 hits across 16 bars, landing off the strict downbeat) vs. opening-magic's steady on-beat woodblock accents and boss-1's 3-hits-total ritual bell; melodic/ostinato register sits in viola (mid-low) rather than celesta (bright, high) or solo cello (low, sparse, singular); and it is the only one of the three carrying any brass at all. On every axis Lorena's brief named as a contrast point, the delivered file is measurably different from both existing tracks, not just differently labeled.

**Overall verdict: `in-progress-with-owner`, not `shipped-and-validated`.** The file-level deliverable is clean — well-formed MIDI, zero broken note-pairing, no channel-misrouting silent-instrument bug (the one failure mode with real precedent in this exact pipeline), a correctly-trimmed render with no clipping and no multi-second dead air, and a composition that is measurably, checkably distinct from both prior tracks on every axis the brief cared about. No BLOCKING findings; nothing sent back to Composer for rework. But finding 1 stands: this ticket exists because a track that passed every one of these same categories of check still failed the only test that actually mattered — a human hearing it in the actual game. A clean pass here is real progress and a legitimate gate to clear before wiring, not the same thing as confirming the developer's actual complaint is fixed. That confirmation needs a real ear on the rendered file in context, ideally with a closer-to-real-instrument render if one becomes available, before this closes #142.

## 2026-08-09 (2) — Independent critique of PR #153, issue #139: brass/percussion revision of the mini-boss/Invigilator combat loop

Composer's 2026-08-09 entry above (and the PR #153 description) explicitly disclosed its own "Heckler's hat" self-critique as **not** a substitute for a real independent pass and named two open questions rather than resolving them itself. This is that independent pass — first read of this PR by any agent other than Composer. Read Lorena's original brief in full (`docs/agents/lorena/log.md`, 2026-08-04 entry "4.9: First music brief"), Composer's 2026-08-09 log entry, the generation script (`docs/agents/composer/scripts/revise-boss-1-invigilator-trial-theme-brass-percussion.py`), and the full PR diff — then went past all three and re-derived every checkable number from the actual delivered files myself, in a fresh scratch venv (not reusing Composer's), rather than trusting the self-report.

**Independent re-derivation, not taken on Composer's word:**
- `mido.MidiFile` on the delivered `.mid`: type 1, **8 tracks**, `ticks_per_beat` 10080, `set_tempo` → exactly **96.0 BPM**. Per-track `note_on` counts recomputed from scratch: Violoncello 59, Harp 192, Warm Pad 48, Tubular Bells 3 (all four **identical** to the pre-revision file — confirmed by also parsing the original `.mid` from git history, commit `dfd46d9`, not just trusting "unchanged"), Horn 96, Trombone 48, Timpani 48. Every one of these matches Composer's log exactly.
- GM program numbers on the three new tracks are correctly mapped: Horn=60 (French Horn), Trombone=57 (Trombone), Timpani=47 (Timpani), all valid 0-indexed GM patch numbers for their instrument names — not a copy-paste program-number bug.
- `afinfo` on the delivered `.ogg`: **60.000000s exactly**, 2,646,000 valid frames = exactly 60×44100, no rounding slop. `afinfo` on the pre-revision `.ogg` pulled fresh from git history (commit `dfd46d9`, not from Composer's log): **61.000000s** — independently confirms both the pre-existing dead-air defect and its fix.
- Loudness-at-downbeat, redone from raw samples (`soundfile`/`numpy`, 120ms window at each of the first 8 bar-downbeats, original vs. revised): **+0.62, +0.31, +1.03, +1.01, +0.26, +0.28, +1.02, +1.03 dB** — this lands inside, and in several bars essentially matches, Composer's claimed "+0.2 to +1.02dB" range almost sample-for-sample. Overall-file RMS: −13.17dB (orig) vs. −13.54dB (revised), matching Composer's reported figures to two decimal places. Clip-sample count (>0.999 amplitude): 40 (orig) vs. **27** (revised) — exact match. This is a case where the self-report's numbers hold up under a fully independent re-measurement, not just a re-statement.
- Loop-tail check on the revised file: last 50 samples range ±0.0003 amplitude, no discontinuity/click at the 60.0s cut — consistent with Composer's claim that the trim point was already at the noise floor.

**BLOCKING**

None found.

**MAJOR**

1. **Adversarial QA / systems designer — a factual inaccuracy in Composer's own description of the new voicing, caught by reading the generation script rather than the log's prose.** Both the composer log and the script's docstring describe the Horn as "one octave below the written harp/pad harmony register." That's not what the code does: `build_horn_part`'s dyad is `note.Note(root/third).transpose(-12)` applied to the *same* `CYCLE_CHORDS` tones the harp ostinato already transposes by the *same* `-12` in `build_ostinato_part` (`low_tones = [note.Note(t).transpose(-12)...]`), and the pad drone's hardcoded D3/A3 sits in that identical octave too. Confirmed on the actual MIDI pitch data: Horn's note range is 50–62, Harp's is 50–65, Pad's is 50–57 — same register, not a new lower layer. The genuinely new low-register content is the Trombone/Timpani pair (`transpose(-24)`, landing at 38–46, i.e. D2/F2 — one octave below Horn/Harp/Pad, correctly described as "two octaves below the written harmony"). Net effect: the Horn doubles/thickens the existing mid-register texture rather than adding a new lower brass layer the way the log's own framing implies. Not a functional bug (nothing plays wrong), but it means one of the two "why this delivers more weight" claims in the record is incorrect, and the actual mechanism is narrower than described — worth a correction note in the log so a future reader doesn't inherit the wrong mental model of this file's voicing.
2. **Narrative critic / player psychologist — Composer's own framing of the brief conflict overstates what the brief actually says, in a way that matters for judging whether this is a legitimate evolution.** The PR description and Composer's log both say "the brief said avoid brass entirely" / "avoid brass." Lorena's actual brief text (`lorena/log.md` line 176) says: *"Avoid genre-standard 'epic orchestral boss' bombast (no bwah-bwah brass hits, no choir climax)"* — a prohibition on a specific bombastic **treatment** (fanfare stabs, choir climax), not a blanket ban on the brass family. The Instrumentation section (lines 182–186) simply never lists brass among its four voices; it doesn't say "no brass." Read this way, sustained chorale-style dyads are arguably a closer, more literal match to the brief's actual words than Composer's own "explicit tension" framing admits — Composer isn't overriding a stated instruction so much as filling a gap the brief left open, while correctly steering clear of the one brass treatment (fanfare stabs) the brief did name. This changes the shape of the finding below but doesn't remove it.
3. **Adversarial QA / player psychologist — even granting the narrower brief-reading above, the specific device chosen for the percussion side sits closer to "genre-standard bombast" than the "restrained" framing credits.** Low brass and timpani doubling the root note in unison on the strong beats (1 and 3) is not a neutral orchestration choice — it is arguably *the* single most stereotypical "epic battle" gesture in the film-score vocabulary, the exact kind of "bwah-bwah" pulse-on-the-downbeat cliché that sits right next to the brief's named "no bwah-bwah brass hits" prohibition, just quieter and without the stab articulation. "Restrained" here describes dosage (velocity, two hits/bar instead of a full groove) and rhythm, not the underlying device, and the underlying device is the part most likely to read as generic rather than as *this specific* clinical, "testing not cruelty" intelligence. This is a real judgment call, not a clear violation — the developer explicitly asked for a battle feel, and this is a legible way to deliver one — but it's the place where Lorena's sign-off actually matters substantively, not just procedurally: whether the Invigilator should sound like *a* battle or like *this* battle is exactly the kind of call the brief's whole "measured rather than hated" framing was written to protect, and it wasn't checked with her.

**MINOR**

4. **Systems designer — same static-pattern risk already flagged twice in this log for other voices in this piece (2026-08-04 (9) finding 1, and Composer's own self-critique here) now applies to a third element.** Trombone/Timpani's two-hits-per-bar pulse is identical across all 24 bars/three cycles, same as the harp ostinato's fixed 4-step degree pattern already noted as a future-refinement risk. Not a defect against issue #139 (variety wasn't asked for), just a growing pattern worth someone eventually addressing across the whole piece at once rather than voice-by-voice.

**CLEAR (specifically checked, not just asserted)**

5. **Feasibility lead — technical/structural claims all hold up under a fully independent re-derivation**, per the re-derivation section above: track/note counts, tempo, rendered duration (both files), loudness deltas at the target beats, and clip-sample counts. Nothing in Composer's numerical self-report was inflated, cherry-picked, or unreproducible.
6. **Business analyst — the backlog row (`docs/agents/ana/backlog.md`, 4.9) was updated honestly, not falsely re-closed.** It appends the #139 follow-up with status `in-progress-with-owner`, explicitly says it still needs an independent Heckler pass and a developer-listening gate, and doesn't overwrite the original 4.9 closure's own developer sign-off record. Good bookkeeping under a revision, not a status-fudge.
7. **Adversarial QA — no game-balance numeric constants (fees, HP, damage, cost) appear anywhere in this PR's prose**; this is a pure audio-content change, so the "prose numbers must trace to a live constant" constraint doesn't apply here.

**On the two questions this dispatch specifically asked about:**

- **Does the composition plausibly deliver on #139's intent, independent of synth quality?** Yes, on balance — more simultaneous voices, a new low-register unison pulse landing exactly on the strong beats, and a measurable (if modest) loudness increase at those beats are a real, structural move toward "more brass and percussion... more of a battle," verified above rather than taken on faith. The one genuine reservation (MAJOR finding 3) is that the device chosen is close enough to genre-standard bombast that whether it reads as "the Invigilator, but more of a battle" versus "a generic boss fight" is a real open question a synth-quality fix cannot settle either way — that's a composition-level judgment call, not a rendering problem, and it's still open.
- **Is the brief conflict real, or a legitimate supersession?** Real, but narrower than Composer's own log claims (MAJOR finding 2) — the brief banned a specific bombastic treatment, not brass as a category, and Composer's chosen voicing mostly respects that literal line. What's actually unresolved is smaller and more specific than "brief says no brass, shipped anyway": it's whether Lorena would sign off on the *specific* low-brass-timpani-unison device given her explicit "measured rather than hated" mandate for this antagonist — a musical-judgment question, not a rule violation. I agree with Composer's own recommendation that this belongs in front of Lorena/Ana as a reconciliation note before being treated as settled, and would add MAJOR finding 3 to what she should actually be asked to weigh in on (not just "brass yes/no," but "is this specific device on-character").

**Overall verdict:** `in-progress-with-owner` stands — this does not clear to `shipped-and-validated`. No BLOCKING findings; the technical/structural work is genuinely solid and independently verified, not just self-reported. Two MAJOR findings (voicing-register misdescription; the brief-conflict framing and the underlying device-choice question) are real but neither is a "send it back to redo the composition" problem — both are documentation/reconciliation gaps, not defects in what's actually playing. Recommend: (1) developer does the disclosed listening pass per the PR's own test plan — that gate genuinely can't be closed by any further self-check or independent MIDI analysis; (2) Composer or Ana corrects the Horn-register description in the log/script docstring; (3) Lorena gets a short reconciliation note on her 2026-08-04 brief, framed around the specific low-brass/timpani device (finding 3), not just a generic "brass now shipped" flag. None of these three block a merge on their own — they're follow-ups, not conditions — so if the developer's own listen confirms it sounds like more brass/percussion without sounding like a different piece, merging as-is is reasonable; the two MAJOR findings should be tracked as fast follow-ups rather than blocking this specific PR.

## 2026-08-10 — Issue #157: Side-Pocket Lore Encounters, independent fourth-pass adversarial review

Reviewed Loomwright's implementation, Pato's economy validation, and Lorena's GDD sync independently — read every touched/new file directly rather than trusting the three self-reports, re-ran `docker-compose run --rm game npm run typecheck`/`npm test` (261/261, matching Loomwright's count) myself, and brought up a live dev server to cross-check.

**BLOCKING:** none.

**MAJOR (1):** the ticket's explicit "restrained ambient dressing" requirement (Implementation Decisions + user story 29 — "the rune does not feel dropped into an otherwise empty space") was not implemented in the first pass, and none of the three prior self-reports caught it. `createSidePocketMarkers()` created exactly one circle per encounter and nothing else. Live-confirmed via screenshot at `?debugLevel=4`: the Level 4 marker rendered as a flat, isolated dot in otherwise-untouched terrain — the literal outcome story 29 exists to prevent, and a direct miss against the ticket's own "the road feels less bare" smoke-gate criterion. Sent back to Loomwright; fixed same-session (3 static muted pebbles per marker, no new asset dependency) and re-verified clean (typecheck/test/build, plus my own re-check of the fix).

**MINOR (1):** the rune's "glow or react" language is realized as a flat-color alpha pulse only (no gradient/blend/particle) — defensible as "restrained," worth a look if a future pass revisits this area.

**Process note:** caught and corrected my own false-positive mid-review — an initial "debug-skip looks broken" signal was actually a stale container on a port held by an unrelated worktree's server, not a defect in this diff; re-verified against a correctly-scoped throwaway container before concluding anything.

**Everything else CLEAR, specifically checked, not assumed:** the `updateEnemies` interception ordering (traced the literal booleans — no off-by-one, no boss-branch conflict possible); `keydown-E`/`keydown-C` don't collide with any existing binding (grepped every `keydown-`/`addKey` call); marker rendering and the offer gate read the identical synchronous flag state, so they can't disagree; `awardPermanent` has no live interaction with `resetExpedition()`/`restoreBalance()` to break (both are dead code paths in the shipped scene, confirmed by grep, predating this ticket); `loreFlags` is genuinely treated as an opaque string set everywhere else in the codebase, so the new flag-naming convention is safe; mutation-tested 5 of the new tests by hand (reverting the implementation line each test names) and confirmed all 5 would actually catch the regression; spot-checked 2 of Lorena's specific GDD claims against the real code/numbers and found both accurate.

**Overall verdict:** does not cleanly clear the gate on first pass (one real MAJOR), clears after the same-session fix. No functional, economic, or persistence defect anywhere in the offer/reveal/reward/persistence machinery after independently re-deriving it end-to-end. Still `in-progress-with-owner`, not `shipped-and-validated` — the ticket's own live-gate items (reachability/off-route feel, prompt readability, no lore during combat, Level 5 unaffected, "less bare" after the dressing fix) need a real developer playtest, same disclosed limit every entry in this log already applies to this sandbox. See `loomwright/log.md`, `pato/log.md`, `lorena/log.md`, and `ana/backlog.md` (3.23), all 2026-08-10.

## 2026-08-12 — Issue #142, second pass: the *wiring*, not the track

Different object than my 2026-08-09 (3) critique. That one asked "does this file match Lorena's brief"; this one asks "does the engine play it at the right times, and does it leave the exploration identity alone." The asset itself is untouched since then — re-checked, not assumed: `shasum -a 256 public/assets/audio/music/combat-encounter-loop.ogg` = `ccc2311e…5d2c1902`, identical to commit `580d2bc`, and `git log -1` on both the `.mid` and `.ogg` still points at that commit. So every finding in 2026-08-09 (3) stands as written, including its MAJOR, and nothing here re-litigates it. Read `bgm.ts`, the six `SpellroadScene` call sites, `WaveLoader.spawnWave`, `waveSession.ts`, and `PauseScene.ts` directly rather than reviewing from Loomwright's log entry.

**BLOCKING**

None.

**MAJOR**

1. **Carried forward, unchanged and unfixed by this PR: nothing here can certify the cue reads as combat by ear.** This is not a new finding, it is the *same* one, and it matters more now than it did on 2026-08-09 because the track is no longer inert in the repo — it now plays, on nearly every wave in Levels 1-4, in the shipped game. Everything verified below is "does it fire at the right moment," which is a different question from "is what fires the right music." Issue #142 was opened because a track that passed every file-level check still failed a human listen; a wiring PR that passes every wiring check is subject to exactly the same gap. The developer playtest is the gate, and this PR must not be read as having moved it.

**MINOR**

2. **Combat music keeps playing over the pause menu — pre-existing behavior, newly reachable almost everywhere.** `PauseScene` never touches the sound manager (checked its full source: no `sound` reference at all; `scene.pause()` freezes the gameplay scene's timers and input, not game-level audio). The boss theme has always behaved this way, so this is inherited convention rather than a regression introduced here — but it used to be reachable only by pausing during the one Level 5 fight, and is now reachable by pausing during any wave of Levels 1-4. Correctly **not** fixed in this PR: muting or ducking on pause is a decision that would change the shipped boss theme's behavior too, which is outside #142. Flagged so it's a known, chosen state rather than an undiscovered one.
3. **The between-waves gap is now audible silence, and that is a design consequence, not a bug — but it is the thing a playtest should listen for second.** The cue stops the instant a wave clears, then the next wave's first spawn restarts it after the 1200ms auto-advance delay plus that wave's own `spawn_delay_ms`. Back-to-back waves therefore get a repeated stop/silence/restart-from-the-top cycle rather than continuous scoring. Lorena's brief explicitly anticipated and priced this in ("starts and stops many times across a single level, sometimes cut short mid-phrase"), and it is the behavior the ticket's own acceptance criterion asks for ("through the end of a wave"), so the implementation is faithful. Named here because "faithful to the brief" and "feels good in sequence" are not the same claim, and only the first one is verifiable from source.

**CLEAR (specifically checked, not asserted)**

4. **A stale wave's spawn timer cannot restart the cue after a death — the exact failure shape issue #48 was filed for.** The start call sits inside `spawnWave`'s `onSpawn`, and `WaveLoader` (read directly, lines 33-49) runs `onSpawn` **only after** its `isStillCurrent()` guard passes inside every staggered `delayedCall`. `handleDeath` calls `session.beginDeath()`, which bumps the generation, so leftovers from the wave the player died in no-op — they cannot reach `playCombatCue`. The one adjacent case I checked rather than assumed: `beginAdvance()` does *not* bump the generation (`updateEnemies` reuses the same token afterward), so a wave-clear does not invalidate pending spawn timers — but a wave can only clear when `enemiesRemainingToSpawn === 0`, meaning every spawn timer has already fired, so there is no pending spawn to leak. Both paths safe, for two different reasons.
5. **The two tracks cannot overlap.** `shouldPlayCombatCueForWave` excludes every `is_boss` wave, and that predicate is the only thing that starts the cue; boss Phase 1 additionally calls `stopCombatCue()` before `playBossTheme()`, which is the only guard covering `?debugLevel=5` (a direct boot into the trial, never preceded by a wave clear). Verified against the real data, not fixtures: no `boss-1.json` phase satisfies the predicate, every authored Level 1 wave does — asserted in `bgm.test.ts` against the imported JSON itself.
6. **Every exit is covered, including the ones the ticket didn't name.** `stopCombatCue()` is reachable from wave clear, death, boss Phase 1, the `create()` restart-reset block, and the SHUTDOWN handler; it is idempotent, and several of those can fire back-to-back with nothing playing. The wave-clear stop is placed at the *top* of the `shouldAutoAdvance` branch — checked why, and it is load-bearing: `startPhaseBreak` and `startSidePocketChoice` both `return` early from that branch, so a stop at the bottom would be skipped for precisely the two transitions where combat music lingering is most wrong.
7. **The exploration identity is genuinely untouched, verified by diff rather than by claim.** `git diff origin/main --stat` touches no file under `_reference/`, no composition script, and no existing audio asset. `opening-experience-brief.md` and `compose-opening-magic-deterministic-original.py` (which regenerates the exploration `.mid` deterministically from source) are both byte-identical to `origin/main`. Separately confirmed — and this is the part the ticket's own wording didn't anticipate — that no opening-magic audio asset is on `main` at all: issue #125 removed the Active Prototype and the losing treatments' assets, so "the exploration loop resumes after combat" is not literally implementable today. Loomwright's log entry says this outright instead of letting the acceptance checkbox read as satisfied, which is the correct handling; the identity is preserved as brief-plus-regeneration-script, which is what "still reachable" can honestly mean right now.
8. **Terrain fit is inherited, not newly asserted.** Nothing in this PR re-opens the stone/dungeon-vs-forest question; the cue's stone/dungeon suitability rests on Lorena's brief and my 2026-08-09 (3) pass, and the wiring adds no forest-coded context (the cue plays in Levels 1-4's existing tilemap, unchanged here).
9. **Independent duration re-check with a third tool.** Composer and I both used `soundfile`; this pass used macOS `afinfo`, which shares no code with either: 1 ch, 44100 Hz, Vorbis, estimated duration **28.585261s** — matching the two prior independently-derived figures to the millisecond. The dead-air-tail fix is real in the shipped bytes, not just in a log. `dist/assets/audio/music/combat-encounter-loop.ogg` after a production build is 79,125 bytes, byte-identical to the repo copy, and the minified bundle references its URL exactly once — the asset actually ships, checked in the output rather than inferred from the source string.

**Overall verdict: `in-progress-with-owner`.** The wiring is correct, minimal, and defensively placed at the two spots where this scene's existing bug history says it would otherwise leak (stale generation-bounded timers, early-returning branches). No BLOCKING findings; nothing sent back for rework. But finding 1 is the whole ticket, and it is unchanged: the only test #142 exists to pass is a human hearing this in a live fight.

## 2026-08-12 (2) — Issue #188: the two exploration-loop sibling variations, checked against Lorena's variation brief

Read Lorena's brief in full (`docs/agents/lorena/log.md`, 2026-08-12 entry — an amendment scoped to two siblings, not a new brief), the original `opening-experience-brief.md` it amends, Composer's 2026-08-12 log entry for what is *claimed*, and both generation scripts. Then re-derived every checkable number from the actual delivered files in a **fresh scratch venv on the host** (`pretty_midi` 0.2.11, `soundfile` 0.14.0, `numpy` 2.5.2) — deliberately a different toolchain from the `mido`/`music21`/`ffprobe` Composer ran inside its own Docker image, same standing practice as the 2026-08-04 (9) and 2026-08-09 (3) music critiques. **Host-venv disclosure per `docker-testing-contract.md`:** the *game* checks below (typecheck/test/build/dev-server) ran in Docker; only this audio-analysis venv is on the host, because the analysis libraries are not in either project image and installing them into one would have made this pass depend on tooling Composer controls.

Also ran the game itself rather than critiquing from a source read: `docker-compose run --rm game npm run typecheck` / `npm test` / `npm run build` all clean (319 tests, 30 files), dev server up, all three `.ogg` served **200** with `content-type: audio/ogg`, all three fetched by the scene's own preload at boot (network log), **zero console errors** on boot and on entering Level 1.

**Independent re-derivation, not taken on Composer's word:**
- `pretty_midi`: all three files report a single tempo change, `128.0` BPM at t=0, and `get_end_time()` of exactly **45.0s** — a useful cross-check on Composer's `mido` figure of 45.469s, since the two libraries disagree precisely by the end-of-track meta padding Composer's entry says accounts for it. Both readings are consistent with the claim; neither alone would have been.
- Note counts reproduced exactly as claimed: original 152/192/192/9, Sibling A **176**/192/192/**9**, Sibling B **128**/192/192/**12**. **Zero malformed notes** (no zero-length or negative-length events) in any part of any file. Velocity ranges 38-68, pitch ranges 50-114 — inside valid MIDI bounds, nothing pegged near 127, consistent with the original brief's "moderate dynamics."
- **Channel-routing check, run because this exact bug has bitten this exact script family before** (`composer/log.md` 2026-08-07: `Woodblock()` auto-routed to channel 10, FluidSynth substituting a drum kit): `pretty_midi` reports the Hand Percussion part as `program=115, is_drum=False` in **all three** files. This is the check that matters most for this part, and it passes independently of Composer's own `mido` channel dump.
- Rendered `.ogg`: all three are Vorbis, 44.1 kHz, **stereo**, **45.3000s** — identical siblings, which is what the brief asked for. Longest interior run below -60 dB anywhere in any file: **0.0s** (no dead air). Tail: last audible 100 ms window at 45.10s against a 45.30s end, i.e. a 100 ms fade-out to silence, and the loop seam steps from a -60 to -69 dB tail into a -13 to -15 dB head — no gap, and no discontinuity to click on.
- Divergence spot-checked against Composer's claim rather than accepted: the two siblings do share the original's exact diatonic pitch-class set (same key, no borrowed tones) while diverging on chord path, ornament, accent placement and bell figure. Sibling A vs Sibling B is as different as either is from the original, so this is a genuine rotation of three.

**BLOCKING**

1. **Adversarial QA / feasibility lead — the three tracks shipped ~27 dB quieter than every other piece of music in the game, which would have made the fix for "i dont hear any music" inaudible.** This is the finding this pass exists for, and Composer's own self-verification could not have caught it: every check in that entry is *intra-file* (is this file the right length, does it clip against its own peak, is its own seam clean) and all of them passed. Measured *against the mix it is joining*, the first render peaked at **-27.92 / -27.40 / -29.88 dBFS** (original / A / B) versus **+0.19 dBFS** for the shipped boss theme and **-0.85 dBFS** for the shipped combat cue — a 27-29 dB deficit, whole-file RMS -41 to -43 dB against the combat cue's -15.9 dB. At `EXPLORATION_LOOP_VOLUME = 0.5` that is roughly -47 dBFS RMS in the mix, under every SFX one-shot in `sfx.ts` by a wide margin and arguably under a laptop speaker's noise floor. Shipping it would have satisfied every acceptance criterion on the ticket while leaving the developer's actual complaint — silence — functionally unfixed, which is precisely the failure shape #142's own critique warned about ("a clean numeric pass read as 'problem solved'"). Root cause is the render step, not either composition: FluidSynth produces a conservative absolute level from these low velocities and thin textures, while `pretty_midi` (which rendered the two existing tracks) normalizes to full scale by default — so the deficit is an artifact of the two tracks in this repo having been rendered by two different synths, and nobody had previously had to compare across them.

  **Fixed before finalizing, and re-verified by me after the fix rather than taken on report:** a single common `volume=26.4dB` stage was added to the ffmpeg chain and all three re-rendered from their unchanged `.mid` sources. Shipped peaks are now **-1.72 / -0.83 / -3.53 dBFS**, RMS **-15.03 / -15.02 / -16.70 dB**, i.e. inside a ~3 dB band with the two existing tracks; **0 samples** at or above 0.999 in any file (no clipping introduced); duration, seam behaviour and interior-silence checks all re-run and unchanged. I specifically checked that the gain was applied as **one** gain rather than per-file normalization — it was, which is why Sibling B's deliberate 1.7 dB "more spacious" offset survives instead of being flattened. **Finding resolved.**

**MAJOR**

2. **Player psychologist — nothing in this pass can certify the brief's own acceptance test, which is perceptual.** Lorena's brief states it plainly and asks for it to be applied rather than paraphrased: a player who has heard the original should register "this bit sounds different," not "the music changed." Every number above measures structure. A 7.2% / 3.9% positional-pitch agreement is evidence of real variation and an identical pitch-class set is evidence of shared identity, but neither is evidence that the *gestalt* lands between "too samey to be worth three tracks" and "that's a different song." I will say that the structural profile is unusually well-suited to passing that test — same key, same tempo, same four voices, same length, same phrase-ending rule, with the differences concentrated in ornament and accent — but that is an argument, not a measurement. This needs the developer's ear, and it is the one thing that should gate calling #188 done.

3. **Systems designer — this is now a rotation of exactly three 45-second loops, which is a 135-second cycle, and the ticket's own premise is that loops eventually bother a listener.** The no-immediate-repeat rule genuinely fixes the reported problem (the same loop back to back). It does not make the material infinite, and a player who spends a long session in Levels 1-4 will hear all three several times. Not a defect against #188 as written — the developer asked for two more tracks, not for unlimited variation — but the same "static cycle repeated identically" note this log has now raised three times (2026-08-04 (9) on the harp ostinato, 2026-08-09 (2) on the trombone/timpani pulse) applies at a higher level here, and the honest framing for the developer is "this makes the repetition much less frequent," not "this fixes repetition."

**MINOR**

4. **Business analyst — the three `.ogg` files add ~1.9 MB to the shipped bundle, roughly 9x the total weight of the two existing music tracks combined** (657 KB + 663 KB + 654 KB, versus 136 KB for the boss theme and 79 KB for the combat cue). The cause is legitimate — the FluidSynth-plus-SoundFont render carries real instrument samples where the `pretty_midi` sine render carries almost no spectral content to encode — and the brief-level decision to keep the original's timbre rather than re-render it through the cheaper path is the right one for a track the developer already approved by ear. But `opening-experience-brief.md`'s own validation section names "viable on the low-spec browser target" as a criterion, all three are preloaded eagerly at scene boot, and 1.9 MB on a slow connection is a real first-load cost for music that first plays after a wave is cleared. Not blocking (the build is clean, the assets serve, and the JS bundle at 1.55 MB is already the larger problem), but worth a deliberate decision rather than drift: a lower `-q:a` on these three specifically would likely halve them with little audible cost at these dynamics.

5. **Narrative critic — Sibling B is doing something the brief asked for that no measurement will show, and it should be named so it doesn't get "fixed" later by someone reading only the numbers.** Its lower note density, longer values, backbeat lean and late octave-lift are all individually visible in the data as "less activity," which reads like a weaker track if you only look at counts. The brief's framing — "the 'stop and look at something off the road' face of the same theme," the sibling most likely to be under the Side-Pocket prompt — is the reason it is like that. Its 1.7 dB level offset is part of the same intent. Flagging so a future loudness-matching pass doesn't erase it as an inconsistency.

**CLEAR (specifically checked, not just asserted)**

6. **Feasibility lead — the wiring's mutual exclusion is real, not asserted.** `shouldPlayExplorationLoopBetweenWaves` refuses any pair where either the cleared or the next wave is a boss wave, so the Director trial's theme keeps sole ownership of Level 5 including its phase-breaks, and the Level 4 → 5 handoff is not interrupted by a one-second interlude. The scene stops this loop on the next wave's first enemy spawn — the same line that starts the combat cue — so the two cannot overlap, and the stop is unconditional there rather than mirroring the cue's boss check, which closes the one path (`?debugLevel=5`) that reaches a boss without a preceding ordinary clear. Stops are also present on death, scene restart and SHUTDOWN, matching the treatment both existing tracks already get.

7. **Adversarial QA — the rotation cannot repeat, cannot crash, and is genuinely testable.** `pickExplorationTrack` is pure, Phaser-free and takes its random source as a parameter, matching the convention `shouldPlayCombatCueForWave` set. Its tests cover every previous-track/roll pair (including the out-of-range `random() === 1` edge, which is clamped rather than indexing off the end), a 60-call chained run that sees all three tracks with no repeat, uniformity across the two candidates, the single-track fallback and the empty-list case. I re-ran the suite myself: 27 tests in `bgm.test.ts`, 319 across the repo, all passing.

8. **Business analyst — the original track is genuinely unchanged as a composition, which is what "i like the level 1 music" actually constrains.** Its `.mid` is byte-identical by sha256 to the file logged on 2026-08-07 (`50dd87f6...228def`). Its `.ogg` is not, and Composer's entry says so rather than glossing it: it is a re-render of that identical `.mid` differing only by the linear gain from finding 1. I verified there is no other difference — same duration, same trim, same fade, same note data — so "the developer's approved track, louder" is an accurate description of what shipped.

**Overall verdict: clears the gate, with one finding fixed inside this pass and one left deliberately open.** One BLOCKING finding was found and resolved (the 27 dB deficit — and it is worth stating that the ticket would have shipped and *appeared* to work without this pass, since nothing Composer could check from inside a single file would have surfaced it). Two MAJOR findings remain, neither of which further analysis can close: whether the siblings actually read as variations rather than different songs, and the honest framing that three loops reduce repetition rather than solving it. Both are developer-ear questions. The file-level deliverable, the wiring, and the rotation logic are all sound.

## 2026-08-31 — Issue #207 six-persona elemental roster review

The full persona reports and preserved disagreements live in the Task 7 SDD evidence bundle.
Initial disposition was HOLD: player/accessibility reviewers found the elemental system untaught,
the 2px frame motifs indistinguishable in mixed normal-scale/CVD views, and the boss's two
resistances visible only after a wasted cast; QA found fractional counts could soft-lock progress,
malformed resistance objects could throw, boss flags were under-constrained, and fixed slots were
not exact. Business review also blocked release readiness without reproducible final-boss
evidence.

Implementation disposition: all code/document findings above are resolved with focused RED/GREEN
tests and a reproducible capture contract. Automated verification is green at 43 Vitest files / 532
tests plus 6 catalog tests, typecheck, and build. The remaining gate is deliberately human:
Reynaldo must identify all four filled badges in normal/grayscale/protanopia/deuteranopia captures
and review the final-boss recording. Reynaldo later approved that reviewed evidence and authorized
the Issue #207 pull request and merge; unchecked live-play observations remain separate from this
code-review disposition.

### 2026-08-31 fix-loop addendum

Independent review caught two MAJOR integration gaps after the first Task 7 commit: the trial HUD
looked across the whole campaign and leaked the future unique-boss resistance cue into phases 1/2,
while direct `debugWave=5` entry skipped trial initialization because the previous authored phase
was also a boss phase. The fix binds presentation to the current wave and treats direct debug entry
as initialization of the selected authored phase. A pure regression proves ordinary phase 1/2 show
no unique-boss plate, normal phase 3 shows it without replaying initialization, and direct phase-3
entry both initializes and shows it. Non-boolean `is_boss` values now also fail validation instead
of activating runtime truthiness. Human visual acceptance remains open; these code findings close.
